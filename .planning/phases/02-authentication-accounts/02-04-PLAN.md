---
phase: 02-authentication-accounts
plan: 04
type: execute
wave: 2
depends_on: ["02-01b"]
files_modified:
  - src/app/admin/page.tsx
  - __tests__/rls.test.ts
  - __tests__/middleware.test.ts
autonomous: true
requirements: [AUTH-05, SEC-02, SEC-03]

must_haves:
  truths:
    - "An admin user reaching /admin sees the admin stub; a non-admin is redirected away"
    - "An unauthenticated request to /admin is redirected to /login by the proxy"
    - "RLS prevents an authenticated user from reading another user's profiles row"
    - "All profiles access uses the Supabase SDK (parameterized) — no raw SQL string building in application code"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "admin-only protected stub with server-side role guard"
      contains: "role"
    - path: "__tests__/rls.test.ts"
      provides: "RLS cross-user isolation test (SEC-02)"
    - path: "__tests__/middleware.test.ts"
      provides: "proxy protected-route redirect test (SEC-03 path-protection behavior)"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "profiles.role"
      via: "server-side getUser + profiles.role select; redirect if not admin"
      pattern: "role"
    - from: "__tests__/rls.test.ts"
      to: "public.profiles"
      via: "two authenticated clients; cross-read returns no rows"
      pattern: "profiles"
---

<objective>
Complete the admin-role and isolation slice: a protected `/admin` stub that only `role='admin'` users can see (AUTH-05), plus the RLS cross-user isolation test (SEC-02) and the proxy protected-route redirect test, and a confirmation that all profiles access is parameterized SDK usage (SEC-03).

Purpose: Phase 2's security guarantees — per-student data isolation and a real admin boundary — are unverified until these tests exist and the admin page enforces role server-side. This slice closes AUTH-05, SEC-02, and SEC-03.

Output: `/admin` stub with server-side role guard, a passing RLS isolation test, a proxy redirect test, and a parameterized-query confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-authentication-accounts/02-CONTEXT.md
@.planning/phases/02-authentication-accounts/02-RESEARCH.md
@.planning/phases/02-authentication-accounts/02-PATTERNS.md
@.planning/phases/02-authentication-accounts/02-UI-SPEC.md
@.planning/phases/02-authentication-accounts/02-VALIDATION.md
@.planning/phases/02-authentication-accounts/02-01b-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 (foundation) this plan consumes. -->
src/lib/supabase/server.ts:  export async function createClient(): Promise<SupabaseClient>
src/lib/supabase/admin.ts:   export function createAdminClient(): SupabaseClient
src/app/proxy.ts:            protects /dashboard, /admin, /account; redirects unauthenticated to /login?next=...
DB: public.profiles(id, username, role 'student'|'admin', ...) with RLS select/update policies scoped to auth.uid()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Protected /admin stub with server-side role guard</name>
  <read_first>
    - src/app/mission/page.tsx (analog: Server Component page wrapper, container, font/color tokens)
    - .planning/phases/02-authentication-accounts/02-PATTERNS.md (admin page pattern — verify role server-side, redirect non-admins, 1040px container)
    - .planning/phases/02-authentication-accounts/02-UI-SPEC.md (/admin screen spec + copywriting contract)
    - .planning/phases/02-authentication-accounts/02-CONTEXT.md (D-16,D-17,D-18 — admin via same login form, role field + protected stub, manual DB assignment)
  </read_first>
  <action>
    Create `src/app/admin/page.tsx` as an async Server Component. Call createClient + getUser; if no user, redirect('/login'). Select profiles.role eq user.id single; if role !== 'admin', redirect('/dashboard') (defense-in-depth — the login action already routes admins here per Plan 02, but the page must independently enforce the boundary). When role === 'admin', render inside a `max-w-[1040px] mx-auto px-5 md:px-6 py-20` container a heading "Admin area" in font-heading 28px font-semibold and a description card (surface-container) with the copy "Content management is coming soon. This area will allow lesson editing and student management." Use only design tokens; sentence case; no green colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "Admin area" src/app/admin/page.tsx && grep -q "role" src/app/admin/page.tsx && grep -q "redirect" src/app/admin/page.tsx && echo OK</automated>
  </verify>
  <done>/admin renders the stub only for role='admin'; non-admins and unauthenticated users are redirected; typechecks</done>
  <acceptance_criteria>
    - `src/app/admin/page.tsx` is async, calls getUser, selects profiles.role server-side
    - Non-admin authenticated users are redirected to /dashboard; unauthenticated to /login
    - Heading "Admin area" + the exact admin stub copy render for admins, in a 1040px container, sentence case
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: RLS isolation test + proxy redirect test (SEC-02, SEC-03)</name>
  <read_first>
    - .planning/phases/02-authentication-accounts/02-VALIDATION.md (Per-Task Verification Map rows for rls + middleware; run commands; manual-only table)
    - .planning/phases/02-authentication-accounts/02-RESEARCH.md (Security Domain — RLS threat patterns; Validation Architecture RLS test row)
    - .planning/phases/02-authentication-accounts/02-PATTERNS.md (Supabase Query Pattern — parameterized SDK usage, RLS auto-isolation; migration RLS policies)
    - __tests__/rls.test.ts, __tests__/middleware.test.ts (the scaffolds to flesh out)
    - src/app/proxy.ts (the proxy whose redirect behavior the middleware test asserts)
  </read_first>
  <action>
    Flesh out `__tests__/rls.test.ts` to assert SEC-02 cross-user isolation: using the publishable (anon) key, create or sign in two distinct users, then have user A attempt to select user B's profiles row by id — assert the query returns no row (RLS blocks it), while A reading its own row succeeds. If a live test Supabase project is unavailable in the run environment, guard the live assertions behind an env check and fall back to asserting the policy intent against the migration SQL (the file contains the two auth.uid()-scoped policies and no insert policy); document in the test that full live RLS validation is the manual verification from 02-VALIDATION.md. Flesh out `__tests__/middleware.test.ts` to assert the proxy's protected-route behavior: a request with no authenticated user to a path under /dashboard, /admin, or /account produces a redirect to /login with the `next` search param set (mock the supabase client so getUser returns null). For SEC-03, add an assertion (or a documented grep-style check) that application source under src/ contains no raw SQL string concatenation against user tables — all access goes through `.from('profiles')...` SDK calls. Use no watch-mode flags.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="(rls|middleware)" && ! grep -rEq "(SELECT|INSERT|UPDATE|DELETE)[[:space:]].*\\+|query\\(['\\\"]\\s*select" src/app src/lib 2>/dev/null && echo OK</automated>
  </verify>
  <done>rls + middleware tests pass (live or guarded fallback); no raw SQL string building found in src/app or src/lib</done>
  <acceptance_criteria>
    - `__tests__/rls.test.ts` asserts a user cannot read another user's profiles row (live, or migration-policy fallback guarded by env)
    - `__tests__/middleware.test.ts` asserts unauthenticated access to a protected path redirects to /login with a next param
    - `npx jest --testPathPattern="(rls|middleware)"` passes
    - No raw SQL string concatenation against user tables anywhere in src/app or src/lib (SEC-03)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user -> /admin | Role boundary; only role='admin' may pass |
| authenticated user A -> user B's data | RLS boundary enforced by Postgres |
| unauthenticated request -> protected paths | proxy redirect boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Elevation of Privilege | Non-admin reaching /admin | mitigate | Server-side profiles.role check in admin page; redirect non-admins; role never client-controlled (AUTH-05, D-16) |
| T-02-12 | Information Disclosure | Cross-student profile reads | mitigate | RLS policies scoped to `(select auth.uid()) = id`; verified by rls.test.ts (SEC-02) |
| T-02-13 | Spoofing | Unauthenticated access to protected routes | mitigate | proxy.ts getUser-gated redirect to /login; verified by middleware.test.ts |
| T-02-14 | Tampering | SQL injection via raw query building | mitigate | Supabase SDK parameterized queries only; grep confirms no string-concatenated SQL in src (SEC-03) |
</threat_model>

<verification>
- `npx jest --testPathPattern="(rls|middleware)"` passes
- `npx tsc --noEmit` exits 0
- No raw SQL string building in src/app or src/lib
- Manual (02-VALIDATION): seeded admin user logs in → redirected to /admin; a student visiting /admin is redirected to /dashboard
- Manual: direct cross-user DB read blocked by RLS
</verification>

<success_criteria>
- Admin role exists and gates /admin server-side (AUTH-05)
- RLS blocks cross-student reads, proven by test (SEC-02)
- All queries parameterized via SDK; no raw SQL (SEC-03)
</success_criteria>

<output>
Create `.planning/phases/02-authentication-accounts/02-04-SUMMARY.md` when done
</output>
