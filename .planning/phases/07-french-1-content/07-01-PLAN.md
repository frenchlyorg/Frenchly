---
phase: 07-french-1-content
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260625_phase7_french1_content.sql
autonomous: false
requirements:
  - CONTENT-01
  - CONTENT-03

must_haves:
  truths:
    - "Lesson 1 (Greetings) has an approved explainer, 2 practice problems, and 1 writing prompt"
    - "SQL for Lesson 1 is written and syntactically correct (dollar-quoted, no bare apostrophes)"
    - "User has reviewed and approved the sample content format before Wave 2 proceeds"
  artifacts:
    - path: "supabase/migrations/20260625_phase7_french1_content.sql"
      provides: "Idempotent UPDATE for Lesson 1 sub-components (content column)"
      contains: "UPDATE public.sub_components"
  key_links:
    - from: "supabase/migrations/20260625_phase7_french1_content.sql"
      to: "public.sub_components.content"
      via: "UPDATE WHERE title = 'How French greetings work' AND lesson_id = (SELECT id FROM lessons WHERE slug = 'greetings')"
      pattern: "UPDATE public\\.sub_components"
---

<objective>
Generate the complete Lesson 1 (Greetings and Introductions) content as a rendered preview and write the SQL for that lesson only. End with a human review gate — the user approves format and style before Wave 2 writes the remaining 5 lessons.

Purpose: D-02 requires a sample review gate. The template locked here becomes the contract all subsequent lessons follow.
Output: supabase/migrations/20260625_phase7_french1_content.sql (Lesson 1 UPDATE section only)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-french-1-content/07-CONTEXT.md
</context>

<interfaces>
<!-- Key contracts the executor must match exactly. No codebase exploration needed. -->

From src/lib/practice/types.ts:
```typescript
type MCProblem = { type: 'mc'; prompt: string; options: string[]; correctAnswer: string }
type MatchingProblem = { type: 'matching'; prompt: string; pairs: Array<{ left: string; right: string }> }
type WrittenProblem = { type: 'written'; prompt: string; hints?: string }
```

From src/lib/practice/schema.ts:
- MCSchema: options min(2) max(4), correctAnswer must be one of options
- MatchingSchema: pairs min(2) max(6)
- WrittenSchema: hints is optional string (newline-separated phrases, rendered whitespace-pre-line)

From supabase/migrations/20260622_phase3_lessons.sql — existing rows for Lesson 1 (slug='greetings'):
- sub_component title='How French greetings work', kind='explainer', position=1
- sub_component title='Practice: match the greeting', kind='practice', position=2  (content IS NULL — UPDATE target)
- sub_component title='Write your own introduction', kind='writing', position=3  (content IS NULL — UPDATE target)

From supabase/migrations/20260623_phase5_practice.sql — additional rows added at positions 10–19:
- position=10: mc 'Practice: formal vs informal greeting' (content already set — DO NOT overwrite)
- position=11: mc 'Practice: choose the farewell' (content already set)
- SQL pattern for content: single-quoted JSON strings or $$...$$ dollar-quoting for apostrophes

Sub-component kind values: 'explainer' | 'practice' | 'writing'
problem_type column: NULL for explainer/writing; 'mc'|'fill-in'|'conjugation-table'|'conjugation-single'|'matching' for practice
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate Lesson 1 content preview and write SQL migration skeleton</name>
  <files>supabase/migrations/20260625_phase7_french1_content.sql</files>

  <read_first>
    - supabase/migrations/20260622_phase3_lessons.sql — existing Lesson 1 sub-component titles and positions; dollar-quoting pattern
    - supabase/migrations/20260623_phase5_practice.sql — JSON content format for each problem_type; positions 10–19 already used on greetings lesson
    - src/lib/practice/types.ts — MCProblem, MatchingProblem, WrittenProblem field names
    - src/lib/practice/schema.ts — Zod constraints (MC options min 2 max 4, matching pairs min 2 max 6)
    - .planning/phases/07-french-1-content/07-CONTEXT.md — D-01 (update existing, not delete/reinsert), D-03 (Greetings → matching + MC), D-04 (dollar-quoting, UPDATE pattern)
  </read_first>

  <action>
Create the migration file at supabase/migrations/20260625_phase7_french1_content.sql.

The file header must explain what each section does and that it is safe to re-run (idempotent).

**Lesson 1 content to author (Greetings and Introductions — D-03: matching + MC):**

The existing Phase 3 seed already wrote the explainer for 'How French greetings work' with real markdown content, so that sub-component's content column does NOT need updating.

The Phase 5 seed already wrote 2 MC rows at positions 10 and 11 on the greetings lesson (formal vs informal, farewell choice). These must NOT be touched.

What remains NULL and must be updated per D-01:
- title='Practice: match the greeting', position=2, kind='practice' — set problem_type='matching' and content to a MatchingProblem JSON
- title='Write your own introduction', position=3, kind='writing' — set content to a WrittenProblem JSON

**Practice sub-component (matching) — author with 4 pairs mapping French greetings to their English meaning or usage context:**
- Pairs should be educationally meaningful: map greeting to register or time-of-day meaning
- Example pairs (use these or improve): Bonjour→"Hello / Good morning", Salut→"Hi (informal)", Bonsoir→"Good evening", Bonne nuit→"Good night"
- prompt: "Match each French greeting to its meaning."

**Writing sub-component — author a contextual prompt for greetings:**
- prompt: "Write a short introduction of yourself in French. Include your name and a greeting appropriate for meeting someone new."
- hints (3 newline-separated phrases): Je m'appelle [prénom].\nBonjour, je suis étudiant(e).\nEnchanté(e) de vous rencontrer.

**SQL UPDATE pattern (per D-04):**
- Use a DO $$ BEGIN ... END; $$ block that resolves lesson IDs by slug
- For the matching sub-component UPDATE: set both content AND problem_type='matching'
- For the writing sub-component UPDATE: content only (kind='writing', problem_type stays NULL)
- Use dollar-quoting ($$...$$ or $json$...$json$) for any string literals containing apostrophes or double-quotes
- UPDATE WHERE title = '[exact title]' AND lesson_id = (SELECT id FROM public.lessons WHERE slug = 'greetings')
- Guard: RAISE NOTICE if lesson not found (same pattern as Phase 5 migration)

The migration file at this stage contains ONLY the Lesson 1 UPDATE section. Wave 2 will append Lessons 2–6.

Add a comment at the bottom: -- Wave 2 content (Lessons 2-6) appended by 07-02-PLAN.md
  </action>

  <verify>
    <automated>
      node -e "
const fs = require('fs');
const sql = fs.readFileSync('supabase/migrations/20260625_phase7_french1_content.sql', 'utf8');
// Check file exists and has UPDATE statements
const hasUpdate = sql.includes('UPDATE public.sub_components');
// Check matching content JSON structure
const hasMatching = sql.includes('\"type\":\"matching\"') || sql.includes('type:matching');
// Check written content JSON structure
const hasWritten = sql.includes('\"type\":\"written\"') || sql.includes('type:written');
// Check dollar-quoting used (no bare single-quote apostrophes in French words)
const hasDollarQuote = sql.includes('\$\$') || sql.includes('\$json\$');
// Check greetings slug is referenced
const hasSlug = sql.includes(\"'greetings'\");
console.log('UPDATE present:', hasUpdate);
console.log('Matching JSON present:', hasMatching);
console.log('Written JSON present:', hasWritten);
console.log('Dollar-quoting used:', hasDollarQuote);
console.log('Greetings slug referenced:', hasSlug);
console.log(hasUpdate && hasMatching && hasWritten && hasSlug ? 'PASS' : 'FAIL');
"
    </automated>
  </verify>

  <done>
    - Migration file exists at supabase/migrations/20260625_phase7_french1_content.sql
    - File contains UPDATE for 'Practice: match the greeting' with problem_type='matching' and valid MatchingProblem JSON (4 pairs, prompt field, pairs array with left/right keys)
    - File contains UPDATE for 'Write your own introduction' with valid WrittenProblem JSON (type='written', prompt, hints with newline-separated phrases)
    - No bare apostrophes in French string content — dollar-quoting used throughout
    - Node validation script prints PASS
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Sample lesson review gate — approve format before Wave 2</name>

  <what-built>
    Task 1 created the SQL for Lesson 1 (Greetings and Introductions) — the template that all 5 remaining lessons will follow. This is the D-02 sample review gate: you must approve the content format and style before any more lessons are written.
  </what-built>

  <how-to-verify>
    Review the generated content by reading:
      supabase/migrations/20260625_phase7_french1_content.sql

    Check the following:

    **Explainer (already in DB from Phase 3 — confirm it looks right):**
    The explainer content for 'How French greetings work' is already seeded (Bonjour vs Salut table). It will NOT be changed by this migration. Confirm you're happy with what's already there, or note if it needs adjustment.

    **Practice (matching) — 'Practice: match the greeting':**
    - Does the matching prompt make sense for beginners?
    - Are the 4 pairs correct and educationally useful?
    - Is the JSON format readable?

    **Writing prompt — 'Write your own introduction':**
    - Is the prompt appropriate for Lesson 1 (greeting + introduction)?
    - Are the 3 hints useful without giving too much away?
    - Are the hints in natural French?

    **Style check:**
    - Sentence case used in prompts (not Title Case)?
    - Content is appropriately brief and direct for high-school students?
    - No overly complex vocabulary for French 1 level?

    If you want changes: describe them in your reply and Wave 2 will apply the corrected template.
    If content looks good: reply "approved" or "approved — proceed to Wave 2".
  </how-to-verify>

  <resume-signal>Reply "approved" to proceed to Wave 2. Describe any changes needed to adjust before continuing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase DB | SQL runs in Supabase SQL Editor with elevated privileges; malformed JSON in content column fails silently at render time (parseProblemContent returns null) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | sub_components.content | mitigate | Dollar-quote all SQL strings; UPDATE WHERE title+lesson_id to avoid unintended row matches |
| T-07-02 | Information Disclosure | Migration file | accept | Migration file is in version control; contains no secrets, only curriculum content |
| T-07-SC | Tampering | npm/pip installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After Task 1 completes:
- supabase/migrations/20260625_phase7_french1_content.sql exists
- Node validation script prints PASS for all 5 checks
- SQL contains no bare apostrophes in French strings (dollar-quoting used)

After Task 2 (checkpoint):
- User has read and approved the Lesson 1 content format
- Any requested changes are noted for Wave 2
</verification>

<success_criteria>
1. Migration file created with correct Lesson 1 UPDATE SQL
2. Matching problem JSON passes MatchingSchema constraints (2-6 pairs, left/right keys)
3. Written problem JSON passes WrittenSchema constraints (type, prompt, optional hints string)
4. User approves content format — Wave 2 is unblocked
</success_criteria>

<output>
Create .planning/phases/07-french-1-content/07-01-SUMMARY.md when done
</output>
