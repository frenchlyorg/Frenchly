---
phase: 07-french-1-content
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - supabase/migrations/20260625_phase7_french1_content.sql
autonomous: false
requirements:
  - CONTENT-01
  - CONTENT-03

must_haves:
  truths:
    - "All 6 French 1 lessons exist in the DB with real content (explainer markdown + 2 practice problems + 1 writing prompt)"
    - "All 4 auto-graded problem types appear across the 6 lessons (MC, fill-in, matching, conjugation)"
    - "A student can open every lesson and complete every sub-component without hitting a null content error"
    - "Time estimates on the French 1 level page total approximately 72 minutes (6 lessons × 12 min)"
  artifacts:
    - path: "supabase/migrations/20260625_phase7_french1_content.sql"
      provides: "Complete SQL: UPDATE Lessons 1-2 + INSERT Lessons 3-6 with all sub-components"
      contains: "INSERT INTO public.lessons"
    - path: "supabase/migrations/20260625_phase7_french1_content.sql"
      provides: "Lesson 2 definite-articles UPDATE"
      contains: "slug = 'definite-articles'"
  key_links:
    - from: "supabase/migrations/20260625_phase7_french1_content.sql"
      to: "public.sub_components.content"
      via: "INSERT sub-components for lessons 3-6; UPDATE for lessons 1-2 (slug-resolved)"
      pattern: "INSERT INTO public\\.sub_components"
    - from: "public.sub_components.content (JSON)"
      to: "parseProblemContent() in schema.ts"
      via: "Zod discriminated union — type field routes to correct schema"
      pattern: "\"type\":(\"mc\"|\"fill-in\"|\"conjugation-table\"|\"conjugation-single\"|\"matching\"|\"written\")"
---

<objective>
Append SQL for Lessons 2–6 to the migration file started in Wave 1, then run the blocking manual migration in Supabase, and verify end-to-end that all French 1 content is accessible.

Purpose: Delivers the full French 1 curriculum to the database, making the level completable end-to-end.
Output: Complete supabase/migrations/20260625_phase7_french1_content.sql applied to the live Supabase project
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-french-1-content/07-CONTEXT.md
@.planning/phases/07-french-1-content/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Canonical contracts — executor must match exactly. No codebase exploration needed. -->

From src/lib/practice/types.ts (all types used in this plan):
```typescript
type MCProblem           = { type: 'mc'; prompt: string; options: string[]; correctAnswer: string }
type FillInProblem       = { type: 'fill-in'; prompt: string; correctAnswer: string }
type ConjugationTableProblem = {
  type: 'conjugation-table'; prompt: string; verb: string;
  answers: { je: string; tu: string; il: string; nous: string; vous: string; ils: string }
}
type ConjugationSingleProblem = { type: 'conjugation-single'; prompt: string; correctAnswer: string }
type MatchingProblem     = { type: 'matching'; prompt: string; pairs: Array<{ left: string; right: string }> }
type WrittenProblem      = { type: 'written'; prompt: string; hints?: string }
```

From src/lib/practice/schema.ts — Zod constraints:
- MCSchema: options min(2) max(4); correctAnswer must be one of options
- MatchingSchema: pairs min(2) max(6)
- ConjTableSchema: answers object must have all 6 keys (je, tu, il, nous, vous, ils)
- FillInSchema, ConjSingleSchema: prompt + correctAnswer only
- WrittenSchema: hints is optional string (newline-separated phrases)

Problem type → SQL problem_type column value mapping:
  MCProblem → 'mc'
  FillInProblem → 'fill-in'
  ConjugationTableProblem → 'conjugation-table'
  ConjugationSingleProblem → 'conjugation-single'
  MatchingProblem → 'matching'
  WrittenProblem → problem_type stays NULL (kind='writing')

Existing DB state after Phase 3 + Phase 5 migrations:
- slug='greetings': positions 1 (explainer ✓), 2 (practice NULL), 3 (writing NULL), 10-11 (mc ✓), 12-13 (fill-in ✓), 16-17 (conj-single ✓), 18-19 (matching ✓)
- slug='definite-articles': positions 1 (explainer ✓), 2 (practice NULL), 14-15 (conj-table ✓), 18-19 (matching ✓)
- Lessons 3-6: DO NOT EXIST yet — need INSERT into lessons then sub_components

D-03 problem type assignments:
  Lesson 1 Greetings         → matching (pos 2) + writing (pos 3)  [MC already seeded at pos 10-11]
  Lesson 2 Definite articles → MC + fill-in (pos 2 = UPDATE to MC; INSERT new fill-in at pos 4) + writing (INSERT at pos 5)
  Lesson 3 Numbers           → fill-in + matching + writing
  Lesson 4 Pronouns + être   → conjugation-table + fill-in + writing
  Lesson 5 Indefinite arts   → MC + fill-in + writing
  Lesson 6 Être + adjectives → fill-in + conjugation-single + writing

Lesson slugs (new):
  'numbers-and-counting', 'pronouns-and-etre', 'indefinite-articles', 'etre-adjectives'

SQL dollar-quoting: use $$...$$ for any string containing apostrophes (m', l', d', j', s', c', n', qu')
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Append Lessons 2–6 SQL to migration file</name>
  <files>supabase/migrations/20260625_phase7_french1_content.sql</files>

  <read_first>
    - supabase/migrations/20260625_phase7_french1_content.sql — existing Lesson 1 section; dollar-quoting and UPDATE pattern already established; note the Wave 2 comment at the bottom
    - supabase/migrations/20260622_phase3_lessons.sql — Lesson 2 (slug='definite-articles') exact sub-component titles and positions for UPDATE targets
    - supabase/migrations/20260623_phase5_practice.sql — positions already used (10-19) on greetings and definite-articles lessons; DO NOT insert at these positions
    - src/lib/practice/types.ts — field names for each problem type (must match JSON keys exactly)
    - src/lib/practice/schema.ts — Zod constraints (all must pass)
    - .planning/phases/07-french-1-content/07-CONTEXT.md — D-01 (titles, slugs), D-03 (type assignments), D-04 (dollar-quoting, idempotency)
    - .planning/phases/07-french-1-content/07-01-SUMMARY.md — any approved changes from Wave 1 sample review
  </read_first>

  <action>
Append to supabase/migrations/20260625_phase7_french1_content.sql starting after the Wave 2 comment. Author all content inline — no placeholders.

**Lesson 2: Definite articles: le, la, l', les (UPDATE existing, slug='definite-articles')**

Existing from Phase 3: position 1 = explainer (content ✓, skip), position 2 = 'Practice: choose the right article' (content NULL, kind='practice').
Existing from Phase 5: positions 14-15 = conjugation-table (parler, aimer), positions 18-19 = matching (articles + greeting-register). These are at unusual positions but are on this lesson — do not touch them.

Per D-03: Lesson 2 gets MC + fill-in for its two primary practice slots, plus a writing prompt.

UPDATE position 2 sub-component: set kind stays 'practice', set problem_type='mc', set content to an MCProblem — prompt asking which article fits a masculine noun (e.g., "Which definite article goes before 'livre' (book)?", options: ["le","la","l'","les"], correctAnswer: "le").

INSERT new sub-component at position 4 (fill-in): FillInProblem asking student to supply the correct article for a feminine noun in a sentence (e.g., "_____ table est grande." correctAnswer: "La").

INSERT new sub-component at position 5 (writing, kind='writing'): WrittenProblem — prompt asking student to write 2-3 sentences describing objects around them using le, la, l', or les. hints: 3 newline-separated phrases like "Le livre est sur la table.\nLa fenêtre est grande.\nL'école est proche."

UPDATE the lesson's estimated_minutes to 12 (per Claude's Discretion — 4 sub-components × ~3 min). Also UPDATE title to 'Definite articles: le, la, l'', les' if the existing title is 'Definite articles: le, la, les' (Phase 3 seeded without l').

**Lesson 3: Numbers and counting (NEW, slug='numbers-and-counting', position=3)**

INSERT into public.lessons: level_id from (SELECT id FROM levels WHERE slug='french-1'), slug='numbers-and-counting', title='Numbers and counting', estimated_minutes=12, position=3.

Sub-components (4 total):
- pos 1, kind='explainer', title='French numbers 1–20': markdown with ## heading, short intro, table of numbers 1–20 (deux columns: Nombre | Français). Include note about pronunciation of numbers before vowels (e.g., "vingt et un").
- pos 2, kind='practice', problem_type='fill-in', title='Practice: write the number': FillInProblem — "How do you write the number 7 in French?" correctAnswer: "sept". Choose a number that requires spelling knowledge (not trivial: avoid 1, 2).
- pos 3, kind='practice', problem_type='matching', title='Practice: match numbers to words': MatchingProblem with 5 pairs mapping numerals to French words (e.g., 3→trois, 8→huit, 12→douze, 15→quinze, 20→vingt).
- pos 4, kind='writing', title='Write about numbers': WrittenProblem — "Write a few sentences about numbers in your daily life. Include at least three French numbers." hints: 3 phrases, e.g., "J'ai ___ ans.\nMon numéro de téléphone est...\nIl y a ___ élèves dans ma classe."

**Lesson 4: Subject pronouns + être (NEW, slug='pronouns-and-etre', position=4)**

INSERT into public.lessons: slug='pronouns-and-etre', title='Subject pronouns and être', estimated_minutes=12, position=4.

Sub-components (4 total):
- pos 1, kind='explainer', title='Subject pronouns and être': markdown ## heading, short intro sentence about subject pronouns, then a table with columns Pronoun | Être (present) — all 6 forms (je suis, tu es, il/elle est, nous sommes, vous êtes, ils/elles sont).
- pos 2, kind='practice', problem_type='conjugation-table', title='Practice: conjugate être': ConjugationTableProblem — prompt: "Conjugate être (to be) in the present tense.", verb: "être", answers: {je:"suis", tu:"es", il:"est", nous:"sommes", vous:"êtes", ils:"sont"}.
- pos 3, kind='practice', problem_type='fill-in', title='Practice: choose the pronoun': FillInProblem — "_____ suis étudiant. (I am a student.) What is the correct subject pronoun?" correctAnswer: "Je".
- pos 4, kind='writing', title='Introduce yourself with être': WrittenProblem — "Write 3–4 sentences about yourself using être and subject pronouns. Describe who you are and where you are from." hints: 3 phrases, e.g., "Je suis étudiant(e).\nNous sommes en classe.\nIl est professeur."

**Lesson 5: Indefinite articles: un, une, des (NEW, slug='indefinite-articles', position=5)**

INSERT into public.lessons: slug='indefinite-articles', title='Indefinite articles: un, une, des', estimated_minutes=12, position=5.

Sub-components (4 total):
- pos 1, kind='explainer', title='Un, une, des': markdown ## heading, short intro comparing indefinite articles to English "a/an/some", then a table: Article | Use | Example — un (masc. sing., un stylo), une (fem. sing., une table), des (plural, des livres).
- pos 2, kind='practice', problem_type='mc', title='Practice: choose the article': MCProblem — "Which indefinite article goes before 'crayon' (pencil, masculine)?" options: ["un","une","des","le"], correctAnswer: "un".
- pos 3, kind='practice', problem_type='fill-in', title='Practice: complete the sentence': FillInProblem — "_____ fille lit un livre. (A girl is reading a book.)" correctAnswer: "Une".
- pos 4, kind='writing', title='Describe your classroom': WrittenProblem — "Write 3–4 sentences describing objects in your classroom. Use un, une, or des." hints: 3 phrases, e.g., "Il y a un tableau dans la salle.\nJ'ai une gomme et des crayons.\nIl y a des fenêtres et une porte."

**Lesson 6: Être + adjectives — basic descriptions (NEW, slug='etre-adjectives', position=6)**

INSERT into public.lessons: slug='etre-adjectives', title='Être + adjectives: basic descriptions', estimated_minutes=12, position=6.

Sub-components (4 total):
- pos 1, kind='explainer', title='Describing with être and adjectives': markdown ## heading, intro about adjective agreement (masculine/feminine/plural), then a small table showing a base adjective in 4 forms: grand/grande/grands/grandes. Include note that adjective follows the noun in French (unlike English).
- pos 2, kind='practice', problem_type='fill-in', title='Practice: adjective agreement': FillInProblem — "La maison est _____. (The house is big.) Use the correct form of 'grand'." correctAnswer: "grande".
- pos 3, kind='practice', problem_type='conjugation-single', title='Practice: conjugate être': ConjugationSingleProblem — prompt: "Elles _____ (être) intelligentes. Fill in the correct form.", correctAnswer: "sont".
- pos 4, kind='writing', title='Describe yourself and a friend': WrittenProblem — "Write 3–4 sentences describing yourself and a friend using être and adjectives. Remember to match the adjective to the gender of the subject." hints: 3 phrases, e.g., "Je suis grand(e) et intelligent(e).\nMon ami est sympa.\nNous sommes étudiants sérieux."

**Critical SQL rules for all new content:**
- Resolve v_french1_id with: SELECT id INTO v_french1_id FROM public.levels WHERE slug = 'french-1'
- Use DO $$ DECLARE ... BEGIN ... END; $$ block for the entire Wave 2 section
- Dollar-quote every string literal that contains apostrophes: $$ ... $$ or named tags like $txt$ ... $txt$
- For JSON strings with both apostrophes and double-quotes, use a named dollar-quote tag to avoid ambiguity
- Every INSERT into sub_components for 'practice' kind must include the problem_type column
- Every INSERT into sub_components for 'writing' kind must omit problem_type (it stays NULL)
- GUARD at top of DO block: SELECT id INTO v_french1_id FROM public.levels WHERE slug='french-1'; IF v_french1_id IS NULL THEN RAISE EXCEPTION 'French 1 level not found — run Phase 3 migration first.'; END IF;
- UPDATE lesson estimated_minutes + title for Lesson 2 by slug (per D-01)
  </action>

  <verify>
    <automated>
      node -e "
const fs = require('fs');
const sql = fs.readFileSync('supabase/migrations/20260625_phase7_french1_content.sql', 'utf8');

// Count INSERT INTO public.lessons (should be 4 new lessons: 3-6)
const lessonInserts = (sql.match(/INSERT INTO public\.lessons/gi) || []).length;
// Count INSERT INTO public.sub_components (4 sub-components per new lesson = 16 + some for lesson 2 = ~18-19)
const scInserts = (sql.match(/INSERT INTO public\.sub_components/gi) || []).length;
// All 4 problem types present
const hasConjTable = sql.includes('conjugation-table');
const hasConjSingle = sql.includes('conjugation-single');
const hasMC = sql.includes('\"type\":\"mc\"');
const hasFillIn = sql.includes('\"type\":\"fill-in\"');
const hasMatching = sql.includes('\"type\":\"matching\"');
const hasWritten = sql.includes('\"type\":\"written\"');
// New slugs present
const hasSlugs = ['numbers-and-counting','pronouns-and-etre','indefinite-articles','etre-adjectives']
  .every(s => sql.includes(s));

console.log('Lesson INSERTs (expect 4):', lessonInserts);
console.log('Sub-component INSERTs (expect 14+):', scInserts);
console.log('All 4 practice types present:', hasConjTable && hasConjSingle && hasMC && hasFillIn && hasMatching);
console.log('Written type present:', hasWritten);
console.log('All 4 new slugs present:', hasSlugs);
const pass = lessonInserts >= 4 && scInserts >= 14 && hasConjTable && hasConjSingle && hasMC && hasFillIn && hasMatching && hasWritten && hasSlugs;
console.log(pass ? 'PASS' : 'FAIL');
"
    </automated>
  </verify>

  <done>
    - Migration file contains 4 INSERT INTO public.lessons statements (Lessons 3–6)
    - Migration file contains 14+ INSERT INTO public.sub_components statements covering all new sub-components
    - All 4 auto-graded problem types appear: 'mc', 'fill-in', 'conjugation-table' (Lesson 4), 'conjugation-single' (Lesson 6), 'matching' (Lessons 1 and 3)
    - All 4 new slugs present: numbers-and-counting, pronouns-and-etre, indefinite-articles, etre-adjectives
    - No bare apostrophes in French string content (dollar-quoting used)
    - Node validation script prints PASS
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Run migration in Supabase SQL Editor</name>

  <what-built>
    Task 1 completed the full migration SQL file at supabase/migrations/20260625_phase7_french1_content.sql. This task requires you to execute it manually in the Supabase dashboard — the CLI is not installed in this project.
  </what-built>

  <how-to-verify>
    1. Open the Supabase dashboard for this project
    2. Navigate to SQL Editor (left sidebar)
    3. Click "New query"
    4. Open supabase/migrations/20260625_phase7_french1_content.sql in your editor and copy the ENTIRE file contents
    5. Paste into the SQL Editor query window
    6. Click "Run" (or Ctrl+Enter)
    7. Confirm the query completes without errors (green success message or row count in output)
    8. Verify row counts with this query in a new SQL Editor tab:

    SELECT
      l.title,
      l.position,
      l.estimated_minutes,
      COUNT(sc.id) AS sub_component_count,
      COUNT(CASE WHEN sc.content IS NOT NULL THEN 1 END) AS with_content
    FROM public.lessons l
    JOIN public.levels lv ON lv.id = l.level_id AND lv.slug = 'french-1'
    LEFT JOIN public.sub_components sc ON sc.lesson_id = l.id
    GROUP BY l.id, l.title, l.position, l.estimated_minutes
    ORDER BY l.position;

    Expected output: 6 rows, each with sub_component_count >= 4 and with_content >= 3 (the primary 4 sub-components each lesson + any Phase 5 seeded rows that had content).

    Also run:
    SELECT COUNT(*) FROM public.lessons l
    JOIN public.levels lv ON lv.id = l.level_id
    WHERE lv.slug = 'french-1';

    Expected: 6
  </how-to-verify>

  <resume-signal>Paste the output of both verification queries, or type "migration applied" if both queries returned the expected results.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification — complete a French 1 lesson</name>

  <what-built>
    The full French 1 curriculum is now in the database. This checkpoint confirms all 6 lessons are accessible, the sub-component types render correctly, and there are no null-content dead ends.
  </what-built>

  <how-to-verify>
    1. Start the dev server: npm run dev
    2. Log in as a test student account
    3. Navigate to /levels/french-1
       - Confirm 6 lesson cards appear
       - Confirm each shows "12 min" (or similar) estimated time
       - Confirm lesson titles match: Greetings and introductions, Definite articles: le, la, l', les, Numbers and counting, Subject pronouns and être, Indefinite articles: un, une, des, Être + adjectives: basic descriptions

    4. Open Lesson 1 (Greetings and introductions):
       - Confirm the explainer renders formatted markdown (not raw ## syntax)
       - Find the matching practice sub-component and confirm the matching card renders with pairs (not "not available yet")
       - Find the writing sub-component and confirm the writing card renders with the prompt and hints
       - Mark sub-components complete and confirm progress saves

    5. Open Lesson 3 (Numbers and counting):
       - Confirm fill-in practice card renders
       - Confirm matching practice card renders with 5 pairs

    6. Open Lesson 4 (Subject pronouns and être):
       - Confirm conjugation-table card renders with all 6 pronoun rows

    7. Open Lesson 6 (Être + adjectives):
       - Confirm conjugation-single card renders

    8. Quick check Lessons 2, 5: open each, confirm no sub-component shows "not available yet"

    Any "not available yet" message means a sub-component has null or malformed content JSON — report which lesson and sub-component title.
  </how-to-verify>

  <resume-signal>Type "all lessons verified" if all 6 lessons render correctly with no dead ends. Describe any issues found (lesson name + sub-component title).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase DB | SQL runs with elevated privileges in SQL Editor; content column accepts any text — malformed JSON shows "not available yet" silently via parseProblemContent returning null |
| Student → lesson page | Server-side parseProblemContent validates JSON before render; invalid content never reaches the client as raw JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | sub_components INSERT | mitigate | Guard at top of DO block raises exception if french-1 level not found; prevents orphaned sub_components |
| T-07-04 | Tampering | problem_type column | mitigate | Check constraint on sub_components.problem_type enforces allowed values at DB level (from Phase 5 migration) |
| T-07-05 | Information Disclosure | Migration file | accept | Contains only curriculum content — no secrets, no PII |
| T-07-06 | Denial of Service | Null content → "not available yet" | mitigate | End-to-end checkpoint (Task 3) confirms all sub-components have non-null, valid content before phase closes |
| T-07-SC | Tampering | npm/pip installs | accept | No new package installs in this plan |
</threat_model>

<verification>
After Task 1:
- Node validation script prints PASS
- 4 INSERT INTO public.lessons present
- 14+ INSERT INTO public.sub_components present
- All 6 problem type strings present in file

After Task 2 (migration applied):
- SELECT COUNT(*) FROM lessons JOIN levels WHERE slug='french-1' returns 6
- Per-lesson sub_component counts query shows sub_component_count >= 4 for all 6 rows

After Task 3 (human UAT):
- All 6 lessons open without errors
- No sub-component shows "not available yet"
- All 4 problem type cards render (MC, fill-in, matching, conjugation-table, conjugation-single) across the 6 lessons
- Writing card renders with prompt and hints on all 6 lessons
</verification>

<success_criteria>
1. SELECT COUNT(*) FROM public.lessons l JOIN public.levels lv ON lv.id=l.level_id WHERE lv.slug='french-1' returns 6
2. Every French 1 sub-component with kind='practice' or kind='writing' has non-null content
3. Opening each of the 6 lessons shows all sub-components rendered (no "not available yet" messages)
4. All 4 auto-graded problem types confirmed working: MC (Lessons 2, 5), fill-in (Lessons 2, 3, 4, 5, 6), matching (Lessons 1, 3), conjugation-table (Lesson 4), conjugation-single (Lesson 6)
5. Writing prompt + hints renders on all 6 lessons
</success_criteria>

<output>
Create .planning/phases/07-french-1-content/07-02-SUMMARY.md when done
</output>
