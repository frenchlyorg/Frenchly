---
phase: 11-pages-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/page.tsx
autonomous: true
requirements: [PAGE-05]
must_haves:
  truths:
    - "A placed-in student visiting /dashboard sees their current level name (French 1 or French 2)"
    - "The dashboard shows a progress bar and an 'N of M lessons complete' label for the current level"
    - "The dashboard shows a 'Continue lesson' button linking to the first incomplete lesson; when all lessons are done it shows a 'take the level quiz' link instead"
    - "The dashed 'coming soon' placeholder no longer appears on the dashboard"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard with real Supabase-backed progress UI"
      contains: "sub_component_progress"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "levels + lessons + sub_components"
      via: "nested Supabase select scoped to current level"
      pattern: 'from\("levels"\)'
    - from: "src/app/dashboard/page.tsx"
      to: "sub_component_progress"
      via: "user-scoped progress query"
      pattern: 'eq\("user_id", user\.id\)'
    - from: "src/app/dashboard/page.tsx"
      to: "/levels/{slug}/lessons/{lessonId}"
      via: "Continue CTA Link using firstIncomplete.id"
      pattern: "lessons/\\$\\{"
---

<objective>
Replace the dashboard's hardcoded "coming soon" placeholder with a real, Supabase-backed progress view: current level card, lesson-completion progress bar, and a Continue CTA to the first incomplete lesson.

Purpose: Satisfies PAGE-05 / SC-3 — students see their actual progress and have a one-click path back into learning.
Output: A data-driven dashboard for placed students, reusing the proven query + completion algorithm from the level page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pages-navigation/11-CONTEXT.md
@.planning/phases/11-pages-navigation/11-PATTERNS.md
@.planning/phases/11-pages-navigation/11-RESEARCH.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Canonical query + completion algorithm. Source: src/app/levels/[levelSlug]/page.tsx (verified). -->
<!-- Reuse these shapes exactly — they handle RLS, empty-list, and ordering edge cases. -->

Lesson route lookup key (VERIFIED from src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx line 76):
  the lesson page does `.eq('id', lessonId)` — the route param is the lesson UUID, NOT the slug.
  → Continue href MUST use `firstIncomplete.id`, e.g. `/levels/${level.slug}/lessons/${firstIncomplete.id}`

Nested level query (PostgREST relationship select):
  supabase.from("levels")
    .select("id, slug, name, level_number, lessons ( id, slug, title, position, sub_components ( id, position ) )")
    .eq("level_number", profile?.unlocked_through_level_number ?? 1)
    .order("position", { referencedTable: "lessons" })
    .single()

Progress query (empty-list sentinel is MANDATORY — Supabase .in([]) generates invalid SQL):
  const allSubIds = (level?.lessons ?? []).flatMap(l => (l.sub_components ?? []).map(s => s.id));
  supabase.from("sub_component_progress")
    .select("sub_component_id")
    .eq("user_id", user.id)
    .in("sub_component_id", allSubIds.length > 0 ? allSubIds : ["__none__"]);
  const completedSet = new Set((rows ?? []).map(r => r.sub_component_id));

Completion algorithm (a lesson is complete when EVERY sub_component has a progress row):
  const lessons = level?.lessons ?? [];
  const completedLessons = lessons.filter(l => (l.sub_components ?? []).every(s => completedSet.has(s.id)));
  const totalLessons = lessons.length;
  const pct = totalLessons > 0 ? Math.round((completedLessons.length / totalLessons) * 100) : 0;
  const firstIncomplete = lessons.find(l => (l.sub_components ?? []).some(s => !completedSet.has(s.id)));

Progress bar tokens (VERIFIED: LevelCard.tsx uses bg-primary, not bg-secondary):
  track: h-2 w-full rounded-full bg-surface-container-high overflow-hidden
  fill:  h-full rounded-full bg-primary transition-all duration-300  (inline style width: `${pct}%`)

Primary CTA classes:
  inline-block px-6 py-3 bg-primary text-on-primary rounded font-label text-sm hover:bg-primary/90 transition-colors
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fetch current level + lesson progress in the dashboard Server Component</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx (current state — auth guard lines 9-18, DiagnosticGate guard lines 20-40, profile fetch lines 42-46, placeholder lines 58-66 to be removed)
    - src/app/levels/[levelSlug]/page.tsx (canonical nested query + completedSet + first-incomplete pattern, lines 94-144)
    - src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx (line 76 — confirms lesson lookup is by id/UUID, so Continue href uses firstIncomplete.id)
    - .planning/phases/11-pages-navigation/11-RESEARCH.md (Patterns 2 + dashboard skeleton, lines 175-237 and 435-479; Pitfalls 1-3)
  </read_first>
  <action>
    Implements D-02 (data half) / PAGE-05. In src/app/dashboard/page.tsx, add the data-fetching logic AFTER the existing profile fetch (line 46) and BEFORE the `return` (line 50). Do NOT touch the auth guard (lines 9-18), the DiagnosticGate early-return (lines 20-40), or the profile fetch (lines 42-46) — those are correct and must stay.

    Add the nested level query exactly as in the `<interfaces>` block: `supabase.from("levels").select("id, slug, name, level_number, lessons ( id, slug, title, position, sub_components ( id, position ) )").eq("level_number", profile?.unlocked_through_level_number ?? 1).order("position", { referencedTable: "lessons" }).single()`. Use the `?? 1` fallback (Pitfall 2 — the column can be null for edge-case users; default to French 1). The `.order(...{ referencedTable: "lessons" })` is mandatory (Pitfall 3 — without it, first-incomplete is non-deterministic).

    Add the progress query exactly as in `<interfaces>`, including the `allSubIds.length > 0 ? allSubIds : ["__none__"]` sentinel (Pitfall 1 — empty `.in([])` crashes Supabase). Build `completedSet` from the returned rows.

    Derive `lessons`, `completedLessons`, `totalLessons`, `pct`, and `firstIncomplete` using the completion algorithm in `<interfaces>`. Compute `continueHref`: if `firstIncomplete` exists → `/levels/${level?.slug}/lessons/${firstIncomplete.id}` (use `.id` — the UUID — NOT `.slug`, verified from the lesson route); else if `level` exists → `/levels/${level.slug}` (the level quiz path); else `null`.

    This task adds data plumbing only — it does not yet render the new UI (Task 2 does). To keep `tsc` green and the page functional in the interim, you may leave the existing placeholder render in place until Task 2; but all the derived variables must be declared and typed. Do not add `"use client"` and do not introduce any client-side fetching (useEffect/useState) — all queries stay server-side (anti-pattern in RESEARCH.md).
  </action>
  <verify>
    <automated>cd c:/Users/Ericc/frenchly && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/page.tsx contains `from("levels")` with a nested `lessons ( id, slug, title, position, sub_components ( id, position ) )` select
    - File contains `.order("position", { referencedTable: "lessons" })`
    - File contains the sentinel `allSubIds.length > 0 ? allSubIds : ["__none__"]` (or equivalent variable name) on the sub_component_progress query
    - File contains `profile?.unlocked_through_level_number ?? 1`
    - File contains `.eq("user_id", user.id)` on the sub_component_progress query
    - The Continue href construction references `firstIncomplete.id` (NOT `firstIncomplete.slug`) and the path segment `/lessons/`
    - File does NOT contain `"use client"`
    - The DiagnosticGate early-return block (lines ~20-40) is unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard fetches the student's current level with nested lessons + sub_components and their progress, and derives completedLessons/totalLessons/pct/firstIncomplete/continueHref server-side; tsc clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Render the dashboard progress UI and remove the placeholder</name>
  <files>src/app/dashboard/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx (current return block lines 50-68 — the greeting at 53-56 stays; the dashed placeholder at 58-66 is removed)
    - src/components/lessons/LevelCard.tsx (progress bar tokens — bg-primary fill on bg-surface-container-high track, around lines 101-117)
    - DESIGN.md (color tokens, typography, tonal-layer card rules — no heavy shadows)
    - .planning/phases/11-pages-navigation/11-PATTERNS.md (dashboard progress bar + Continue CTA + container sections, lines 175-226)
  </read_first>
  <action>
    Implements D-02 (UI half) / PAGE-05 / SC-3. Add `import Link from "next/link";` to the dashboard imports. Replace the dashed placeholder block (lines 58-66, the `border-2 border-dashed ... French 1 content launches in a future update.` div) with three real sections inside the existing `<div className="max-w-[1040px] mx-auto px-5 md:px-6 py-20">` container (keep the greeting `<h1>` and the 1040px container per CLAUDE.md rule #10).

    Section 1 — Current level card: a card (`bg-surface-container-low` or `bg-surface-container`, `rounded-[16px]`, padding `p-8`, `border border-outline-variant`; tonal layers, no heavy drop shadow per DESIGN.md rule #6) containing the level name via `font-heading` (e.g. `{level?.name}`) and a small label line. Handle the `!level` case gracefully (show a brief "Your level is being prepared" line rather than crashing).

    Section 2 — Progress bar: render the bar exactly per the `<interfaces>` tokens — a `<div>` track with `h-2 w-full rounded-full bg-surface-container-high overflow-hidden` wrapping a fill `<div>` with `h-full rounded-full bg-primary transition-all duration-300` and inline `style={{ width: \`${pct}%\` }}`. Add ARIA: on the track set `role="progressbar"`, `aria-valuenow={completedLessons.length}`, `aria-valuemin={0}`, `aria-valuemax={totalLessons}`, `aria-label={\`${completedLessons.length} of ${totalLessons} lessons complete\`}`. Below the bar render a label `<p className="font-label text-sm text-on-surface-variant mt-2">` with text `{completedLessons.length} of {totalLessons} lessons complete`. Use `bg-primary` (coral) NOT `bg-secondary` — verified against LevelCard. Do NOT use `text-tertiary`/`bg-tertiary` (green is correctness-only).

    Section 3 — Continue CTA: when `continueHref` is non-null, render a `<Link href={continueHref}>` with className `inline-block px-6 py-3 bg-primary text-on-primary rounded font-label text-sm hover:bg-primary/90 transition-colors`. Visible text: `Continue lesson` when `firstIncomplete` is truthy, otherwise `All done — take the level quiz` (sentence case). When `continueHref` is null, render nothing for this section.

    Per Claude's discretion (RESEARCH Open Question 2): OMIT the completed-lesson list — the three sections (level card, progress bar + label, Continue CTA) convey full status and fit one screen for a 6-lesson French 1 student. Keep the layout to these three sections only.

    Use only DESIGN.md token classes throughout — no ad-hoc hex values.
  </action>
  <verify>
    <automated>cd c:/Users/Ericc/frenchly && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/dashboard/page.tsx does NOT contain `French 1 content launches in a future update`
    - File does NOT contain `border-dashed`
    - File contains `import Link from "next/link"`
    - File contains `role="progressbar"` with `aria-valuenow`, `aria-valuemin`, `aria-valuemax`, and `aria-label`
    - Progress fill uses `bg-primary` and inline `width: ${pct}%` style
    - File contains the label text `lessons complete`
    - File contains a `<Link href={continueHref}` (or `href={continueHref}` on a Link) for the Continue CTA
    - File contains both copy strings `Continue lesson` and `take the level quiz`
    - File does NOT contain `text-tertiary` or `bg-tertiary`
    - File does NOT contain `bg-secondary` on the progress fill
    - No raw hex color string appears in any new className
    - The greeting `<h1>` and `max-w-[1040px]` container are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard renders a level card, an accessible bg-primary progress bar with "N of M lessons complete", and a Continue CTA to the first incomplete lesson (or the level-quiz link when done); placeholder removed; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Component (dashboard) | Authenticated request; user identity derived server-side via getUser(), never from client input |
| Server Component → Supabase | Progress queries cross into the DB; RLS enforces student row isolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-D01 | Spoofing / Elevation | dashboard progress query | mitigate | All queries scope to `user.id` from server-side `getUser()` (never a client-supplied id); RLS on `sub_component_progress` enforces `user_id = auth.uid()` at the DB layer (verified migration 20260622_phase3_lessons.sql). Existing auth + DiagnosticGate guards remain untouched. |
| T-11-D02 | Information Disclosure | levels/lessons query | accept | Level and lesson metadata (names, titles, slugs) are non-sensitive content; no PII returned by the nested select. |
| T-11-D03 | Denial of Service | empty `.in([])` query | mitigate | Sentinel `["__none__"]` guard prevents invalid SQL on empty sub-component lists (Pitfall 1). |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (RESEARCH.md: "No new packages required"). No supply-chain action needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after each task.
- `npm test` (full 156-test suite) stays green at wave merge — confirms no regression to existing auth/diagnostic/lesson flows (SC-7 path is covered by the existing SEC-05 suite).
- Manual browser walkthrough (Server Component — cannot be Jest-tested per 11-VALIDATION.md):
  1. Log in as a placed student → visit `/dashboard` → confirm current level name, progress bar, and "N of M lessons complete" label render with real data.
  2. Click "Continue lesson" → confirm arrival at the first incomplete lesson (`/levels/{slug}/lessons/{uuid}`).
  3. For a fully-completed level, confirm the CTA reads "All done — take the level quiz" and links to `/levels/{slug}`.
  4. Confirm the dashed "coming soon" placeholder is gone.
  5. Confirm an unplaced student still hits the DiagnosticGate (early return unchanged).
</verification>

<success_criteria>
- SC-3 / PAGE-05: Dashboard shows current level, lesson-completion progress bar with count, and a Continue path into learning.
- Reuses the verified level-page query + completion algorithm; honors all six RESEARCH pitfalls (empty-.in sentinel, null-level fallback, referenced-table ordering, lesson-id href, server-only fetch, DiagnosticGate untouched).
- No regressions: full `npm test` suite green at wave merge.
</success_criteria>

<output>
Create `.planning/phases/11-pages-navigation/11-02-SUMMARY.md` when done.
</output>
