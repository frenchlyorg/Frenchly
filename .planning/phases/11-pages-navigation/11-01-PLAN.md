---
phase: 11-pages-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/components/hero.tsx
  - src/app/contact/page.tsx
  - src/components/nav.tsx
autonomous: true
requirements: [PAGE-01, PAGE-02, PAGE-07]
must_haves:
  truths:
    - "A logged-out visitor on the home page sees an enabled 'Create account' button that navigates to /signup"
    - "Visiting /contact shows a page with a clickable mailto:frenchlyorg@gmail.com link"
    - "A Contact link appears in both the desktop nav bar and the mobile drawer, for both logged-in and logged-out users"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home page with enabled signup CTA"
      contains: 'href="/signup"'
    - path: "src/app/contact/page.tsx"
      provides: "Static contact page with support mailto link"
      contains: "mailto:frenchlyorg@gmail.com"
    - path: "src/components/nav.tsx"
      provides: "Navigation with Contact link (desktop + mobile)"
      contains: 'href="/contact"'
  key_links:
    - from: "src/app/page.tsx"
      to: "/signup"
      via: "next/link Link button"
      pattern: 'href="/signup"'
    - from: "src/app/contact/page.tsx"
      to: "frenchlyorg@gmail.com"
      via: "mailto anchor"
      pattern: "mailto:frenchlyorg@gmail.com"
---

<objective>
Enable the home page conversion CTA, build the static Contact page, and add a Contact link to the navigation (desktop + mobile). These are the three small, independent, statically-rendered pieces of Phase 11.

Purpose: Unblocks the signup funnel (home CTA was a disabled "Coming soon" button) and makes the support email discoverable without login.
Output: Enabled `/signup` CTA on home, new `/contact` page, Contact nav link in both nav states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-pages-navigation/11-CONTEXT.md
@.planning/phases/11-pages-navigation/11-PATTERNS.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Shared primary-button token classes (DESIGN.md, verified from hero.tsx existing DisabledCTA, minus disabled modifiers). -->
<!-- Use these EXACT class strings — do not invent new color/spacing values. -->

Primary CTA button / link:
  px-6 py-3 bg-primary text-on-primary rounded font-label text-sm hover:bg-primary/90 transition-colors
  (add "inline-block" prefix when the element is a block-level <Link>/<a>)

Nav public link (desktop):
  text-on-surface-variant hover:text-on-surface text-sm transition-colors

Nav public link (mobile drawer):
  block px-6 py-3 text-on-surface-variant hover:text-on-surface hover:bg-surface-container text-sm
  + onClick={() => setIsOpen(false)}

Static info-page layout (from src/app/mission/page.tsx):
  <main className="min-h-screen bg-background py-20">
    <article className="max-w-[720px] mx-auto px-5 md:px-6">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable home CTA and remove the now-dead DisabledCTA export</name>
  <files>src/app/page.tsx, src/components/hero.tsx</files>
  <read_first>
    - src/app/page.tsx (current state — line 1 import, line 16 `<DisabledCTA />` usage)
    - src/components/hero.tsx (DisabledCTA export at lines 110-124; HeroBackground at 17-108 must stay)
    - DESIGN.md (primary button token rules — rule #4: bg-primary fill, white/on-primary text)
    - .planning/phases/11-pages-navigation/11-PATTERNS.md (page.tsx + hero.tsx pattern sections, lines 21-81)
  </read_first>
  <action>
    Implements D-01. In src/app/page.tsx: change the import on line 1 from `import { HeroBackground, DisabledCTA } from "@/components/hero";` to import only `HeroBackground` from "@/components/hero" AND add `import Link from "next/link";`. Replace the `<DisabledCTA />` on line 16 with a `<Link>` whose `href` is `/signup`, whose className is the exact primary CTA string `px-6 py-3 bg-primary text-on-primary rounded font-label text-sm hover:bg-primary/90 transition-colors`, and whose visible text is `Create account` (sentence case per CLAUDE.md rule #9). Do not add any Supabase session check to the home page — D-01 prohibits auth-awareness here; page.tsx stays a Server Component with no `"use client"` directive.

    In src/components/hero.tsx: remove the entire `DisabledCTA` export (lines 110-124). Leave `HeroBackground` (lines 17-108) and its imports untouched. Remove any imports in hero.tsx that become unused ONLY if they were used solely by DisabledCTA — do not touch HeroBackground's imports.

    These two edits are atomic: the import removal in page.tsx and the export removal in hero.tsx must both land or `tsc` will fail (Pitfall 4 in RESEARCH.md).
  </action>
  <verify>
    <automated>cd c:/Users/Ericc/frenchly && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `href="/signup"`
    - src/app/page.tsx contains `import Link from "next/link"`
    - src/app/page.tsx does NOT contain the token `DisabledCTA`
    - src/components/hero.tsx does NOT contain the token `DisabledCTA`
    - src/components/hero.tsx still contains `export function HeroBackground` (or equivalent HeroBackground export)
    - The CTA className equals `px-6 py-3 bg-primary text-on-primary rounded font-label text-sm hover:bg-primary/90 transition-colors`
    - No raw hex color appears in the new CTA className (CLAUDE.md rule #1)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Home page renders an enabled "Create account" Link pointing to /signup styled as the primary button; DisabledCTA is fully removed from both files; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create the static Contact page</name>
  <files>src/app/contact/page.tsx</files>
  <read_first>
    - src/app/mission/page.tsx (exact layout analog — main/article wrapper, heading, prose, anchor)
    - DESIGN.md (typography tokens: font-heading, font-body, font-label; color tokens)
    - .planning/phases/11-pages-navigation/11-PATTERNS.md (contact page section, lines 230-281)
  </read_first>
  <action>
    Implements D-03 / satisfies PAGE-07 / SC-5. Create the new file src/app/contact/page.tsx as a pure Server Component (no `"use client"`, no data fetching, no API route). Export a `metadata` object with `title: "Contact — Frenchly"` (matches dashboard convention). Default-export `ContactPage`. Structure mirrors mission/page.tsx exactly: outer `<main className="min-h-screen bg-background py-20">`, inner `<article className="max-w-[720px] mx-auto px-5 md:px-6">`. Inside: an `<h1>` with classes `font-heading text-4xl font-bold text-on-surface mb-8` and sentence-case text `Contact`; a `<p>` with classes `font-body text-lg text-on-surface-variant leading-relaxed mb-8` and a short blurb such as `Have a question or feedback? We&apos;d love to hear from you.`; and an `<a>` with `href="mailto:frenchlyorg@gmail.com"`, className `inline-block px-6 py-3 bg-primary text-on-primary rounded font-label text-sm hover:bg-primary/90 transition-colors`, and visible text `frenchlyorg@gmail.com`. Use a plain `<a>` for the mailto (next/link is not needed for mailto). Use only DESIGN.md token classes — no ad-hoc hex, no `text-tertiary`/`bg-tertiary` (green is correctness-only per CLAUDE.md).
  </action>
  <verify>
    <automated>cd c:/Users/Ericc/frenchly && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/contact/page.tsx exists
    - File contains `mailto:frenchlyorg@gmail.com`
    - File contains `max-w-[720px]` and `bg-background py-20` (mission layout match)
    - File default-exports a function named `ContactPage`
    - File exports `metadata` with title `Contact — Frenchly`
    - File does NOT contain `"use client"`
    - File does NOT contain `text-tertiary` or `bg-tertiary`
    - No raw hex color string appears in any className
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>/contact renders a static page with a heading, blurb, and a clickable mailto:frenchlyorg@gmail.com button styled per DESIGN.md; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Add Contact link to nav (desktop + mobile drawer)</name>
  <files>src/components/nav.tsx</files>
  <read_first>
    - src/components/nav.tsx (desktop link list lines 36-49; `{username ? ...}` conditional begins line 51; mobile drawer links lines 131-144; mobile `{username ? ...}` begins line 146)
    - .planning/phases/11-pages-navigation/11-PATTERNS.md (nav.tsx section, lines 285-344 — critical placement note)
  </read_first>
  <action>
    Implements D-04 / satisfies SC-6. Add a `Contact` `<Link href="/contact">` in two places in src/components/nav.tsx, both OUTSIDE the `{username ? ... : ...}` conditional so the link is visible to logged-in AND logged-out users.

    Desktop: insert the new Contact `<Link>` immediately after the existing Mission `<Link>` (which closes at line 49) and before the `{username ? (` block (line 51). Use the exact desktop nav class string `text-on-surface-variant hover:text-on-surface text-sm transition-colors` and visible text `Contact`.

    Mobile drawer: insert the new Contact `<Link>` immediately after the existing mobile Mission `<Link>` (closes at line 144) and before the mobile `{username ? (` block (line 146). Use the exact mobile nav class string `block px-6 py-3 text-on-surface-variant hover:text-on-surface hover:bg-surface-container text-sm`, add `onClick={() => setIsOpen(false)}` (so tapping closes the drawer, matching sibling mobile links), and visible text `Contact`.

    Nav order after this change: Home → Mission → Contact → [Dashboard / Log in]. This is a surgical addition — do not modify the Home, Mission, Dashboard, account-dropdown, ThemeToggle, or any other existing markup.
  </action>
  <verify>
    <automated>cd c:/Users/Ericc/frenchly && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav.tsx contains exactly two occurrences of `href="/contact"` (one desktop, one mobile)
    - The desktop Contact link uses class `text-on-surface-variant hover:text-on-surface text-sm transition-colors`
    - The mobile Contact link uses class `block px-6 py-3 text-on-surface-variant hover:text-on-surface hover:bg-surface-container text-sm` and includes `onClick={() => setIsOpen(false)}`
    - Both `href="/contact"` links appear BEFORE their respective `{username ?` conditional (verifiable: the Contact link text sits between the Mission link and the username conditional in both the desktop block and the mobile drawer block)
    - Existing Home, Mission, Dashboard, and Log in links are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Contact link appears in desktop nav and mobile drawer for both auth states, positioned after Mission; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → static page | Home, Contact, and nav are statically rendered; no user input crosses any boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | mailto link on /contact | accept | frenchlyorg@gmail.com is the intended public support address; publishing it is the goal (SC-5). Standard low-value spam exposure accepted. |
| T-11-02 | Tampering | home CTA / nav links | accept | All links are static internal/mailto hrefs with no user-controlled input; no injection surface. |
| T-11-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan (RESEARCH.md: "No new packages required"). No supply-chain action needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 after every task.
- Manual browser walkthrough (Server Components cannot be Jest-tested per 11-VALIDATION.md):
  1. Visit `/` → click "Create account" → confirm navigation to `/signup`.
  2. Visit `/contact` → confirm heading, blurb, and mailto link render; clicking opens mail client to frenchlyorg@gmail.com.
  3. On a wide viewport, confirm "Contact" appears in the desktop nav between Mission and the auth links. Resize to mobile, open the hamburger drawer, confirm "Contact" appears between Mission and the auth links and closes the drawer when tapped.
  4. Confirm all of the above while both logged in and logged out (Contact must show in both states).
</verification>

<success_criteria>
- SC-1: Home page has a prominent enabled "Create account" button linking to /signup.
- SC-5: /contact links to frenchlyorg@gmail.com.
- SC-6 (partial — Contact link half): Contact reachable from consistent nav in both desktop and mobile, both auth states.
- PAGE-01, PAGE-07 satisfied; PAGE-02 (Mission) confirmed reachable via nav.
- No regressions: full `npm test` suite stays green when run at wave merge.
</success_criteria>

<output>
Create `.planning/phases/11-pages-navigation/11-01-SUMMARY.md` when done.
</output>
