---
phase: 10-security-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ErrorCard.tsx
  - .github/dependabot.yml
  - src/app/api/check-writing/route.ts
autonomous: true
requirements: [SEC-08]
must_haves:
  truths:
    - "A shared warm-card component exists that error.tsx, global-error.tsx, and not-found.tsx can all render"
    - "Dependabot scans npm dependencies weekly (grouped minor+patch) with immediate security PRs and no auto-merge"
    - "The /api/check-writing route rejects malformed input with HTTP 400 and a friendly message, leaking no stack traces"
    - "No raw SQL string building exists anywhere in src/"
  artifacts:
    - path: "src/components/ui/ErrorCard.tsx"
      provides: "Shared warm error/404 card (heading, body, action: onClick OR href)"
      min_lines: 25
    - path: ".github/dependabot.yml"
      provides: "npm Dependabot version-update config"
      contains: "package-ecosystem"
    - path: "src/app/api/check-writing/route.ts"
      provides: "Validated API handler returning 400/401 on bad input, never 500-with-internals"
      contains: "safeParse"
  key_links:
    - from: ".github/dependabot.yml"
      to: "npm ecosystem"
      via: "package-ecosystem: npm + groups.minor-and-patch"
      pattern: "package-ecosystem"
---

<objective>
Build the shared error-card primitive, activate dependency scanning, and verify-and-harden the API input-validation surface.

Purpose: Establishes the visual contract (ErrorCard) that the error/404 boundaries in plan 02 consume, and closes two of the phase's five success criteria (dependency scanning SEC-08, malformed-input handling) plus the SQL audit.
Output: src/components/ui/ErrorCard.tsx, .github/dependabot.yml, a verified/hardened src/app/api/check-writing/route.ts, and a recorded SQL-audit result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-quality/10-CONTEXT.md
@.planning/phases/10-security-quality/10-RESEARCH.md
@.planning/phases/10-security-quality/10-PATTERNS.md
@DESIGN.md
@CLAUDE.md

<interfaces>
<!-- ErrorCard contract — plan 02 (error.tsx, global-error.tsx, not-found.tsx) imports this. -->
<!-- Action is a discriminated union: onClick (reset, client) XOR href (navigation link). -->

ErrorCardProps:
  heading: string
  body: string
  action:
    | { label: string; onClick: () => void; href?: never }
    | { label: string; href: string; onClick?: never }

From src/components/ui/LinkButton.tsx (reuse for the href branch):
  LinkButtonProps { href: string; children: React.ReactNode; className?: string; ariaLabel?: string }
  default export LinkButton — useTransition + router.push, shows Spinner while pending

Warm-card layout source of truth: src/components/diagnostic/DiagnosticGate.tsx lines 26-41
  main: "min-h-screen w-full overflow-x-hidden bg-background"
  wrapper: "mx-auto max-w-[480px] px-5 py-20"
  card: "rounded-[16px] bg-surface-container-low p-8 text-center"
  h1: "font-heading text-[28px] font-semibold text-on-surface"
  p:  "mt-4 font-body text-[16px] leading-7 text-on-surface-variant"
  button class: "mt-8 inline-flex items-center justify-center min-h-[44px] rounded-[8px] bg-primary px-6 py-3 font-label font-semibold text-white hover:bg-primary/90 disabled:opacity-70 disabled:cursor-wait"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared ErrorCard component</name>
  <files>src/components/ui/ErrorCard.tsx</files>
  <read_first>
    - src/components/diagnostic/DiagnosticGate.tsx (canonical warm full-page card layout to mirror — lines 26-56)
    - src/components/ui/LinkButton.tsx (reuse directly for the href/navigation branch; mirror its disabled+Spinner pattern for the onClick branch)
    - DESIGN.md (warm palette tokens — bg-background, bg-surface-container-low, text-on-surface, text-on-surface-variant, bg-primary; no ad-hoc hex; no green)
    - CLAUDE.md (rule 3: green is correct-answer feedback only; rule 9: sentence case copy)
  </read_first>
  <action>
    Create src/components/ui/ErrorCard.tsx as a 'use client' component (first line `'use client'`) because the onClick branch receives a function prop (reset) that cannot cross the RSC boundary. Implement the exact ErrorCardProps discriminated-union interface from the interfaces block: heading: string, body: string, and action that is either { label, onClick } XOR { label, href }. Render the DiagnosticGate warm-card structure exactly: outer main with `min-h-screen w-full overflow-x-hidden bg-background`, wrapper `mx-auto max-w-[480px] px-5 py-20`, card `rounded-[16px] bg-surface-container-low p-8 text-center`, h1 with `font-heading text-[28px] font-semibold text-on-surface` rendering {heading}, p with `mt-4 font-body text-[16px] leading-7 text-on-surface-variant` rendering {body}. For the action: when action.href is present, render the LinkButton component (import from '@/components/ui/LinkButton') with href={action.href}, ariaLabel={action.label}, and the button class string from the interfaces block; when action.onClick is present, render a native button with type="button", onClick={action.onClick}, aria-label={action.label}, and the same button class string. Use only DESIGN.md tokens — no ad-hoc hex, no green, sentence case. Do not accept or render error.message anywhere.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -i "ErrorCard" || echo "ErrorCard typechecks clean"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ErrorCard.tsx first line is `'use client'`
    - File contains `interface ErrorCardProps` with `heading`, `body`, and an `action` union containing both `onClick` and `href` shapes
    - File imports LinkButton from '@/components/ui/LinkButton'
    - File contains the class strings `bg-surface-container-low`, `rounded-[16px]`, `max-w-[480px]`, and `bg-primary`
    - File does NOT contain the substring `error.message`
    - File does NOT contain any `#` hex color literal and does NOT contain `tertiary` or `green`
    - `npx tsc --noEmit` reports no errors referencing ErrorCard.tsx
  </acceptance_criteria>
  <done>ErrorCard renders the warm centered card with a heading, body, and either a reset button (onClick) or a navigation LinkButton (href), using only DESIGN.md tokens, and typechecks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add Dependabot npm config (SEC-08)</name>
  <files>.github/dependabot.yml</files>
  <read_first>
    - .planning/phases/10-security-quality/10-RESEARCH.md (Pattern 5 — exact Dependabot YAML schema, lines 287-312)
    - .planning/phases/10-security-quality/10-CONTEXT.md (D-06 npm ecosystem, D-07 grouped weekly + immediate security, D-08 no auto-merge)
  </read_first>
  <action>
    Create the .github/ directory and .github/dependabot.yml. Use schema version 2 with a single update entry for package-ecosystem "npm", directory "/", schedule interval "weekly" on day "monday" at time "09:00" timezone "America/New_York" (per D-07 low-noise weekly cadence). Add a groups block named minor-and-patch with update-types "minor" and "patch" so minor+patch land in one grouped PR; major-version bumps arrive as separate PRs automatically because they are excluded from the group (D-07). Set open-pull-requests-limit to 5. Do NOT add any auto-merge key — its absence keeps auto-merge disabled (D-08). Do not add a separate schedule for security updates: GitHub Dependabot opens security PRs immediately regardless of schedule (D-07), so no extra config is needed.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/dependabot.yml','utf8'); if(!/package-ecosystem:\s*[\"']?npm/.test(y)) process.exit(1); if(!/minor-and-patch/.test(y)) process.exit(1); if(/auto-?merge/i.test(y)) process.exit(2); console.log('dependabot.yml valid: npm ecosystem, grouped, no auto-merge')"</automated>
  </verify>
  <acceptance_criteria>
    - .github/dependabot.yml exists
    - File contains `version: 2`
    - File contains `package-ecosystem: "npm"` and `directory: "/"`
    - File contains a `groups:` block with `minor-and-patch:` containing update-types `minor` and `patch`
    - File contains `interval: "weekly"`
    - File does NOT contain any `auto-merge` / `automerge` key (case-insensitive)
    - Node YAML-shape check exits 0
  </acceptance_criteria>
  <done>.github/dependabot.yml is valid v2 YAML scanning the npm ecosystem weekly with grouped minor+patch PRs and no auto-merge; satisfies SEC-08, D-06, D-07, D-08.</done>
</task>

<task type="auto">
  <name>Task 3: Audit and harden API input validation + SQL audit (D-09/D-10/D-11, SC-3, SC-4)</name>
  <files>src/app/api/check-writing/route.ts</files>
  <read_first>
    - src/app/api/check-writing/route.ts (the live handler — confirm the existing 401/400/404/429 guards and friendly Anthropic fallback are present; only add code if a guard is missing)
    - .planning/phases/10-security-quality/10-PATTERNS.md (D-09/D-10 audit checklist, lines 307-313 — what to verify, not rewrite)
    - .planning/phases/10-security-quality/10-CONTEXT.md (D-09 400 + friendly message, D-10 500 generic line, D-11 check-writing is the primary surface)
    - CLAUDE.md (AI Checker Rules — graceful fallback pattern; Security Rules — parameterized queries only)
  </read_first>
  <action>
    First, run the SQL audit grep across src/ (the multi-pattern grep from RESEARCH.md SQL audit section): search for raw SQL string building — `.rpc(` with a template literal, backtick-quoted SELECT/INSERT/UPDATE/DELETE, and `.query(` calls. Expected result is zero matches because all queries use the Supabase builder. Record the command and its (empty) output in the SUMMARY as the SC-3 audit evidence. If any match is found, replace the raw string with the equivalent `.from().select().eq()` builder call — but per RESEARCH.md none are expected.

    Second, audit src/app/api/check-writing/route.ts against the D-09/D-10 checklist: confirm it returns 401 on no auth, 400 on invalid JSON (try/catch around request.json()), 400 on Zod safeParse failure, 404 on unknown sub-component, and a friendly 200 fallback on Anthropic error — and that NO response renders error.message, a stack trace, or raw internal details. If every guard is present (expected per PATTERNS.md), make no functional change. If the Zod safeParse failure branch currently returns `details: parsed.error.flatten()`, replace that response body with a single friendly message ("That didn't look right — try again.") and a generic `error` field only, removing the field-level details leak per D-09/D-11 (field-specific breakdowns are out of scope and the flattened Zod error can echo input internals). Confirm there is no other API route under src/app/api besides check-writing (list the directory); if another route exists, apply the same 400/500-no-internals discipline to it.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="check-writing" --no-coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SQL-audit grep over src/ for raw SQL string building returns zero matches (record command + empty output in SUMMARY)
    - src/app/api/check-writing/route.ts contains `safeParse` and returns status 400 on schema failure
    - The schema-failure response body does NOT contain `parsed.error.flatten()` (no Zod field-detail leak) and instead returns a single friendly message
    - No response in route.ts renders `error.message` or a stack trace (grep route.ts: no `error.message` inside a NextResponse.json body)
    - `ls src/app/api` shows check-writing is the only route directory, OR any additional route applies the same 400/no-internals guard
    - `npm test -- --testPathPattern="check-writing"` exits 0
  </acceptance_criteria>
  <done>SQL audit confirms zero raw SQL (SC-3); check-writing rejects malformed input with friendly 400s and leaks no internals on 400/500 (SC-4, D-09/D-10/D-11); check-writing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/check-writing | Untrusted JSON body crosses into the server handler |
| repo → npm registry | Third-party dependency code enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | check-writing 400/500 responses | mitigate | Return friendly static message only; strip `parsed.error.flatten()` from 400 body; never render error.message (D-09/D-10) |
| T-10-02 | Tampering | SQL queries across src/ | mitigate | SQL audit confirms Supabase query builder everywhere (parameterized by construction); SC-3 |
| T-10-03 | Tampering | npm dependency supply chain | mitigate | Dependabot weekly version updates + immediate security PRs (SEC-08, D-06/D-07) |
| T-10-SC | Tampering | npm installs | accept | No new packages installed this phase (RESEARCH.md: zero new dependencies); no install task to gate |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for ErrorCard.tsx
- `.github/dependabot.yml` passes the node YAML-shape check
- SQL-audit grep returns zero matches
- `npm test -- --testPathPattern="check-writing"` green
</verification>

<success_criteria>
- ErrorCard exists and exports the discriminated-union action contract for plan 02 to consume
- Dependabot config active for npm, grouped weekly, no auto-merge (SEC-08)
- check-writing returns friendly 400s with no internals leak; SQL audit clean (phase SC-3, SC-4)
</success_criteria>

<output>
Create `.planning/phases/10-security-quality/10-01-SUMMARY.md` when done. Record the SQL-audit command and its empty output as SC-3 evidence.
</output>
