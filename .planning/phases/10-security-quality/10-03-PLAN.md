---
phase: 10-security-quality
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - __tests__/diagnostic/actions.test.ts
autonomous: true
requirements: [SEC-05]
must_haves:
  truths:
    - "The login flow has a passing automated test (signIn success + wrong password + rate limit)"
    - "The save-progress flow has a passing automated test (markSubComponentComplete upsert + auth guard)"
    - "The diagnostic-unlock flow has a passing automated test that asserts the watermark advances to the next level"
    - "The full Jest suite is green"
  artifacts:
    - path: "__tests__/auth/login.test.ts"
      provides: "Login-flow coverage (verified existing)"
      contains: "signIn"
    - path: "__tests__/lessons/actions.test.ts"
      provides: "Save-progress coverage (verified existing)"
      contains: "markSubComponentComplete"
    - path: "__tests__/diagnostic/actions.test.ts"
      provides: "Diagnostic-unlock coverage incl. explicit watermark-advance assertion"
      contains: "unlocked_through_level_number"
  key_links:
    - from: "__tests__/diagnostic/actions.test.ts"
      to: "watermark advance"
      via: "assertion that unlock advances to level number 2"
      pattern: "unlocked_through_level_number"
---

<objective>
Verify and, if needed, gap-fill the SEC-05 three-path automated test suite (login, save-progress, diagnostic-unlock) and confirm the full suite is green.

Purpose: Closes SEC-05 and the phase's "automated tests pass for the three critical paths" success criterion. Per RESEARCH.md the three paths are already covered (18 suites, 156 tests green); this plan verifies that and adds at most one explicit watermark-advance assertion to the diagnostic-unlock path.
Output: A verified green suite and, if the assertion is not already explicit, a single added assertion in the diagnostic actions test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-quality/10-CONTEXT.md
@.planning/phases/10-security-quality/10-RESEARCH.md
@.planning/phases/10-security-quality/10-PATTERNS.md

<interfaces>
<!-- SEC-05 path → test file map (RESEARCH.md "Existing Test Coverage" + Validation Architecture) -->
Login flow:        __tests__/auth/login.test.ts          (signIn success, wrong password, role redirect, rate limit)
Save-progress:     __tests__/lessons/actions.test.ts     (markSubComponentComplete upsert, auth guard, UUID validation)
Diagnostic-unlock: __tests__/diagnostic/actions.test.ts  (submitDiagnosticAnswer at 100% advances watermark via admin client; expected assertion: unlocked_through_level_number: 2)

Test mock pattern (PATTERNS.md): jest.mock('@/lib/supabase/server', ...) before importing the action under test.
Run commands: per-path `npm test -- --testPathPattern="<path>"`; full suite `npm test`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the three SEC-05 paths pass and confirm the watermark-advance assertion</name>
  <files>__tests__/diagnostic/actions.test.ts</files>
  <read_first>
    - __tests__/auth/login.test.ts (confirm login-flow coverage: signIn success, wrong password, rate limit)
    - __tests__/lessons/actions.test.ts (confirm save-progress coverage: markSubComponentComplete upsert + auth guard)
    - __tests__/diagnostic/actions.test.ts (confirm diagnostic-unlock coverage; locate or add the explicit watermark-advance assertion)
    - .planning/phases/10-security-quality/10-RESEARCH.md (Existing Test Coverage — SEC-05 Gap Analysis, lines 481-493: assertion expected near actions.test.ts line 158)
    - .planning/phases/10-security-quality/10-PATTERNS.md (Test Mock Pattern, lines 355-368)
  </read_first>
  <action>
    Run the three SEC-05 path tests individually and confirm each is green: login (`npm test -- --testPathPattern="auth/login"`), save-progress (`npm test -- --testPathPattern="lessons/actions"`), diagnostic-unlock (`npm test -- --testPathPattern="diagnostic/actions"`). In __tests__/diagnostic/actions.test.ts, locate the end-of-level / completion test that exercises a passing diagnostic and confirm it explicitly asserts the watermark advances to the next level — i.e. an expectation that the admin client upsert/update is called with `unlocked_through_level_number` set to 2 (RESEARCH.md notes this assertion is expected near line 158). If that explicit assertion is already present, make NO code change — this task is verification only and you record the line in the SUMMARY. If and only if the assertion is missing (the test exercises the unlock path but does not assert the resulting watermark value), add a single `expect(...)` asserting the upsert payload contains `unlocked_through_level_number: 2`, reusing the existing mock and test structure in the file — do not restructure existing tests. Do not add new test files; the other two paths require no edits.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern="auth/login"` exits 0
    - `npm test -- --testPathPattern="lessons/actions"` exits 0
    - `npm test -- --testPathPattern="diagnostic/actions"` exits 0
    - __tests__/diagnostic/actions.test.ts contains an assertion referencing `unlocked_through_level_number` with the value 2 (the watermark-advance proof)
    - `npm test` (full suite) exits 0 with zero failing suites
    - SUMMARY records the file:line of the watermark-advance assertion and whether it was pre-existing or added
  </acceptance_criteria>
  <done>All three SEC-05 paths have passing tests, the diagnostic-unlock path explicitly asserts the watermark advances to level 2, and the full Jest suite is green. Satisfies SEC-05 and the phase's "automated tests pass for the three critical paths" criterion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan verifies existing test coverage; introduces no new runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Elevation of Privilege | diagnostic-unlock watermark | mitigate | Automated test asserts unlock advances the watermark only on a passing diagnostic, guarding against silent gating regressions (SEC-05) |
</threat_model>

<verification>
- Each of the three SEC-05 path test commands exits 0
- `npm test` full suite green
- Watermark-advance assertion present in diagnostic/actions.test.ts
</verification>

<success_criteria>
- Login, save-progress, and diagnostic-unlock flows each have passing automated tests (SEC-05)
- Diagnostic-unlock test explicitly proves the watermark advances
- Full suite green
</success_criteria>

<output>
Create `.planning/phases/10-security-quality/10-03-SUMMARY.md` when done. Record per-path test results and the watermark-assertion location.
</output>
