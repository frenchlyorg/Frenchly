---
phase: 10-security-quality
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/app/error.tsx
  - src/app/global-error.tsx
  - src/app/not-found.tsx
  - src/app/levels/[levelSlug]/page.tsx
  - src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "A runtime error during render shows the warm full-page error card with a working retry button, not a blank page"
    - "A root-layout-level error shows the warm card via global-error.tsx (production)"
    - "An unmatched route or an unknown level/lesson id shows the branded 404 card with a back-to-dashboard link"
    - "Supabase-unavailable during a page render surfaces through the error card, never a blank page"
  artifacts:
    - path: "src/app/error.tsx"
      provides: "Route-level error boundary rendering ErrorCard with reset"
      contains: "'use client'"
    - path: "src/app/global-error.tsx"
      provides: "Root fallback owning html/body, rendering ErrorCard with reset"
      contains: "<html"
    - path: "src/app/not-found.tsx"
      provides: "Branded 404 card with back-to-dashboard link"
      contains: "ErrorCard"
  key_links:
    - from: "src/app/error.tsx"
      to: "src/components/ui/ErrorCard.tsx"
      via: "import ErrorCard, action.onClick = reset"
      pattern: "ErrorCard"
    - from: "src/app/levels/[levelSlug]/page.tsx"
      to: "src/app/not-found.tsx"
      via: "notFound() from next/navigation on unknown slug"
      pattern: "notFound\\("
---

<objective>
Create the three Next.js error/404 boundaries and wire the two inline "not found" pages to the branded 404.

Purpose: Closes the phase's graceful-error-state and 404 success criteria (SC-1, SC-5) using the ErrorCard primitive from plan 01. Replaces plain inline "Lesson not found." / "Level not found." text with the branded full-page 404 (D-04).
Output: src/app/error.tsx, src/app/global-error.tsx, src/app/not-found.tsx, and two level/lesson pages updated to call notFound().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-security-quality/10-CONTEXT.md
@.planning/phases/10-security-quality/10-RESEARCH.md
@.planning/phases/10-security-quality/10-PATTERNS.md
@.planning/phases/10-security-quality/10-01-SUMMARY.md
@DESIGN.md
@CLAUDE.md

<interfaces>
<!-- ErrorCard from plan 01 — consume, do not redefine. -->
From src/components/ui/ErrorCard.tsx ('use client'):
  ErrorCardProps {
    heading: string
    body: string
    action: { label: string; onClick: () => void } | { label: string; href: string }
  }

Next.js error.tsx / global-error.tsx prop signature:
  { error: Error & { digest?: string }; reset: () => void }
  - MUST be 'use client' (reset is a function prop)
  - Do NOT render error.message (server internals)
  - global-error.tsx MUST own <html lang="en"><body> (replaces root layout); production-only

Next.js not-found.tsx:
  - Server Component, no 'use client', no props
  - Triggered by unmatched routes or notFound() from 'next/navigation'

Copy (CONTEXT.md §Specific Ideas, D-04/D-05):
  error/global-error: heading "Something went wrong" / body "We couldn't load this. Give it another try." / action label "Try again"
  not-found: heading "Page not found" / body "That page wandered off. Let's get you back on track." / action label "Back to dashboard", href "/dashboard"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error.tsx and global-error.tsx boundaries (D-01, D-03)</name>
  <files>src/app/error.tsx, src/app/global-error.tsx</files>
  <read_first>
    - src/components/ui/ErrorCard.tsx (the component to render — created in plan 01; confirm its action prop shape)
    - .planning/phases/10-security-quality/10-RESEARCH.md (Pattern 1 & 2 + Code Examples — exact signatures; Pitfalls 1, 2, 3)
    - .planning/phases/10-security-quality/10-PATTERNS.md (error.tsx and global-error.tsx assignments; layout.tsx html/body note, lines 145-190)
    - src/app/layout.tsx (html/body ownership pattern global-error must replicate minimally)
  </read_first>
  <action>
    Create src/app/error.tsx as a 'use client' component (first line `'use client'`). Export default function Error with props typed `{ error: Error & { digest?: string }; reset: () => void }`. Import ErrorCard from '@/components/ui/ErrorCard'. Render ErrorCard with heading "Something went wrong", body "We couldn't load this. Give it another try.", and action { label: 'Try again', onClick: reset }. Do NOT render error.message anywhere (it may contain server internals). The error parameter may be omitted from the destructure if unused, but the type must include it.

    Create src/app/global-error.tsx as a 'use client' component (first line `'use client'`). Export default function GlobalError with the same prop type. Because global-error.tsx replaces the root layout, it MUST render its own `<html lang="en">` and `<body>` wrapping the ErrorCard — do not import the root layout's font variables or ThemeProvider (they are bypassed); keep the markup minimal and rely on Tailwind base styles. Render the same ErrorCard heading/body/action as error.tsx. Note in a code comment that global-error.tsx only fires in production builds (next build && next start), not in next dev (RESEARCH.md Pitfall 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -iE "error\.tsx|global-error\.tsx" || echo "error boundaries typecheck clean"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/error.tsx first line is `'use client'`
    - src/app/error.tsx imports ErrorCard and passes `onClick: reset` (or `onClick={reset}` shape) in the action
    - src/app/error.tsx does NOT contain the substring `error.message`
    - src/app/global-error.tsx first line is `'use client'`
    - src/app/global-error.tsx contains `<html` and `<body`
    - src/app/global-error.tsx does NOT contain the substring `error.message`
    - `npx tsc --noEmit` reports no errors referencing error.tsx or global-error.tsx
  </acceptance_criteria>
  <done>Both error boundaries render the warm ErrorCard with a working retry (reset) button; global-error owns html/body; neither leaks error.message. Satisfies D-01, D-03, and phase SC-1/SC-5.</done>
</task>

<task type="auto">
  <name>Task 2: Create not-found.tsx and wire inline not-found pages to it (D-04, D-05)</name>
  <files>src/app/not-found.tsx, src/app/levels/[levelSlug]/page.tsx, src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx</files>
  <read_first>
    - src/components/ui/ErrorCard.tsx (the component to render — confirm href action branch)
    - .planning/phases/10-security-quality/10-RESEARCH.md (Pattern 3 + Code Examples — not-found.tsx is a server component, no props)
    - src/app/levels/[levelSlug]/page.tsx (existing inline "Level not found." block near line 121-130 — replace with notFound())
    - src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx (existing inline "Lesson not found." block near line 80-88 — replace with notFound())
    - .planning/phases/10-security-quality/10-CONTEXT.md (D-04 branded 404 + back-to-dashboard; D-05 distinct copy)
  </read_first>
  <action>
    Create src/app/not-found.tsx as a Server Component (NO 'use client', no props). Import ErrorCard from '@/components/ui/ErrorCard'. Export default function NotFound rendering ErrorCard with heading "Page not found", body "That page wandered off. Let's get you back on track.", and action { label: 'Back to dashboard', href: '/dashboard' }. Copy must stay distinct from the error card per D-05.

    In src/app/levels/[levelSlug]/page.tsx: import { notFound } from 'next/navigation' (if not already imported), and replace the `if (!level)` block that returns the inline `<main>...Level not found.</main>` markup with `if (!level) notFound()`. Remove the now-dead inline JSX for that branch.

    In src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx: import { notFound } from 'next/navigation' (if not already imported), and replace the `if (!lesson)` block that returns the inline `<main>...Lesson not found.</main>` markup with `if (!lesson) notFound()`. Remove the now-dead inline JSX for that branch.

    Both notFound() calls will surface the branded src/app/not-found.tsx (D-04 — replaces the plain inline text where a full page is appropriate).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - src/app/not-found.tsx exists, does NOT contain `'use client'`, imports ErrorCard, and contains the href `/dashboard`
    - src/app/not-found.tsx heading copy is "Page not found" (distinct from error card per D-05)
    - src/app/levels/[levelSlug]/page.tsx contains `notFound()` and no longer contains the literal `Level not found.`
    - src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx contains `notFound()` and no longer contains the literal `Lesson not found.`
    - `npm run build` completes without error
  </acceptance_criteria>
  <done>Unmatched routes and unknown level/lesson ids render the branded 404 card with a back-to-dashboard link; inline "not found" text is gone. Satisfies D-04, D-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Three error/404 boundaries (error.tsx, global-error.tsx, not-found.tsx) rendering the warm ErrorCard, plus the level/lesson pages wired to the branded 404. The full suite and build pass.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit a nonexistent route, e.g. http://localhost:3000/this-does-not-exist — confirm the branded 404 card appears ("Page not found" / "That page wandered off..." + "Back to dashboard"), not the default Next.js 404.
    2. Visit a level URL with a bad slug, e.g. http://localhost:3000/levels/not-a-real-level — confirm the same branded 404 card (not the old inline "Level not found." text).
    3. Click "Back to dashboard" — confirm it navigates to /dashboard.
    4. To verify the error card + global-error (production-only): run `npm run build && npm start`, deliberately throw in a page or layout, and confirm the warm "Something went wrong" card with a working "Try again" button appears — never a blank page. (Per RESEARCH.md, global-error only fires in production builds.)
    5. Confirm all cards use the warm palette (cream/charcoal background, coral button) and contain no green.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what looked wrong (copy, colors, broken retry/back link, blank page).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render → client error UI | Thrown server errors reach the client error boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | error.tsx / global-error.tsx | mitigate | Render static warm copy only; never render error.message in the UI (RESEARCH.md Pitfall — error.message may contain server internals) |
| T-10-05 | Denial of Service (UX) | Supabase-unavailable page render | mitigate | error boundary catches the throw and shows the warm card instead of a blank page (D-03, phase SC-5) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for error.tsx / global-error.tsx
- `npm run build` completes without error
- Human checkpoint: branded 404 on bad route + bad slug; warm error card on thrown error (production build); back-to-dashboard works; no green, no blank page
</verification>

<success_criteria>
- Runtime errors and Supabase-down render the warm error card with working retry, never a blank page (phase SC-1, SC-5)
- Unmatched routes and unknown level/lesson ids render the branded 404 with back-to-dashboard (D-04)
- error.message is never exposed in the UI
</success_criteria>

<output>
Create `.planning/phases/10-security-quality/10-02-SUMMARY.md` when done.
</output>
