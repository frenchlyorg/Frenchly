---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/theme-provider.tsx
  - tsconfig.json
  - package.json
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-08

must_haves:
  truths:
    - "npm run dev starts without errors"
    - "globals.css defines all light palette tokens in @theme block"
    - "Dark token overrides in .dark selector block flip to warm charcoal + orange palette"
    - "next-themes ThemeProvider wraps the app with localStorage persistence, enableSystem=false"
    - "TypeScript strict mode is on and tsc --noEmit exits 0"
  artifacts:
    - path: "src/app/globals.css"
      provides: "@import tailwindcss; @custom-variant dark; @theme token block; .dark overrides"
      contains: "--color-primary: #a03e40"
    - path: "src/components/theme-provider.tsx"
      provides: "Client boundary wrapper re-exporting next-themes ThemeProvider"
      exports: ["ThemeProvider"]
    - path: "tsconfig.json"
      provides: "TypeScript strict mode"
      contains: '"strict": true'
  key_links:
    - from: "src/app/globals.css"
      to: "Tailwind v4 utility classes"
      via: "@theme custom properties"
      pattern: "--color-primary.*#a03e40"
    - from: ".dark selector"
      to: "dark mode palette"
      via: "next-themes .dark class on <html>"
      pattern: ".dark \\{.*--color-background.*#1a1715"
---

<objective>
Scaffold the Next.js project and wire all DESIGN.md tokens into Tailwind v4's CSS-first
configuration. Install next-themes and create the ThemeProvider client wrapper. This plan
establishes the token foundation every future plan and phase builds on.

Purpose: Every design token from DESIGN.md must exist as a CSS custom property in globals.css
before any component can be built. The ThemeProvider wrapper must exist before layout.tsx can
reference it in Plan 02.

Output:
- `src/app/globals.css` with full @theme token block (light palette + dark overrides)
- `src/components/theme-provider.tsx` client wrapper
- `tsconfig.json` with strict mode confirmed
- `next-themes` and `lucide-react` installed
</objective>

<execution_context>
@c:/Users/Ericc/frenchly/.planning/phases/01-project-foundation/01-SKELETON.md
</execution_context>

<context>
@c:/Users/Ericc/frenchly/.planning/ROADMAP.md
@c:/Users/Ericc/frenchly/DESIGN.md
@c:/Users/Ericc/frenchly/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project and install dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts (or next.config.mjs)</files>

  <read_first>
    Read DESIGN.md and CLAUDE.md before touching anything.
    After scaffolding, read the generated tsconfig.json and package.json to verify contents.
  </read_first>

  <action>
    Run create-next-app to bootstrap the project inside the existing repo directory.
    Because the repo root (c:\Users\Ericc\frenchly) already exists as a git repo, scaffold
    into the current directory: `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"`.
    Accept all defaults. This installs Next.js, Tailwind CSS v4, and TypeScript.

    After scaffolding:
    1. Verify package.json contains `tailwindcss` at version 4.x. If it shows 3.x,
       upgrade: `npm install tailwindcss@latest @tailwindcss/postcss@latest`.
    2. Install additional dependencies: `npm install next-themes lucide-react`.
    3. Open tsconfig.json and confirm `"strict": true` is present under `compilerOptions`.
       If it is absent, add it.
    4. Confirm the project structure has `src/app/`, `src/app/layout.tsx`,
       `src/app/page.tsx`, and `src/app/globals.css`.

    Per D-01: TypeScript strict mode is non-negotiable. Every file uses `.tsx` or `.ts`.

    Do NOT use `--no-strict`. Do NOT add `// @ts-nocheck` anywhere.
  </action>

  <verify>
    <automated>cd /c/Users/Ericc/frenchly && npm run build 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `package.json` contains `"next"` at 16.x and `"tailwindcss"` at 4.x
    - `package.json` contains `"next-themes"` and `"lucide-react"` in dependencies
    - `tsconfig.json` has `"strict": true` in compilerOptions
    - `src/app/globals.css` exists (scaffold default content is fine; will be replaced in Task 2)
    - `npm run build` exits 0 (clean scaffold build)
  </acceptance_criteria>

  <done>Project scaffolded, dependencies installed, TypeScript strict mode confirmed, build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DESIGN.md tokens into globals.css and create ThemeProvider wrapper</name>
  <files>src/app/globals.css, src/components/theme-provider.tsx</files>

  <read_first>
    Read DESIGN.md in full — it is the source of truth for every token value.
    Read the existing src/app/globals.css (scaffold version) to understand what to replace.
    The scaffold likely has `@tailwind base; @tailwind components; @tailwind utilities;`
    directives — these are Tailwind v3 syntax and MUST be removed entirely.
  </read_first>

  <action>
    Replace `src/app/globals.css` entirely with the Tailwind v4 CSS-first configuration.
    Structure the file in this exact order:

    1. `@import "tailwindcss";` — replaces the three @tailwind directives from v3.

    2. `@custom-variant dark (&:where(.dark, .dark *));`
       This wires the `dark:` Tailwind utility prefix to the `.dark` class on `<html>`
       (injected by next-themes). Per D-04 and D-02.

    3. `@theme { ... }` block with ALL tokens from DESIGN.md:

       Light palette colors (from DESIGN.md `colors:` section):
       --color-background:              #fff8f5
       --color-surface:                 #fff8f5
       --color-surface-container-low:   #fbf2ed
       --color-surface-container:       #f5ece7
       --color-surface-container-high:  #efe6e2
       --color-surface-container-highest: #e9e1dc
       --color-on-surface:              #1e1b18
       --color-on-surface-variant:      #564241
       --color-outline:                 #897271
       --color-outline-variant:         #dcc0bf
       --color-primary:                 #a03e40
       --color-on-primary:              #ffffff
       --color-primary-container:       #e57373
       --color-on-primary-container:    #5e0c15
       --color-secondary:               #835500
       --color-secondary-container:     #feb64c
       --color-tertiary:                #006c4a
       --color-on-tertiary:             #ffffff
       --color-error:                   #ba1a1a
       --color-on-error:                #ffffff

       Typography (maps next/font CSS vars — these resolve after layout.tsx applies font variables to <html>):
       --font-heading: var(--font-literata)
       --font-body:    var(--font-be-vietnam-pro)
       --font-label:   var(--font-work-sans)

       Spacing (from DESIGN.md `spacing:` section):
       --spacing-container-max: 1040px
       --spacing-content-max:   720px
       --spacing-section-gap:   80px
       --spacing-gutter:        24px
       --spacing-margin-mobile: 20px

       Border radii (from DESIGN.md `rounded:` section):
       --radius-sm:   0.25rem
       --radius:      0.5rem
       --radius-md:   0.75rem
       --radius-lg:   1rem
       --radius-xl:   1.5rem
       --radius-full: 9999px

    4. `.dark { ... }` block with dark palette overrides (from DESIGN.md `dark:` section):
       --color-background:              #1a1715
       --color-surface:                 #1a1715
       --color-surface-container-low:   #221e1c
       --color-surface-container:       #262220
       --color-surface-container-high:  #312c29
       --color-surface-container-highest: #3c3633
       --color-on-surface:              #f0e7e2
       --color-on-surface-variant:      #d6c5c0
       --color-outline:                 #9c8580
       --color-outline-variant:         #534340
       --color-primary:                 #ffb866
       --color-on-primary:              #3a2400
       --color-primary-container:       #6b4a16
       --color-secondary:               #ffb3b1
       --color-secondary-container:     #80272b
       --color-tertiary:                #63dca7
       --color-on-tertiary:             #003522
       --color-error:                   #ffb4ab
       --color-on-error:                #690005

    5. `body { ... }` block applying base styles:
       background-color: var(--color-background);
       color: var(--color-on-surface);
       font-family: var(--font-body), sans-serif;

    IMPORTANT: Do NOT keep any `@tailwind base/components/utilities` directives — these are
    Tailwind v3 syntax. The single `@import "tailwindcss"` replaces all three.

    IMPORTANT: No ad-hoc hex values anywhere else in the codebase — all colors via CSS vars.

    Then create `src/components/theme-provider.tsx` as the client boundary wrapper:

    The file must have `"use client"` as its first line.
    Import ThemeProvider as NextThemesProvider from "next-themes".
    Import ComponentProps from "react".
    Export a ThemeProvider function that accepts children and any NextThemesProvider props
    and renders NextThemesProvider with those props.
    This wrapper is needed because next-themes is a client component and layout.tsx is a
    Server Component — importing next-themes directly in layout.tsx causes an RSC error.
  </action>

  <verify>
    <automated>cd /c/Users/Ericc/frenchly && npx tsc --noEmit 2>&1 && npm run build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/globals.css` starts with `@import "tailwindcss";` (no @tailwind directives)
    - `globals.css` contains `@custom-variant dark (&:where(.dark, .dark *))`
    - `globals.css` contains `--color-primary: #a03e40` inside `@theme { }`
    - `globals.css` contains `.dark {` block with `--color-primary: #ffb866`
    - `globals.css` contains `--font-heading: var(--font-literata)` inside `@theme { }`
    - `src/components/theme-provider.tsx` exists and starts with `"use client"`
    - `src/components/theme-provider.tsx` exports a `ThemeProvider` named export
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>

  <done>
    All DESIGN.md tokens are in globals.css as @theme custom properties. Dark mode overrides
    in .dark block. ThemeProvider wrapper exists. TypeScript compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | Third-party packages installed during scaffold |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | npm install (next-themes, lucide-react) | mitigate | All packages verified in Package Legitimacy Audit in RESEARCH.md — next, tailwindcss, next-themes, lucide-react all APPROVED with npm registry verification |
| T-01-02 | Information Disclosure | CSS custom properties | accept | Design tokens are public UI values; no secrets, no user data in Phase 1 CSS |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Package Legitimacy Audit in RESEARCH.md confirms all four packages: no postinstall scripts, official GitHub repos, high download counts |
</threat_model>

<verification>
After both tasks complete:
- `npm run dev` starts on port 3000 without errors
- Home page (scaffold default) renders on a warm cream background (`#fff8f5`) in light mode
- No console errors about missing Tailwind directives or unknown CSS
- `npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- All DESIGN.md tokens present in globals.css @theme block with correct hex values
- Dark mode .dark overrides present with charcoal/orange palette
- ThemeProvider wrapper created and compiles cleanly
- npm run build exits 0
</success_criteria>

<output>
Create `.planning/phases/01-project-foundation/01-01-SUMMARY.md` when done.
</output>
