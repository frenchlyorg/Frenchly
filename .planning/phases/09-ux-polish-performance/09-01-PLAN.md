---
phase: 09-ux-polish-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/levels/[levelSlug]/loading.tsx
  - src/app/levels/[levelSlug]/lessons/[lessonId]/loading.tsx
  - src/app/dashboard/loading.tsx
  - src/app/diagnostic/placement/loading.tsx
  - src/app/diagnostic/end-of-level/[levelSlug]/loading.tsx
  - src/app/levels/[levelSlug]/page.tsx
autonomous: true
requirements:
  - UX-04
  - UX-06

must_haves:
  truths:
    - "A warm-pulsing skeleton displays immediately on every data-fetching route before content loads"
    - "The level page shows «  on the first lesson with any incomplete sub-component"
    - "If all lessons in a level are complete, no «  marker appears on any card"
    - "Skeleton blocks match the structural layout of the real page (same grid, same proportions)"
    - "Skeletons are invisible to screen readers (aria-hidden=true on wrapper)"
    - "Skeletons respect prefers-reduced-motion (animate-none when motion-reduce)"
  artifacts:
    - path: "src/app/levels/[levelSlug]/loading.tsx"
      provides: "Level page skeleton — max-w-[1040px], 2-column card grid"
      contains: "animate-pulse motion-reduce:animate-none"
    - path: "src/app/levels/[levelSlug]/lessons/[lessonId]/loading.tsx"
      provides: "Lesson page skeleton — max-w-[720px], stacked item list"
      contains: "animate-pulse motion-reduce:animate-none"
    - path: "src/app/dashboard/loading.tsx"
      provides: "Dashboard skeleton — max-w-[1040px], card blocks"
      contains: "animate-pulse motion-reduce:animate-none"
    - path: "src/app/diagnostic/placement/loading.tsx"
      provides: "Placement diagnostic skeleton — max-w-[720px], option blocks"
      contains: "animate-pulse motion-reduce:animate-none"
    - path: "src/app/diagnostic/end-of-level/[levelSlug]/loading.tsx"
      provides: "End-of-level skeleton — same structure as placement"
      contains: "animate-pulse motion-reduce:animate-none"
    - path: "src/app/levels/[levelSlug]/page.tsx"
      provides: "activeLessonId computation + isActive threaded to LevelCard"
      contains: "activeLessonId"
  key_links:
    - from: "src/app/levels/[levelSlug]/page.tsx"
      to: "src/components/lessons/LevelCard"
      via: "isActive={lesson.id === activeLessonId} prop"
      pattern: "activeLessonId"
    - from: "src/app/levels/[levelSlug]/loading.tsx"
      to: "Next.js App Router Suspense boundary"
      via: "loading.tsx file convention"
      pattern: "export default function Loading"
---

<objective>
Create five loading.tsx skeleton files — one per data-fetching route — and wire the active-lesson guillemet marker on the level page using already-fetched progress data.

Purpose: Users on slow school Chromebooks see a warm structural skeleton immediately instead of a blank page. The guillemet (« ») marks the lesson they should continue next, driven by real progress data.

Output: Five new loading.tsx files + one change to levels/[levelSlug]/page.tsx (activeLessonId computation + isActive prop update).
</objective>

<execution_context>
@C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md
@C:\Users\Ericc\frenchly\CLAUDE.md
</execution_context>

<context>
@C:\Users\Ericc\frenchly\.planning\PROJECT.md
@C:\Users\Ericc\frenchly\.planning\ROADMAP.md

<interfaces>
<!-- Key types and structure from src/app/levels/[levelSlug]/page.tsx (read in full above) -->
<!-- activeLessonId insertion point: AFTER line 142 where completedSet is built -->
<!-- isActive prop: line 190, currently isActive={false} — change to isActive={lesson.id === activeLessonId} -->

From src/app/levels/[levelSlug]/page.tsx (relevant excerpt):
  Line 132: const lessons = level.lessons ?? []
  Line 136: const allSubComponentIds = lessons.flatMap((l) => (l.sub_components ?? []).map((s) => s.id))
  Line 142: const completedSet = new Set((progressRows ?? []).map((r) => r.sub_component_id))
  [INSERT activeLessonId IIFE HERE — after line 142]
  Line 190: isActive={false}   ← change to isActive={lesson.id === activeLessonId}

SubComponentRow interface (already defined in the level page file):
  interface SubComponentRow { id: string; position: number }

LessonRow already has: sub_components: SubComponentRow[]
lessons array is already ordered by position ASC (Supabase query uses .order('position', { referencedTable: 'lessons' }))

From src/components/lessons/LevelCard.tsx (isActive prop already wired):
  isActive prop accepted — renders «  glyph and "Continue" CTA when isActive && !isLocked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create five loading.tsx skeleton files (per D-SK-01 through D-SK-05)</name>
  <files>
    src/app/levels/[levelSlug]/loading.tsx,
    src/app/levels/[levelSlug]/lessons/[lessonId]/loading.tsx,
    src/app/dashboard/loading.tsx,
    src/app/diagnostic/placement/loading.tsx,
    src/app/diagnostic/end-of-level/[levelSlug]/loading.tsx
  </files>

  <read_first>
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md §Layout Contracts — all five skeleton specs with exact dimensions
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md §Q5 — complete JSX patterns for all five skeletons
    - C:\Users\Ericc\frenchly\CLAUDE.md — design rules 1, 5, 7 (no ad-hoc hex; lesson max-width 720px; dashboard 1040px)
    - C:\Users\Ericc\frenchly\src\app\levels\[levelSlug]\page.tsx — confirm max-w-[1040px] container, 2-column grid, py-20 padding
    - C:\Users\Ericc\frenchly\src\app\levels\[levelSlug]\lessons\[lessonId]\page.tsx — confirm max-w-[720px], py-12 padding, layout structure
  </read_first>

  <action>
    Create all five loading.tsx files. Each is a default export named Loading(). Each wraps its content in a div with aria-hidden="true" so screen readers skip the skeleton entirely (per D-A11Y in UI-SPEC §Interaction Contracts).

    Every skeleton block uses these base classes: bg-surface-container-high rounded-[8px] animate-pulse motion-reduce:animate-none (per D-SK-02, D-SK-05, D-A11Y-04).

    LEVEL PAGE skeleton (src/app/levels/[levelSlug]/loading.tsx):
    - Outer: main.min-h-screen.bg-background with aria-hidden="true"
    - Container: max-w-[1040px] mx-auto px-5 md:px-6 py-20
    - Header group (mb-10): heading block h-12 w-48 rounded-[8px] mb-3, description block h-4 max-w-[480px] rounded-[8px]
    - Card grid: grid grid-cols-1 md:grid-cols-2 gap-4
    - 4 skeleton cards: h-[120px] rounded-[16px] bg-surface-container-high animate-pulse motion-reduce:animate-none
    Note: rounded-[16px] on cards (matches LevelCard border-radius); rounded-[8px] on text blocks

    LESSON PAGE skeleton (src/app/levels/[levelSlug]/lessons/[lessonId]/loading.tsx):
    - Outer: main.min-h-screen.bg-background with aria-hidden="true"
    - Container: max-w-[720px] mx-auto px-5 md:px-6 py-12
    - Back link placeholder: h-4 w-32 rounded-[8px] mb-6
    - Title block: h-8 w-[320px] rounded-[8px] mb-2
    - Time estimate: h-4 w-16 rounded-[8px] mb-8
    - Progress bar: h-1 w-full rounded-full bg-surface-container-high animate-pulse motion-reduce:animate-none mb-6
    - Item list: flex flex-col gap-2, 4 items each h-[52px] rounded-[8px]
    Note: h-[52px] matches the accordion header min-h-[48px] + py-3 = ~52px rendered height

    DASHBOARD skeleton (src/app/dashboard/loading.tsx):
    - Outer: main.min-h-screen.bg-background with aria-hidden="true"
    - Container: max-w-[1040px] mx-auto px-5 md:px-6 py-20
    - Header: h-10 w-40 rounded-[8px] mb-8
    - 2 card blocks: flex flex-col gap-4, each h-[80px] rounded-[16px]

    DIAGNOSTIC PLACEMENT skeleton (src/app/diagnostic/placement/loading.tsx):
    - Outer: main.min-h-screen.bg-background with aria-hidden="true"
    - Container: max-w-[720px] mx-auto px-5 md:px-6 py-20
    - Question block: h-8 w-[400px] rounded-[8px] mb-6
    - 4 option blocks: flex flex-col gap-3, each h-[48px] rounded-[8px]

    END-OF-LEVEL DIAGNOSTIC skeleton (src/app/diagnostic/end-of-level/[levelSlug]/loading.tsx):
    - Identical structure to placement skeleton above

    Do NOT use fenced code blocks in this action. Do NOT create a shared SkeletonBlock component — inline the classes per-file as specified in UI-SPEC §Component Inventory (SkeletonBlock pattern applied inline, not extracted).
  </action>

  <verify>
    <automated>
      cd /c/Users/Ericc/frenchly && grep -r "animate-pulse" src/app/levels/src/app/dashboard/src/app/diagnostic/ 2>/dev/null | wc -l || true
    </automated>
    Manually verify:
    1. All five files exist: ls src/app/levels/[levelSlug]/loading.tsx src/app/levels/[levelSlug]/lessons/[lessonId]/loading.tsx src/app/dashboard/loading.tsx src/app/diagnostic/placement/loading.tsx src/app/diagnostic/end-of-level/[levelSlug]/loading.tsx
    2. Each file exports a default function named Loading
    3. grep -r "aria-hidden" src/app/levels/[levelSlug]/loading.tsx — confirms aria-hidden="true" on outer wrapper
    4. grep -r "motion-reduce:animate-none" src/app/levels/[levelSlug]/loading.tsx — present
    5. grep -r "bg-surface-container-high" src/app/dashboard/loading.tsx — no ad-hoc hex values
    6. npm run build -- --no-lint passes (TypeScript compiles)
  </verify>

  <acceptance_criteria>
    - All five loading.tsx files exist at the exact paths listed in files_modified
    - Every skeleton block uses bg-surface-container-high (no ad-hoc hex per CLAUDE.md rule 1)
    - Every skeleton block has both animate-pulse AND motion-reduce:animate-none (per D-SK-02, D-A11Y-04)
    - Outer wrapper on every skeleton has aria-hidden="true" (per D-A11Y in UI-SPEC §Interaction Contracts)
    - Level page skeleton uses max-w-[1040px] and grid grid-cols-1 md:grid-cols-2 gap-4 with 4 cards at h-[120px] rounded-[16px]
    - Lesson page skeleton uses max-w-[720px] and 4 item blocks at h-[52px]
    - Diagnostic skeletons use max-w-[720px] with 4 option blocks at h-[48px]
    - npm run build passes without TypeScript errors
  </acceptance_criteria>

  <done>Five loading.tsx files exist. Dev server shows warm pulsing skeletons on route navigation with slow network throttle in Chrome DevTools.</done>
</task>

<task type="auto">
  <name>Task 2: Wire activeLessonId on level page (per D-GM-01 through D-GM-04)</name>
  <files>src/app/levels/[levelSlug]/page.tsx</files>

  <read_first>
    - C:\Users\Ericc\frenchly\src\app\levels\[levelSlug]\page.tsx — full file already read; insertion point is after line 142 (completedSet assignment), change is at line 190 (isActive={false})
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md §Q4 — exact IIFE pattern with edge case analysis
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md §Interaction Contracts — D-GM section
  </read_first>

  <action>
    Edit src/app/levels/[levelSlug]/page.tsx with two changes:

    CHANGE 1 — Insert activeLessonId computation after line 142 (the completedSet assignment):
    Add a const activeLessonId block immediately after the line that reads:
      const completedSet = new Set((progressRows ?? []).map((r) => r.sub_component_id))

    The computation uses an IIFE: iterate over lessons (already position-sorted by the Supabase query), find the first lesson where any sub-component id is NOT in completedSet, return that lesson's id. If all lessons are complete, return null.

    Pattern per D-GM-01: activeLessonId = first lesson by position with at least one incomplete sub-component.
    Edge case D-GM-02: if all complete, returns null → no card gets isActive={true}.
    The lessons array is already position-sorted — no sort step needed (Supabase query uses .order('position', { referencedTable: 'lessons' })).
    lesson.sub_components is SubComponentRow[] with id: string — no schema change needed.

    CHANGE 2 — Update line 190 from isActive={false} to isActive={lesson.id === activeLessonId}.

    No other changes to the file. Do not modify the existing queries, lock logic, or JSX structure.

    Add a comment above the IIFE referencing the decision: "// D-GM-01: first lesson (by position) with any incomplete sub-component"
  </action>

  <verify>
    <automated>
      cd /c/Users/Ericc/frenchly && grep -n "activeLessonId" src/app/levels/[levelSlug]/page.tsx
    </automated>
    Expected: at least 2 lines — the const declaration and the isActive prop usage.
    Additional check: grep -n "isActive={false}" src/app/levels/[levelSlug]/page.tsx — must return 0 results (the hardcoded false is gone).
    TypeScript: npm run build passes.
  </verify>

  <acceptance_criteria>
    - grep "isActive={false}" returns no results in levels/[levelSlug]/page.tsx
    - grep "activeLessonId" returns at least 2 lines (declaration + usage in isActive prop)
    - The IIFE iterates over lessons in the existing array order (no sort added)
    - The IIFE returns null when completedSet covers all sub-component ids across all lessons
    - isActive={lesson.id === activeLessonId} used in the LevelCard render (line ~190)
    - npm run build passes without TypeScript errors
    - Manual check: with partial progress (some sub-components done, others not), the «  glyph appears on exactly one card (the first lesson with any incomplete sub-component)
  </acceptance_criteria>

  <done>Level page passes real activeLessonId to each LevelCard. The «  guillemet marker appears on the correct lesson card driven by real progress data — not hardcoded false.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Client | loading.tsx files are pure server-rendered JSX with no user data — no trust boundary crossed |
| Server → DB | Level page activeLessonId computation uses already-fetched completedSet (RLS-enforced query) — no new query, no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-SK | Information Disclosure | Skeleton loaders | accept | Skeletons contain no user data — only structural placeholders; aria-hidden prevents AT exposure of loading state |
| T-09-01-GM | Tampering | activeLessonId (server-side) | accept | Computed server-side from RLS-enforced completedSet; client cannot influence the value; no user input involved |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — verified by 09-RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
After both tasks complete:

1. Five loading.tsx files exist at exact paths in files_modified
2. Each loading.tsx exports a default function named Loading
3. Run: grep -rn "animate-pulse" src/app/levels/[levelSlug]/loading.tsx src/app/dashboard/loading.tsx src/app/diagnostic/ — all five files have matches
4. Run: grep -n "motion-reduce:animate-none" src/app/levels/[levelSlug]/loading.tsx — present
5. Run: grep -n "aria-hidden" src/app/levels/[levelSlug]/loading.tsx — "true" on outer wrapper
6. Run: grep -n "isActive={false}" src/app/levels/[levelSlug]/page.tsx — zero results
7. Run: grep -n "activeLessonId" src/app/levels/[levelSlug]/page.tsx — 2+ results
8. npm run build passes
9. No ad-hoc hex values in any new file: grep -rn "#[0-9a-fA-F]" src/app/levels/[levelSlug]/loading.tsx src/app/dashboard/loading.tsx — zero results expected
</verification>

<success_criteria>
1. All five loading.tsx files exist and export a default Loading function
2. Every skeleton block: bg-surface-container-high + animate-pulse + motion-reduce:animate-none (no ad-hoc hex)
3. All skeleton outer wrappers have aria-hidden="true"
4. Level page: activeLessonId computed from existing completedSet data; isActive={false} replaced with isActive={lesson.id === activeLessonId}
5. npm run build passes with no TypeScript errors
6. With partial progress, the guillemet «  appears on exactly one level card (first incomplete lesson)
</success_criteria>

<output>
Create .planning/phases/09-ux-polish-performance/09-01-SUMMARY.md when done
</output>
