---
phase: 09-ux-polish-performance
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - "any files surfaced by Lighthouse/axe audit (determined at runtime)"
autonomous: false
requirements:
  - SEC-06
  - SEC-07
  - UX-04
  - UX-05
  - UX-10

must_haves:
  truths:
    - "Lighthouse accessibility audit returns no new violations on any of the five modified/new routes"
    - "All accordion header buttons are keyboard-reachable via Tab and toggle via Enter/Space"
    - "The coral button #a03e40 on cream background #fff8f5 passes WCAG AA (≥4.5:1 contrast ratio)"
    - "Lighthouse mobile performance score is ≥ 85 on at least the level page and lesson page"
    - "No new violations introduced by Phase 9 changes (skeletons, accordion, post-lesson bar)"
  artifacts:
    - path: "any source files modified during audit fixes"
      provides: "WCAG AA compliant markup and contrast"
  key_links:
    - from: "Chrome DevTools Lighthouse"
      to: "localhost:3000 dev server"
      via: "Accessibility + Performance audit"
      pattern: "manual audit workflow"
---

<objective>
Run a WCAG AA accessibility audit and Lighthouse ≥85 mobile performance audit against the Phase 9 changes. Fix all violations found. This plan is a human-guided audit: Claude runs the dev server and fixes any issues surfaced; the developer runs Lighthouse in Chrome DevTools and reports results.

Purpose: Ensure the Phase 9 additions (accordion, skeleton loaders, post-lesson bar, active-lesson marker) do not introduce accessibility regressions and that the site performs adequately on school Chromebooks.

Output: Lighthouse accessibility = zero new violations. Performance ≥ 85 mobile. All interactive elements keyboard-navigable.
</objective>

<execution_context>
@C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md
@C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md
@C:\Users\Ericc\frenchly\CLAUDE.md
</execution_context>

<context>
@C:\Users\Ericc\frenchly\.planning\PROJECT.md
@C:\Users\Ericc\frenchly\.planning\ROADMAP.md

<interfaces>
<!-- Audit targets introduced in Phase 9: -->
New/modified files to audit:
  src/app/levels/[levelSlug]/loading.tsx       — skeleton, aria-hidden wrapper
  src/app/levels/[levelSlug]/lessons/[lessonId]/loading.tsx — skeleton
  src/app/dashboard/loading.tsx                — skeleton
  src/app/diagnostic/placement/loading.tsx     — skeleton
  src/app/diagnostic/end-of-level/[levelSlug]/loading.tsx — skeleton
  src/components/lessons/SubComponentItem.tsx  — accordion button + ARIA
  src/components/lessons/SubComponentList.tsx  — PostLessonBar progressbar ARIA

<!-- WCAG AA requirement references from 09-UI-SPEC.md §Accessibility Contracts: -->
- Accordion header button: aria-expanded={isOpen}, aria-controls={`content-${id}`}, id={`header-${id}`}
- Content region: id={`content-${id}`}, role="region", aria-labelledby={`header-${id}`}
- PostLessonBar: role="progressbar", aria-valuenow, aria-valuemin, aria-valuemax, aria-label="Loading next lesson"
- Skeletons: outer wrapper aria-hidden="true"

<!-- Contrast check target from 09-UI-SPEC.md §Accessibility Contracts: -->
bg-primary (#a03e40) on bg-surface-container-lowest (#ffffff) — minimum WCAG AA ratio 4.5:1 for normal text
The existing primary button already passes (verified in prior phases). Re-confirm no new coral-on-cream pairings introduced.

<!-- Lighthouse audit workflow from 09-RESEARCH.md §Q7 and §Q8: -->
1. npm run dev → open localhost:3000 in Chrome
2. DevTools → Lighthouse tab → Mobile → Accessibility + Performance → Analyze page load
3. Run 3× and average (±5 point variance on simulated throttling)
4. Key performance metrics: LCP, TBT, CLS
5. CLS cause: skeleton blocks not matching real content height → structural match in 09-01 mitigates this

<!-- Keyboard navigation contract from 09-UI-SPEC.md §Interaction Contracts D-A11Y: -->
Tab moves between accordion header buttons. Enter/Space toggles. No arrow-key navigation required.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-audit automated checks and keyboard navigation verification</name>
  <files>
    src/components/lessons/SubComponentItem.tsx,
    src/components/lessons/SubComponentList.tsx
  </files>

  <read_first>
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md §Q7 — audit workflow; §Pitfall 4 — nested interactive element check
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md §Accessibility Contracts — full ARIA contract for accordion and PostLessonBar
    - C:\Users\Ericc\frenchly\src\components\lessons\SubComponentItem.tsx — verify ARIA attributes after Plan 02 changes
    - C:\Users\Ericc\frenchly\src\components\lessons\SubComponentList.tsx — verify PostLessonBar ARIA after Plan 02 changes
  </read_first>

  <action>
    Run automated static checks before the human Lighthouse audit.

    CHECK 1 — ARIA contract completeness: grep the modified component files for required ARIA attributes. Every accordion header button must have aria-expanded, aria-controls, and id. Every content region must have role="region", aria-labelledby, and id. The PostLessonBar must have role="progressbar", aria-valuenow, aria-valuemin, aria-valuemax, aria-label.

    CHECK 2 — No nested interactive elements: grep SubComponentItem.tsx for patterns that would indicate a button inside a button. The explainer kind "Mark complete" button must only appear inside the content region div (id=content-${id}), not in the accordion header button.

    CHECK 3 — UX-10 green-free check: grep -rn "tertiary" src/components/lessons/ src/app/levels/ src/app/dashboard/ src/app/diagnostic/ — must return zero results in Phase 9 additions.

    CHECK 4 — No ad-hoc hex in Phase 9 files: grep -rn "#[0-9a-fA-F]{3,6}" src/app/levels/[levelSlug]/loading.tsx src/app/dashboard/loading.tsx src/app/diagnostic/ src/components/lessons/SubComponentItem.tsx src/components/lessons/SubComponentList.tsx — must return zero results.

    CHECK 5 — Existing test suite: run npm test. All prior tests must still pass. Report any failures with the error message for diagnosis.

    CHECK 6 — TypeScript build: run npm run build. Confirms no type errors introduced by new props.

    CHECK 7 — Keyboard navigation smoke test instructions (Claude prepares the dev server):
    Run npm run dev and confirm the server starts at localhost:3000. Report the URL.

    If any static check (1–6) reveals a violation, fix it in the relevant file before the human checkpoint.
    Fixes allowed: add missing ARIA attributes, move misplaced buttons, remove accidental tertiary tokens.
    Report what was found and fixed before handing off to the human checkpoint.
  </action>

  <verify>
    <automated>
      cd /c/Users/Ericc/frenchly && grep -rn "tertiary" src/components/lessons/ src/app/levels/$(echo '[levelSlug]')/loading.tsx src/app/dashboard/loading.tsx src/app/diagnostic/ 2>/dev/null | wc -l
    </automated>
    Expected: 0 (no tertiary tokens in Phase 9 additions)

    Additional automated checks:
    - npm test — all existing tests pass
    - npm run build — no TypeScript errors
    - grep -n "aria-expanded" src/components/lessons/SubComponentItem.tsx — present
    - grep -n "role=\"progressbar\"" src/components/lessons/SubComponentList.tsx — present
  </verify>

  <acceptance_criteria>
    - All six automated checks pass (ARIA present, no nested buttons, no tertiary, no ad-hoc hex, npm test passes, npm run build passes)
    - Any violations found are fixed before the human checkpoint
    - Dev server starts and is accessible at localhost:3000
  </acceptance_criteria>

  <done>Automated static checks pass. Dev server running. Ready for human Lighthouse audit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 9 complete implementation:
    - 5 skeleton loading.tsx files (warm-tinted, structural match, aria-hidden, animate-pulse)
    - Guillemet « active-lesson marker on level page (first incomplete lesson)
    - Accordion layout on lesson page (one-open-at-a-time, auto-advance, explainer Mark complete in content region)
    - Post-lesson coral loading bar (1.5s fill → navigate to /levels/[levelSlug])
    - All static ARIA checks passed (Task 1 above)
  </what-built>

  <how-to-verify>
    Run all checks with the dev server running at localhost:3000.

    --- SKELETON LOADERS (UX-04, SEC-06) ---
    1. Open Chrome DevTools → Network tab → throttle to "Slow 3G"
    2. Navigate to each page and verify a warm pulsing skeleton appears before content loads:
       - /levels/french-1  →  2-column card grid skeleton (1040px container)
       - /levels/french-1/lessons/[any-lesson-id]  →  stacked item list skeleton (720px container)
       - /dashboard  →  card blocks skeleton
       - /diagnostic/placement  →  option blocks skeleton
    3. Confirm: skeleton blocks are warm-tinted (bone/charcoal), not cold grey
    4. Confirm: skeleton blocks match the structural layout of the real page (same grid pattern, same number of blocks)
    5. Confirm: skeletons are NOT announced by a screen reader (aria-hidden="true" on wrapper)

    --- GUILLEMET MARKER (UX-06) ---
    6. Navigate to /levels/french-1 with partial progress (some lessons complete, at least one incomplete)
    7. Confirm: exactly ONE card shows the «  glyph — the first lesson with any incomplete sub-component
    8. Confirm: no card shows «  if all lessons are complete (test with fully completed level if possible)

    --- ACCORDION LESSON LAYOUT (SEC-07) ---
    9. Open any lesson page: confirm first item is expanded, all others are collapsed
    10. Click a collapsed item header → it should expand; the previously open item should collapse
    11. For an explainer item: expand it → verify "Mark complete" button appears INSIDE the content area (below the text), NOT in the header row
    12. Mark the explainer complete → verify it auto-collapses and the next item auto-expands
    13. Tab through the page using keyboard only → confirm each accordion header is reachable via Tab; Enter/Space toggles it
    14. Confirm completed items show the filled coral checkmark in the header even when collapsed

    --- POST-LESSON LOADING BAR (UX-05) ---
    15. Complete all sub-components in a lesson → confirm:
        a. The old "Head back to French 1 to choose your next lesson." card is GONE
        b. A coral 4px loading bar appears at the very top of the viewport (fixed, full-width)
        c. The bar fills left-to-right over ~1.5 seconds
        d. A message from the pool appears below the bar (e.g., "Well done! Loading your next lesson.")
        e. After ~1.5s, the page navigates to /levels/[levelSlug]

    --- WCAG AA + LIGHTHOUSE (SEC-06) ---
    16. In Chrome DevTools → Lighthouse tab:
        Device: Mobile | Categories: [Accessibility, Performance]
        Run "Analyze page load" on: /levels/french-1 and /levels/french-1/lessons/[any-lesson-id]
        Run 3× on the lesson page and average the Performance score
    17. Report the Accessibility score and any red/orange violations
    18. Report the averaged mobile Performance score
    19. Manually check coral button contrast:
        DevTools Elements panel → find any button with bg-primary (#a03e40) → click the color swatch → note the "Contrast ratio" value
        Required: ≥ 4.5:1 for WCAG AA on normal text

    --- GREEN-FREE CHECK (UX-10) ---
    20. Visually scan all Phase 9 additions (skeletons, accordion, loading bar) — confirm NO green elements appear except in correct-answer feedback panels

    Report: Accessibility violations (if any), Performance score, any visual issues found.
  </how-to-verify>

  <resume-signal>
    Type one of:
    - "approved" — all checks pass; Phase 9 complete
    - "fix: [description]" — specific issue found; Claude will fix and re-verify
    Report the Lighthouse Performance score and any Accessibility violations so they can be addressed before approval.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Audit tool | Lighthouse runs against the local dev server — no production data exposed during audit |
| Audit fixes → Source | Any fixes are applied to source files and committed; no runtime user data involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-ID | Information Disclosure | Lighthouse audit | accept | Audit runs against localhost:3000 with no production user data; no risk of disclosure |
| T-09-03-A11Y | Spoofing | Accordion ARIA state | mitigate | aria-expanded must reflect actual open state (boolean passed from openId === sc.id); verified in Task 1 grep checks |
| T-09-03-CLS | Denial of Service | Skeleton CLS (layout shift) | mitigate | Skeleton heights match real content per UI-SPEC exact values (h-[120px], h-[52px], h-[48px]); structural match constraint (D-SK-04) directly prevents CLS |
| T-09-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan. Lighthouse CLI is optional and not installed — DevTools path only. |
</threat_model>

<verification>
Phase 9 is complete when:

1. Human checkpoint approved (all 20 items in how-to-verify confirmed)
2. Lighthouse Accessibility: no new violations vs. baseline on lesson page and level page
3. Lighthouse Performance: ≥ 85 mobile average (3 runs) on lesson page
4. Coral button (#a03e40) contrast ratio ≥ 4.5:1 confirmed via DevTools color picker
5. Keyboard navigation confirmed: Tab reaches all accordion headers; Enter/Space toggles
6. Post-lesson bar triggers, fills, and navigates (manual end-to-end)
7. Guillemet «  appears on exactly the right card (first incomplete lesson)
8. All skeleton routes show warm structural skeletons during slow network
9. npm test passes (no regressions)
10. grep -rn "tertiary" src/components/lessons/ src/app/levels/ — zero results
</verification>

<success_criteria>
1. Lighthouse Accessibility: no new violations on lesson page or level page (SEC-06)
2. Lighthouse mobile Performance ≥ 85 averaged over 3 runs (D-A11Y-03)
3. Coral button #a03e40 passes WCAG AA contrast check ≥ 4.5:1 (D-A11Y-01 manual check)
4. All accordion headers reachable and operable via Tab + Enter/Space (SEC-07, D-A11Y-02)
5. No green (tertiary) tokens introduced anywhere in Phase 9 additions (UX-10)
6. Human checkpoint approved confirming all six success criteria from the phase goal are met
</success_criteria>

<output>
Create .planning/phases/09-ux-polish-performance/09-03-SUMMARY.md when done
</output>
