---
phase: 09-ux-polish-performance
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/components/lessons/SubComponentList.tsx
  - src/components/lessons/SubComponentItem.tsx
  - src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx
autonomous: true
requirements:
  - UX-05
  - UX-10
  - SEC-07

must_haves:
  truths:
    - "Each sub-component is a collapsible accordion item with a full-width button header"
    - "First sub-component is open on lesson load; all others are closed"
    - "Completing a sub-component auto-collapses it and opens the next incomplete one"
    - "For explainer kind, the Mark complete button lives inside the expanded content region — not nested inside the accordion header button"
    - "Completed items can be manually re-opened; their checkmark remains visible on the collapsed header"
    - "When all sub-components are done, the PostLessonBar appears (fixed top-0, 4px, coral fill)"
    - "The bar fills left-to-right over 1500ms then navigates to /levels/[levelSlug]"
    - "A random ≤8-word message from the 5-message pool appears below the bar"
    - "No green (tertiary) tokens appear in any new code in this plan"
    - "Accordion header button has aria-expanded and aria-controls; content region has role=region and aria-labelledby"
  artifacts:
    - path: "src/components/lessons/SubComponentList.tsx"
      provides: "openId state, handleComplete auto-advance, PostLessonBar, levelSlug prop"
      exports: ["SubComponentList"]
      contains: "openId"
    - path: "src/components/lessons/SubComponentItem.tsx"
      provides: "accordion shell: button header + collapsible max-height region"
      exports: ["SubComponentItem"]
      contains: "aria-expanded"
    - path: "src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx"
      provides: "levelSlug prop threaded to SubComponentList"
      contains: "levelSlug"
  key_links:
    - from: "src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx"
      to: "src/components/lessons/SubComponentList.tsx"
      via: "levelSlug prop"
      pattern: "levelSlug"
    - from: "src/components/lessons/SubComponentList.tsx"
      to: "src/components/lessons/SubComponentItem.tsx"
      via: "isOpen={openId === sc.id} onToggle={() => setOpenId(...)} props"
      pattern: "isOpen"
    - from: "src/components/lessons/SubComponentList.tsx"
      to: "next/navigation useRouter"
      via: "router.push after 1500ms setTimeout"
      pattern: "router.push"
---

<objective>
Refactor SubComponentItem into a collapsible accordion shell and add the post-lesson loading bar to SubComponentList. Also thread levelSlug from the lesson page down to SubComponentList so the bar can navigate back to the level.

Purpose: Students on lesson pages now see one focused sub-component at a time (accordion). Completing the final sub-component triggers a smooth coral loading bar that navigates them back to their level page.

Output: Modified SubComponentList.tsx + SubComponentItem.tsx + lessons/[lessonId]/page.tsx
</objective>

<execution_context>
@C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md
@C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md
@C:\Users\Ericc\frenchly\CLAUDE.md
</execution_context>

<context>
@C:\Users\Ericc\frenchly\.planning\PROJECT.md
@C:\Users\Ericc\frenchly\.planning\ROADMAP.md

<interfaces>
<!-- Current SubComponentList.tsx state (read in full above) -->
interface SubComponentListProps {
  subComponents: SubComponentData[]
  initialCompletedIds: string[]
  // ADD: levelSlug: string   ← new required prop (D-PL, Pitfall 5)
}

<!-- Current SubComponentItem.tsx props (read in full above) -->
interface SubComponentItemProps {
  id: string
  title: string
  kind: 'explainer' | 'practice' | 'writing'
  content: string | null
  isCompleted: boolean
  onComplete: (id: string) => void
  // ADD: isOpen: boolean    ← new (D-AC)
  // ADD: onToggle: () => void  ← new (D-AC)
}

<!-- SubComponentItemInternalProps (also in SubComponentItem.tsx) -->
interface SubComponentItemInternalProps extends SubComponentItemProps {
  problemData?: ProblemData | null
  initialFeedback?: string | null
  initialSubmissionText?: string | null
  // ADD: isOpen: boolean
  // ADD: onToggle: () => void
}

<!-- Lesson page (read in full above) — SubComponentList render at line 170: -->
<SubComponentList
  subComponents={subComponents}
  initialCompletedIds={completedIds}
  // ADD: levelSlug={levelSlug}   ← levelSlug already destructured from params at line 58
/>

<!-- CRITICAL nested-button constraint (RESEARCH §Q2, §Open Questions #2): -->
For kind === 'explainer': the current completion <button> (onClick={() => onComplete(id)}) lives in the header row.
Moving it inside the accordion header <button> would create nested buttons — invalid HTML.
Fix: For explainer kind, the completion button MUST move into the expanded content region (below the markdown),
not into the accordion header. The accordion header button for explainers shows the circle as a visual indicator only
(no click handler on the circle). The circle div for explainers in the collapsed header is purely presentational.

<!-- For practice/writing kinds: the current non-interactive spacer <div role="presentation" tabIndex={-1}> -->
When placed inside the accordion header <button>: remove role="presentation" and tabIndex={-1}.
The spacer div inside a button must be a plain <div> with no interactive attributes.
The existing aria-label on the spacer should also be removed (parent button handles AT interaction).

<!-- Existing allDone pattern in SubComponentList (line 67): -->
const allDone = total > 0 && completedCount === total
The PostLessonBar renders when allDone === true.

<!-- Existing 4px progress bar pattern in SubComponentList (lines 82-99): -->
Post-lesson bar should match this style: h-1 rounded-full bg-surface-container-high with bg-primary fill.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accordion shell to SubComponentItem (per D-AC-01 through D-AC-05)</name>
  <files>src/components/lessons/SubComponentItem.tsx</files>

  <read_first>
    - C:\Users\Ericc\frenchly\src\components\lessons\SubComponentItem.tsx — full file (read above); understand existing structure before modifying
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md §Q2 — accordion implementation pattern with exact class names; §Pitfall 4 — nested interactive element; §Open Questions #2 — explainer Mark complete button placement
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md §Component Inventory — AccordionShell; §Interaction Contracts — D-AC; §Color — Accordion; §Accessibility Contracts — accordion button ARIA
    - C:\Users\Ericc\frenchly\CLAUDE.md — rule 3 (no green), rule 1 (no ad-hoc hex)
  </read_first>

  <action>
    Refactor SubComponentItem.tsx to add two new props (isOpen: boolean, onToggle: () => void) to both SubComponentItemProps and SubComponentItemInternalProps interfaces.

    The outer div (currently className="py-2") becomes the accordion container. Restructure the JSX into two sections:

    SECTION 1 — Accordion header button (full-width, always visible):
    Replace the existing outer flex div (className="flex items-center gap-3") with a button element.
    Button attributes: type="button", id={`header-${id}`}, aria-expanded={isOpen}, aria-controls={`content-${id}`}, onClick={onToggle}.
    Button classes: flex items-center gap-3 py-3 w-full text-left min-h-[48px] hover:bg-surface-container-low focus:outline-none focus:border-primary focus:border-[3px] rounded-[8px].

    Inside the accordion header button, place:
    - For kind === 'practice' or kind === 'writing': The existing non-interactive spacer div. Remove role="presentation" and tabIndex={-1} from it — inside a button, it must be a plain div with no interactive attributes and no aria-label (parent button handles AT). Keep the visual circle classes and the checkmark SVG unchanged.
    - For kind === 'explainer': A new purely-presentational div (not a button) showing the completion circle state. This div has NO onClick, NO aria-pressed, NO disabled attribute. Classes match the existing button classes for the circle appearance but it is a div, not a button. The fill state (isCompleted) still controls bg-primary vs bg-transparent appearance.
    - The title span, kind chip span, and action label span — unchanged from current implementation.

    SECTION 2 — Collapsible content region (below header button):
    Wrap all content (explainer markdown, practice panel, writing panel) in a div with:
      id={`content-${id}`}, role="region", aria-labelledby={`header-${id}`}
      classes: overflow-hidden transition-[max-height] duration-200 ease-out motion-reduce:transition-none
      conditional max-height: isOpen ? 'max-h-[2000px]' : 'max-h-0'
      Use a className array join pattern: [base classes, isOpen ? 'max-h-[2000px]' : 'max-h-0'].join(' ')

    Inside the collapsible region (only visible when isOpen):
    - For kind === 'explainer': The existing markdown content (LessonMarkdown) in its mt-3 sm:ml-[60px] div, PLUS below it, a "Mark complete" button. This button is identical to the current completion toggle button (type="button", onClick={() => onComplete(id)}, disabled={isCompleted}, aria-pressed={isCompleted}, aria-label, same classes). Add mt-4 to separate it from the markdown. This is the ONLY place the Mark complete button lives for explainer kind — it does NOT appear in the header.
    - For kind === 'practice': The existing practice panel div (mt-4 sm:ml-[60px]) with PracticeCardRouter — unchanged.
    - For kind === 'writing': The existing writing panel div (mt-4 sm:ml-[60px]) with PracticeCardRouter — unchanged.

    The completed items can be re-opened manually — this is handled by onToggle in SubComponentList (D-AC-05); no special handling needed in SubComponentItem.

    Do NOT use fenced code blocks. Do NOT add any green (text-tertiary, bg-tertiary) classes anywhere.
  </action>

  <verify>
    <automated>
      cd /c/Users/Ericc/frenchly && grep -n "aria-expanded" src/components/lessons/SubComponentItem.tsx
    </automated>
    Additional checks:
    1. grep -n "aria-controls" src/components/lessons/SubComponentItem.tsx — present with content-${id} pattern
    2. grep -n "role=\"region\"" src/components/lessons/SubComponentItem.tsx — present on content div
    3. grep -n "max-h-\[2000px\]" src/components/lessons/SubComponentItem.tsx — present for open state
    4. grep -n "max-h-0" src/components/lessons/SubComponentItem.tsx — present for closed state
    5. grep -n "tertiary" src/components/lessons/SubComponentItem.tsx — ZERO results (no green per UX-10)
    6. grep -n "nested\|role=\"presentation\"\|tabIndex={-1}" src/components/lessons/SubComponentItem.tsx — tabIndex={-1} must be GONE (removed from spacer div inside button)
    7. npm run build passes
  </verify>

  <acceptance_criteria>
    - SubComponentItemProps has isOpen: boolean and onToggle: () => void
    - SubComponentItemInternalProps has isOpen: boolean and onToggle: () => void
    - Accordion header is a button element with id, aria-expanded, aria-controls, and onClick={onToggle}
    - For explainer kind: accordion header button contains a purely presentational circle div (no onClick, no aria-pressed, no tabIndex); the "Mark complete" button is INSIDE the collapsible content region below the markdown
    - For practice/writing kinds: the non-interactive spacer div inside the header button has NO role="presentation", NO tabIndex={-1}, NO aria-label — it is a plain div
    - Content region div has id={`content-${id}`}, role="region", aria-labelledby={`header-${id}`}
    - Content region uses transition-[max-height] duration-200 ease-out motion-reduce:transition-none and conditional max-h-[2000px] / max-h-0
    - No tertiary color tokens anywhere in the file
    - npm run build passes without TypeScript errors
  </acceptance_criteria>

  <done>SubComponentItem renders as a collapsible accordion item. The header button is accessible (aria-expanded, aria-controls). No nested interactive elements. Mark complete for explainers is inside the expanded content region.</done>
</task>

<task type="auto">
  <name>Task 2: Add accordion state + post-lesson loading bar to SubComponentList; thread levelSlug from lesson page (per D-AC-06, D-PL-01 through D-PL-07)</name>
  <files>
    src/components/lessons/SubComponentList.tsx,
    src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx
  </files>

  <read_first>
    - C:\Users\Ericc\frenchly\src\components\lessons\SubComponentList.tsx — full file (read above); understand existing state and handleComplete before modifying
    - C:\Users\Ericc\frenchly\src\app\levels\[levelSlug]\lessons\[lessonId]\page.tsx — line 170, the SubComponentList render call to update
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-RESEARCH.md §Q3 — complete useEffect + requestAnimationFrame + useRef pattern; §Pitfall 2 (RAF), §Pitfall 3 (StrictMode double-fire), §Pitfall 5 (levelSlug missing), §Pitfall 6 (openId must be useState not derived)
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-UI-SPEC.md §Component Inventory — PostLessonBar anatomy; §Interaction Contracts — D-PL rendering sequence and bar anatomy; §Color — Post-lesson Loading Bar; §Copywriting Contract — message pool
    - C:\Users\Ericc\frenchly\.planning\phases\09-ux-polish-performance\09-CONTEXT.md §D-PL — all 7 decisions, especially D-PL-07 (existing "Lesson complete" card REPLACED)
    - C:\Users\Ericc\frenchly\CLAUDE.md — rule 3 (no green), rule 1 (no ad-hoc hex), rule 9 (sentence case)
  </read_first>

  <action>
    CHANGES TO SubComponentList.tsx:

    1. Add levelSlug: string to SubComponentListProps (required prop, not optional).

    2. Add imports: useRouter from 'next/navigation'; useEffect, useRef, useMemo to the existing React import line. useState is already imported.

    3. Add openId state: const [openId, setOpenId] = useState<string | null>(subComponents.length > 0 ? subComponents[0].id : null). This initializes with the first sub-component open (D-AC-02). This must be useState — NOT derived — to prevent reset on optimistic update (Pitfall 6).

    4. Add post-lesson bar state and refs:
       - const router = useRouter()
       - const [barWidth, setBarWidth] = useState(0)
       - const navigatedRef = useRef(false)
       - const message = useMemo(() => { const pool = [5 messages from D-PL-04]; return pool[Math.floor(Math.random() * pool.length)] }, [])
       The 5 messages verbatim from D-PL-04: "Well done! Loading your next lesson.", "Nice job! Returning to your lessons.", "Lesson complete. Keep up the momentum.", "Très bien! Back to your level.", "Great work. Heading to your next lesson."

    5. Add useEffect for post-lesson bar navigation:
       - Dependency array: [allDone, levelSlug, router]
       - Guard: if (!allDone) return; if (navigatedRef.current) return (Pitfall 3 double-fire guard)
       - Use requestAnimationFrame to set barWidth to 100 on next frame (Pitfall 2 — ensures CSS transition fires)
       - Use setTimeout(1500) for router.push('/levels/${levelSlug}') with navigatedRef.current = true guard before push
       - Cleanup: cancelAnimationFrame(rafId) and clearTimeout(timerId)
       This matches the exact pattern in RESEARCH.md §Q3.

    6. Update handleComplete: after the existing "if (completedIds.has(id)) return" and "setSaveError(null)" lines, derive nextIncomplete BEFORE the startTransition block. Sort subComponents by position, find the entry after id where !completedIds.has(sc.id) && sc.id !== id. Call setOpenId(nextIncomplete?.id ?? null) synchronously (D-AC-03). This must run outside startTransition to be synchronous.

    7. Update SubComponentItem render: pass isOpen={openId === sc.id} and onToggle={() => setOpenId(openId === sc.id ? null : sc.id)} to each SubComponentItem. The onToggle handler: clicking open item collapses all (setOpenId(null)); clicking closed item opens it.

    8. Remove the existing "Lesson complete" static card block (the allDone && <div className="mt-8 p-6..."> block at lines 129-138) — replace entirely with the PostLessonBar JSX (D-PL-07).

    9. Add PostLessonBar JSX rendered when allDone === true:
       - Outer: fixed top-0 left-0 right-0 z-50 div with role="progressbar" aria-valuenow={barWidth} aria-valuemin={0} aria-valuemax={100} aria-label="Loading next lesson"
       - Track: h-1 w-full bg-surface-container-high
       - Fill: h-full bg-primary transition-all duration-[1500ms] ease-in-out motion-reduce:transition-none, style={{ width: `${barWidth}%` }}
       - Message: p element with classes mt-2 text-center font-label text-[13px] text-on-surface-variant, content = {message}
       Bar color bg-primary (coral) and track bg-surface-container-high — no ad-hoc hex.
       Position is fixed top-0 — does not shift page content (D-PL-02).

    CHANGES TO src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx:
    Add levelSlug={levelSlug} to the SubComponentList render call at line ~170. levelSlug is already destructured from params at line 58 — no new query or logic needed.

    Do NOT add any green (text-tertiary, bg-tertiary) classes. Do NOT use ad-hoc hex values. Sentence case on all copy (already satisfied by the 5 messages from D-PL-04).
  </action>

  <verify>
    <automated>
      cd /c/Users/Ericc/frenchly && grep -n "openId\|levelSlug\|barWidth\|navigatedRef\|requestAnimationFrame" src/components/lessons/SubComponentList.tsx
    </automated>
    Additional checks:
    1. grep -n "Lesson complete" src/components/lessons/SubComponentList.tsx — ZERO results (old static card removed, per D-PL-07)
    2. grep -n "role=\"progressbar\"" src/components/lessons/SubComponentList.tsx — present on PostLessonBar
    3. grep -n "levelSlug" src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx — must appear in the SubComponentList render call
    4. grep -n "tertiary" src/components/lessons/SubComponentList.tsx — ZERO results
    5. grep -n "tertiary" src/components/lessons/SubComponentItem.tsx — ZERO results (re-check after Task 1)
    6. npm run build — passes without TypeScript errors
    7. npm test — existing test suite still passes (no regressions from prop changes)
  </verify>

  <acceptance_criteria>
    - SubComponentListProps has levelSlug: string (required)
    - openId is useState<string | null> initialized to first sub-component's id
    - handleComplete derives nextIncomplete synchronously before startTransition and calls setOpenId(nextIncomplete?.id ?? null)
    - useEffect watches allDone; uses RAF for barWidth(100) and setTimeout(1500) for router.push; navigatedRef guard prevents double-fire
    - PostLessonBar renders when allDone === true: fixed top-0 z-50, 4px coral fill, message below, role=progressbar
    - The old "Lesson complete" static card is completely removed from SubComponentList
    - lesson page passes levelSlug={levelSlug} to SubComponentList
    - No tertiary color tokens in SubComponentList.tsx or SubComponentItem.tsx
    - npm run build passes; npm test passes (no regressions)
  </acceptance_criteria>

  <done>SubComponentList manages accordion open state. Completing all sub-components shows a coral loading bar that fills over 1.5s then navigates to /levels/[levelSlug]. The old static "Lesson complete" card is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client state → Navigation | router.push fires after 1500ms timeout; navigatedRef prevents double-navigation in StrictMode |
| Client → Server | Accordion open state (openId) is local UI state — never persisted to server; no new RLS boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-DD | Denial of Service | PostLessonBar setTimeout | accept | 1500ms client-side timer; cannot be externally triggered; navigatedRef guard prevents multiple router.push calls |
| T-09-02-NB | Tampering | Nested button (explainer kind) | mitigate | Per RESEARCH.md Pitfall 4: explainer "Mark complete" button moved entirely into content region; accordion header button has no nested interactive elements; validate with browser axe after implementation |
| T-09-02-OP | Tampering | openId state (optimistic boundary) | accept | openId is local UI state using useState — never in startTransition or useOptimistic; cannot cause server state corruption |
| T-09-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — no new Suspense, no Radix, no third-party accordion. RESEARCH.md Package Legitimacy Audit confirms zero new dependencies. |
</threat_model>

<verification>
After both tasks complete:

1. Run: grep -n "aria-expanded" src/components/lessons/SubComponentItem.tsx — present on accordion header button
2. Run: grep -n "role=\"region\"" src/components/lessons/SubComponentItem.tsx — present on content div
3. Run: grep -n "Lesson complete" src/components/lessons/SubComponentList.tsx — ZERO results
4. Run: grep -n "role=\"progressbar\"" src/components/lessons/SubComponentList.tsx — present (PostLessonBar)
5. Run: grep -n "levelSlug" src/app/levels/[levelSlug]/lessons/[lessonId]/page.tsx — appears in SubComponentList render
6. Run: grep -rn "tertiary" src/components/lessons/ — ZERO results (UX-10 green-free check)
7. npm run build — no TypeScript errors
8. npm test — existing tests pass (no regressions)
9. Manual smoke test: open a lesson, verify first item is open and others are closed; click a header to toggle; mark an explainer complete via the button inside the content region; verify auto-advance to next item; complete all items and verify coral loading bar fills then navigates to level page
</verification>

<success_criteria>
1. Each sub-component row is a collapsible accordion — first item open on load, rest closed (D-AC-02)
2. Clicking a header toggles open/closed; completing an item auto-advances to the next incomplete one (D-AC-03)
3. For explainer kind: "Mark complete" button is INSIDE the expanded content, not in the header button (no nested buttons)
4. For practice/writing kinds: completion circle spacer inside header button is a plain div (no role, no tabIndex)
5. When all sub-components complete: coral PostLessonBar appears at top of viewport, fills over 1500ms, navigates to /levels/[levelSlug]
6. Old static "Lesson complete" card is fully removed
7. PostLessonBar shows one random message from the 5-item pool (font-label, text-on-surface-variant, centered)
8. No green (tertiary) tokens in any modified file (UX-10)
9. npm run build and npm test both pass
</success_criteria>

<output>
Create .planning/phases/09-ux-polish-performance/09-02-SUMMARY.md when done
</output>
