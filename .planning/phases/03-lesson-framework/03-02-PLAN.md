---
phase: 03-lesson-framework
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/lessons/locking.ts
  - src/app/levels/[levelSlug]/page.tsx
  - src/components/lessons/LevelCard.tsx
  - src/components/ui/LockBadge.tsx
  - __tests__/lessons/level.test.ts
autonomous: true
requirements: [LESSON-01, LESSON-04]

must_haves:
  truths:
    - "A student can navigate to /levels/french-1 and see the level name and description"
    - "Each French 1 lesson card shows its title, time estimate ('{N} min'), and sub-component count ('{N} parts')"
    - "French 1 is unlocked for a new student (cards interactive); French 2 renders in a visibly locked state"
    - "The locked/unlocked state is derived from profiles.current_level_id vs the level's id"
    - "Lesson cards link to the lesson view route; locked cards are not clickable or focusable"
  artifacts:
    - path: "src/lib/lessons/locking.ts"
      provides: "deriveIsLevelLocked pure function (LESSON-04 lock logic, testable without DB)"
      exports: ["deriveIsLevelLocked"]
    - path: "src/app/levels/[levelSlug]/page.tsx"
      provides: "Level page Server Component — fetches level+lessons+progress, renders cards with lock state"
      contains: "getUser"
      min_lines: 40
    - path: "src/components/lessons/LevelCard.tsx"
      provides: "Lesson card with title, time estimate, parts count, lock/active state, guillemet marker"
    - path: "src/components/ui/LockBadge.tsx"
      provides: "Locked pill badge (warm tonal, sentence case 'Locked')"
  key_links:
    - from: "src/app/levels/[levelSlug]/page.tsx"
      to: "levels + lessons (Supabase)"
      via: "nested .from('levels').select(... lessons (...))"
      pattern: "from\\(.levels.\\)"
    - from: "src/app/levels/[levelSlug]/page.tsx"
      to: "profiles.current_level_id"
      via: "select current_level_id; passed to deriveIsLevelLocked"
      pattern: "current_level_id"
    - from: "src/components/lessons/LevelCard.tsx"
      to: "lesson view route"
      via: "Link to /levels/[levelSlug]/lessons/[lessonId] when unlocked"
      pattern: "lessons/"
---

<objective>
Build the level page vertical slice: a student visits /levels/french-1 (a protected route) and sees the level name, description, and a card per lesson showing the time estimate, sub-component count, and locked/unlocked state. French 1 is unlocked (cards link into lessons); French 2 renders visibly locked with a lock badge and is non-interactive. Lock state is derived at render time from profiles.current_level_id, and the derivation lives in a pure, testable helper that completes the Wave 0 level.test.ts scaffold.

This slice delivers LESSON-04 (level page: time estimate per lesson, locked/unlocked state, sub-component list) end-to-end: route → DB query → rendered, lock-aware cards. After this plan a student can browse French 1 lessons and see French 2 as a locked future level.

Purpose: LESSON-04 requires the level page to show all three signals (time, lock state, sub-component list). D-L05/D-L06 require the locked/unlocked UI to be real now so Phase 4 only flips data. D-L04 requires every French 1 lesson to be freely jumpable (no sequential gating).
Output: 1 pure helper, 1 Server Component page, 2 components, 1 completed test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-lesson-framework/03-UI-SPEC.md
@.planning/phases/03-lesson-framework/03-PATTERNS.md

<interfaces>
<!-- From Plan 03-01 migration. Use these directly — no DB exploration needed. -->
levels: { id, slug, name, level_number, description }
lessons: { id, level_id, slug, title, description, estimated_minutes, position }  -- order by position
sub_components: { id, lesson_id, title, kind('explainer'|'practice'|'writing'), position }
profiles.current_level_id: uuid | null  -- null means treat as French 1 (unlocked default)

Lock rule (D-L05/D-L06): isLocked = current_level_id != null && level.id !== current_level_id.
  Phase 4 generalizes to level_number <= unlocked_through; keep the helper signature stable.

Routes: level page = /levels/[levelSlug]; lesson view (Plan 03) = /levels/[levelSlug]/lessons/[lessonId]
The proxy matcher already protects all non-static routes (src/app/proxy.ts) — also call getUser() for defense-in-depth (Phase 2 pattern).
</interfaces>

# Server Component analog — mirror auth guard, createClient, profile fetch, layout tokens:
@src/app/dashboard/page.tsx

# Wave 0 scaffold to complete (flip test.todo → real tests):
@__tests__/lessons/level.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock-derivation helper + complete level.test.ts</name>
  <files>src/lib/lessons/locking.ts, __tests__/lessons/level.test.ts</files>
  <read_first>
    - __tests__/lessons/level.test.ts (Wave 0 scaffold from Plan 03-01 — the three test.todo titles to implement)
    - .planning/phases/03-lesson-framework/03-RESEARCH.md (§Pattern 4 — isLocked derivation; Pitfall 3 — null current_level_id treated as French 1)
    - .planning/phases/03-lesson-framework/03-CONTEXT.md (D-L04, D-L05, D-L06)
  </read_first>
  <behavior>
    - deriveIsLevelLocked({ levelId, levelNumber, currentLevelId }) returns false when currentLevelId === levelId (French 1 match → unlocked)
    - returns true when currentLevelId is a different non-null id and the level is not the current level (French 2 → locked)
    - returns false when currentLevelId is null/undefined (new/edge user → treat as French 1, unlocked) — addresses Pitfall 3
  </behavior>
  <action>
    Create src/lib/lessons/locking.ts exporting a pure function `deriveIsLevelLocked(args: { levelId: string; levelNumber: number; currentLevelId: string | null | undefined }): boolean`. Logic: if currentLevelId is null or undefined, return false (treat as French 1 default — Pitfall 3). Otherwise return `levelId !== currentLevelId`. Keep levelNumber in the signature unused-but-documented for Phase 4 (which will switch to `levelNumber > unlockedThroughNumber`); add a comment noting Phase 4 will generalize. No imports, no side effects — pure function so it tests without a DB.

    Then complete __tests__/lessons/level.test.ts: import deriveIsLevelLocked from '@/lib/lessons/locking' and convert the three test.todo placeholders into real assertions: (a) matching id → false; (b) different non-null current_level_id → true; (c) null current_level_id → false. Use plain Jest `expect`, no mocks.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="lessons/level" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/lessons/locking.ts exports `deriveIsLevelLocked`.
    - `npm test -- --testPathPattern=lessons/level` passes with 3 real (non-todo) tests.
    - deriveIsLevelLocked(null current_level_id) returns false; matching id returns false; different id returns true.
  </acceptance_criteria>
  <done>Pure lock-derivation helper created and the Wave 0 level.test.ts scaffold is fully implemented and green.</done>
</task>

<task type="auto">
  <name>Task 2: LockBadge + LevelCard components</name>
  <files>src/components/ui/LockBadge.tsx, src/components/lessons/LevelCard.tsx</files>
  <read_first>
    - .planning/phases/03-lesson-framework/03-UI-SPEC.md (§Component Inventory — LevelCard, LockBadge specs; §Copywriting Contract — exact labels; §Color — token usage; §Interaction Contracts — locked card non-interactive)
    - .planning/phases/03-lesson-framework/03-PATTERNS.md (§LevelCard, §LockBadge — design token classes, guillemet marker, no-green rule)
    - src/components/auth/LoginForm.tsx (design token reference: font-label/font-body/font-heading, bg-surface-container-low, border-outline-variant)
  </read_first>
  <action>
    Create src/components/ui/LockBadge.tsx — a pure presentational pill badge (no 'use client'): `inline-flex items-center gap-1 font-label text-[11px] text-on-surface-variant bg-surface-container rounded-full px-2 py-0.5`, label text "Locked" (sentence case). Optional lock glyph as a text character (no icon library per UI-SPEC).

    Create src/components/lessons/LevelCard.tsx — presentational, Server-renderable (no 'use client' unless needed). Props: `{ levelSlug, lessonId, title, estimatedMinutes, partsCount, isLocked, isActive }`. Render a card: `border border-outline-variant rounded-[16px] p-6 bg-surface-container-low`. Show title in `font-heading` headline-md weight; a time estimate badge rendered as "{estimatedMinutes} min" and a "{partsCount} parts" count, both `font-label text-[13px] text-on-surface-variant`. When isActive, prefix the title with a guillemet `«` in `font-heading text-primary mr-1` (active-lesson marker per CLAUDE.md/DESIGN.md). When isLocked: apply `opacity-60`, render the LockBadge, title in `text-on-surface-variant`, NOT clickable (no Link/onClick), `aria-disabled="true"`, `tabIndex={-1}`, and a `title` attribute "Locked — complete French 1 to unlock". When unlocked: wrap the card in a Next `Link` to `/levels/${levelSlug}/lessons/${lessonId}` with CTA label "Start lesson" (or "Continue" if isActive). Use ONLY DESIGN.md tokens — never green (text-tertiary/bg-tertiary-container is banned this phase), never ad-hoc hex, warm palette only, tonal layers (no heavy shadows). All copy sentence case.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "LevelCard|LockBadge" || echo "no type errors in LevelCard/LockBadge"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/LockBadge.tsx exists; renders the literal text "Locked" (sentence case, not "LOCKED").
    - src/components/lessons/LevelCard.tsx exists and accepts isLocked/isActive props.
    - Locked branch renders aria-disabled="true" and has no Link/onClick; unlocked branch links to `/levels/${levelSlug}/lessons/${lessonId}`.
    - Source contains the guillemet `«` for the isActive marker and uses no `text-tertiary`/`bg-tertiary` (no green) and no raw hex color.
    - `npx tsc --noEmit` reports no type errors in these two files.
  </acceptance_criteria>
  <done>LockBadge and LevelCard components built per UI-SPEC: warm tokens only, guillemet active marker, locked cards non-interactive, all copy sentence case.</done>
</task>

<task type="auto">
  <name>Task 3: Level page Server Component (/levels/[levelSlug])</name>
  <files>src/app/levels/[levelSlug]/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx (Server Component analog: getUser() guard, redirect('/login?next=...'), createClient, profile fetch, layout `max-w-[1040px] mx-auto px-5 md:px-6 py-20`, metadata export)
    - .planning/phases/03-lesson-framework/03-RESEARCH.md (§Code Examples — content table read with nested select + order referencedTable; Pitfall 5 — nested select flattening)
    - .planning/phases/03-lesson-framework/03-UI-SPEC.md (§Layout Contracts — Level Page; §Copywriting Contract)
    - src/lib/lessons/locking.ts (deriveIsLevelLocked — created Task 1)
    - src/components/lessons/LevelCard.tsx (created Task 2)
  </read_first>
  <action>
    Create src/app/levels/[levelSlug]/page.tsx as an async Server Component. Export metadata `{ title: 'French 1 — Frenchly' }` (or derive generic 'Levels — Frenchly'). In the component: `const supabase = await createClient()`, `const { data: { user } } = await supabase.auth.getUser()`, and if no user `redirect('/login?next=/levels/' + params.levelSlug)` (defense-in-depth — proxy already guards). Read params.levelSlug (await params if Next 16 requires).

    Query the level with nested lessons + sub_components: `.from('levels').select('id, slug, name, level_number, description, lessons ( id, slug, title, estimated_minutes, position, sub_components ( id, position ) )').eq('slug', params.levelSlug).order('position', { referencedTable: 'lessons' }).single()`. If level is null, render a not-found / "level not found" state. Separately fetch the profile: `.from('profiles').select('current_level_id').eq('id', user.id).single()`.

    Compute lock state via `deriveIsLevelLocked({ levelId: level.id, levelNumber: level.level_number, currentLevelId: profile?.current_level_id })`. For each lesson (already ordered by position), compute partsCount = lesson.sub_components.length and pass title, estimated_minutes, partsCount, isLocked (the whole level's lock state — French 1 lessons are all unlocked and freely jumpable per D-L04, so do NOT add per-lesson sequential gating), and lessonId to LevelCard. Render layout per UI-SPEC: outer `max-w-[1040px] mx-auto px-5 md:px-6 py-20`; header with level name in display-lg (`font-heading` 48px) and description in body-md; lesson cards single column on mobile, 2-column on tablet+ (`grid` with `gap-4`). When the level is locked, render the LockBadge and the copy "Complete the French 1 placement test to unlock French 2." above the (visually locked, non-interactive) cards. Handle empty lessons array with no crash. All copy sentence case. Beware nested-select shape (Pitfall 5): `level.lessons[].sub_components[]` is nested, not flat — type the result with an inline interface.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "levels/\[levelSlug\]/page" || echo "no type errors in level page"</automated>
  </verify>
  <acceptance_criteria>
    - File src/app/levels/[levelSlug]/page.tsx exists and is an async default-export Server Component.
    - Source contains `getUser`, `redirect("/login` (or `'/login`) with a next param, `from("levels")` (or `'levels'`) nested select including `lessons` and `sub_components`, and `current_level_id`.
    - Source imports and calls `deriveIsLevelLocked` and renders `LevelCard` per lesson.
    - Outer container uses `max-w-[1040px]`; no per-lesson sequential lock logic (D-L04 free navigation).
    - `npx tsc --noEmit` reports no type errors for this page.
  </acceptance_criteria>
  <done>Level page renders French 1 with time estimates, parts counts, and unlocked cards; French 2 renders locked. Lock state derived from current_level_id via the pure helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student browser → level page Server Component | Authenticated request reads content + own profile; user re-verified via getUser() |
| authenticated role → Postgres (RLS) | Content reads gated by `to authenticated using (true)`; profile read scoped to own row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Information Disclosure | level page data fetch | mitigate | getUser() guard + redirect to /login when unauthenticated (defense-in-depth on top of proxy matcher). |
| T-03-07 | Elevation of Privilege | locked-level UI | mitigate | Lock state is derived server-side from profiles.current_level_id (read-only here); the locked card is non-interactive (no Link, aria-disabled). A student cannot reach French 2 lessons by clicking — and cannot change current_level_id (column grant withheld in Plan 01). |
| T-03-08 | Tampering | levelSlug route param | accept | Slug is used only in a parameterized `.eq('slug', ...)` Supabase query (no string building); an unknown slug yields null → "level not found". Low risk, no data exposure. |
</threat_model>

<verification>
- `npm test -- --testPathPattern=lessons/level` passes (LESSON-04 lock derivation, 3 real tests).
- `npx tsc --noEmit` clean for the new files.
- Manual (deferred to phase human-verify): /levels/french-1 shows two lesson cards with "10 min"/"12 min" and parts counts; /levels/french-2 shows a locked state.
</verification>

<success_criteria>
- A student sees French 1 lessons with time estimates, parts counts, and unlocked interactive cards (LESSON-04).
- French 2 renders in a real locked state driven by current_level_id (D-L05/D-L06).
- French 1 lessons are freely jumpable — no sequential gating (D-L04).
- Lock logic is a pure, tested helper.
</success_criteria>

<output>
Create `.planning/phases/03-lesson-framework/03-02-SUMMARY.md` when done.
</output>
