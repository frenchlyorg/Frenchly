---
phase: 05-practice-problem-engine
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/components/practice/ConjugationTableCard.tsx
  - src/components/practice/MatchingCard.tsx
  - src/components/practice/PracticeCardRouter.tsx
autonomous: true
requirements: [PROB-03, PROB-04, PROB-05]
must_haves:
  truths:
    - "ConjugationTableCard renders a 2-column grid of 6 pronoun/input rows"
    - "ConjugationTableCard Check button grades all cells independently via gradeConjugationTable"
    - "ConjugationTableCard calls onComplete immediately on Check click regardless of score (D-06/D-09)"
    - "ConjugationTableCard shows per-cell correct/incorrect state and Answer: {form} below incorrect cells"
    - "ConjugationTableCard initialized in completed/disabled state when isCompleted=true"
    - "MatchingCard shuffles right column at render time (not at storage time), stored in useState"
    - "MatchingCard click-to-pair flow: left click selects, right click pairs, re-click left re-selects"
    - "MatchingCard Check button grades all pairs, shows correct/incorrect, calls onComplete immediately (D-05)"
    - "MatchingCard shows {left} pairs with {correct_right} below the grid for each incorrect pair"
    - "PracticeCardRouter null branches for conjugation-table and matching replaced with real components"
  artifacts:
    - path: "src/components/practice/ConjugationTableCard.tsx"
      provides: "6-cell conjugation grid with per-cell grading"
      contains: "gradeConjugationTable"
    - path: "src/components/practice/MatchingCard.tsx"
      provides: "Two-column click-to-pair matching with all-at-once Check feedback"
      contains: "gradeMatching"
    - path: "src/components/practice/PracticeCardRouter.tsx"
      provides: "Complete router — all 5 types route to a real component (no null cases)"
      contains: "ConjugationTableCard"
  key_links:
    - from: "src/components/practice/ConjugationTableCard.tsx"
      to: "src/lib/practice/grading.ts"
      via: "import { gradeConjugationTable }"
      pattern: "gradeConjugationTable"
    - from: "src/components/practice/MatchingCard.tsx"
      to: "src/lib/practice/grading.ts"
      via: "import { gradeMatching }"
      pattern: "gradeMatching"
    - from: "src/components/practice/PracticeCardRouter.tsx"
      to: "src/components/practice/ConjugationTableCard.tsx"
      via: "import ConjugationTableCard"
      pattern: "ConjugationTableCard"
---

<objective>
New complex components slice — ConjugationTableCard (6-cell grid, any-submission auto-complete) and MatchingCard (click-to-pair, all-at-once Check). This plan runs in parallel with Plan 02 (Wave 2) since both depend only on Plan 01 types and neither touches SubComponentItem or the other plan's files. Plan 03 also patches the two null cases in PracticeCardRouter left by Plan 02.

Purpose: After this plan, all four problem types are fully implemented. Plan 04 (Wave 3) wires them into the lesson page end-to-end.
Output: ConjugationTableCard.tsx, MatchingCard.tsx, and PracticeCardRouter.tsx updated to route all 5 types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-practice-problem-engine/05-CONTEXT.md
@.planning/phases/05-practice-problem-engine/05-RESEARCH.md
@.planning/phases/05-practice-problem-engine/05-UI-SPEC.md
@.planning/phases/05-practice-problem-engine/05-01-SUMMARY.md

<interfaces>
<!-- Types and grading functions from Plan 01. Read these files before building. -->

From src/lib/practice/types.ts:
```typescript
export type ConjugationTableProblem = {
  type: 'conjugation-table'
  prompt: string
  verb: string
  answers: { je: string; tu: string; il: string; nous: string; vous: string; ils: string }
}

export type MatchingProblem = {
  type: 'matching'
  prompt: string
  pairs: Array<{ left: string; right: string }>  // 4–6 pairs
}
```

From src/lib/practice/grading.ts:
```typescript
export function gradeConjugationTable(
  submitted: Record<string, string>,
  answers: Record<string, string>
): Record<string, GradeResult>   // keyed by pronoun: 'je'|'tu'|'il'|'nous'|'vous'|'ils'

export function gradeMatching(
  submitted: Record<string, string>,   // left → right (student's pairing)
  pairs: Array<{ left: string; right: string }>
): Record<string, boolean>             // keyed by left item text
```

From src/components/practice/PracticeCardRouter.tsx (created in Plan 02 — two null cases to replace):
```typescript
// current Plan 02 stubs:
case 'conjugation-table': return null  // ← replace with ConjugationTableCard
case 'matching': return null           // ← replace with MatchingCard
```

UI-SPEC grid layout for ConjugationTableCard:
- Grid: `display grid grid-cols-[auto_1fr] gap-x-3 gap-y-2`
- Pronoun label: `font-label text-[13px] text-on-surface-variant self-center text-right min-w-[36px]`
- Cell input default: `min-h-[44px] rounded px-3 py-2 font-body text-[16px] bg-surface-container-low text-on-surface border border-outline outline-none focus:border-b-[3px] focus:border-primary transition-all`
- Cell correct: add `bg-tertiary/10 border-tertiary border-b-[3px]`
- Cell incorrect: add `bg-error/10 border-error border-b-[3px]`

UI-SPEC for MatchingCard item buttons:
- Item base: `min-h-[44px] w-full flex items-center px-4 py-3 rounded-[8px] border text-left font-body text-[16px] transition-colors`
- Left default: `bg-surface-container-low border-outline text-on-surface`
- Left selected: `bg-surface-container border-primary border-2 text-on-surface`
- Left paired (pre-Check): `bg-surface-container-high border-primary text-on-surface`
- Left correct: `bg-tertiary/10 border-tertiary text-on-surface`
- Left incorrect: `bg-error/10 border-error text-on-surface`
- Right default: `bg-surface-container-low border-outline text-on-surface`
- Right paired: `bg-surface-container-high border-primary text-on-surface`
- Right correct: `bg-tertiary/10 border-tertiary text-on-surface`
- Right incorrect: `bg-error/10 border-error text-on-surface`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConjugationTableCard</name>
  <files>src/components/practice/ConjugationTableCard.tsx</files>
  <action>
    Create `src/components/practice/ConjugationTableCard.tsx` ('use client').

    Props: { problem: ConjugationTableProblem; id: string; isCompleted: boolean; onComplete: (id: string) => void }

    State:
    - cellValues: Record of pronoun → string, initialized to all '' or, if isCompleted=true, initialized to problem.answers (shows correct values on reload — Pitfall 6 guard).
    - checked: boolean, initialized to isCompleted (post-Check or pre-Check state).
    - cellResults: Record<string, GradeResult> | null, initialized to all-correct results if isCompleted=true.

    Pronouns array (in render order): ['je', 'tu', 'il', 'nous', 'vous', 'ils'].

    Card container: `bg-surface-container-low border border-outline-variant rounded-[16px] p-6 mt-4`

    Prompt: `<p className="font-body text-[18px] leading-8 text-on-surface mb-4">{problem.prompt}</p>`

    Grid: `<div role="group" aria-label="Conjugation table" className="grid grid-cols-[auto_1fr] gap-x-3 gap-y-2">` with 6 rows, each containing:
    - Pronoun label: `<span className="font-label text-[13px] text-on-surface-variant self-center text-right min-w-[36px]">{pronoun}</span>`
    - Cell input: native `<input>` (NOT FillInInput — the grid needs direct input control per-cell). Classes applied based on state: default, focused (CSS focus-visible), correct (bg-tertiary/10 border-tertiary border-b-[3px]), incorrect (bg-error/10 border-error border-b-[3px]), disabled. aria-label="{pronoun} form" (e.g., aria-label="je form"). Tab key natural order (DOM order handles this). Enter on last cell (ils) triggers handleCheck via onKeyDown. All cells disabled when checked=true.
    - Below each incorrect cell (checked && cellResults?.[p]?.correct===false): `<p className="font-label text-[13px] text-error col-start-2">Answer: {problem.answers[p]}</p>`

    Check button: `<button disabled={checked || !Object.values(cellValues).some(v => v.trim()!=='')} onClick={handleCheck} className="mt-4 px-6 py-3 bg-primary text-white font-label text-[13px] rounded-[8px] disabled:opacity-40 disabled:cursor-not-allowed">Check conjugations</button>`

    handleCheck: call gradeConjugationTable(cellValues, problem.answers as Record<string,string>), setCellResults, setChecked(true), call onComplete(id) IMMEDIATELY (D-06/D-09 — auto-completes regardless of score).

    Result summary (after Check): `<p role="status" aria-live="polite" className="mt-3 font-label text-[13px] text-on-surface-variant">` showing "{N} of 6 correct" where N = count of { correct: true } in cellResults.

    No retry — single submission (D-09). No "Try again" button.

    Tab + Enter navigation: last cell (ils input) onKeyDown: if e.key === 'Enter' and !checked, trigger handleCheck.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "ConjugationTable" | head -10</automated>
  </verify>
  <done>ConjugationTableCard compiles. Imports gradeConjugationTable from @/lib/practice/grading. Six pronoun rows in grid layout. Check button triggers gradeConjugationTable + onComplete + sets checked state. Disabled state when checked=true. Per-cell correct/incorrect CSS classes applied from cellResults. isCompleted=true initializes in completed state.</done>
</task>

<task type="auto">
  <name>Task 2: MatchingCard + PracticeCardRouter final wiring</name>
  <files>src/components/practice/MatchingCard.tsx, src/components/practice/PracticeCardRouter.tsx</files>
  <action>
    Create `src/components/practice/MatchingCard.tsx` ('use client').

    Props: { problem: MatchingProblem; id: string; isCompleted: boolean; onComplete: (id: string) => void }

    State:
    - shuffledRight: string[] — initialized ONCE in useState with a Fisher-Yates shuffle of problem.pairs.map(p => p.right). Shuffled at render time, not stored shuffled in data (Pitfall 4 guard). Use a useMemo or lazy useState initializer so the shuffle is stable across re-renders.
    - selectedLeft: string | null — which left item is currently selected (awaiting right pick)
    - pairings: Record<string, string> — maps left item text → chosen right item text (student's current pairings)
    - checked: boolean
    - matchResults: Record<string, boolean> | null — per left-item correctness after Check
    - Initialize: if isCompleted=true, set checked=true and pairings=Object.fromEntries(problem.pairs.map(p=>[p.left,p.right])) and matchResults=all true (show completed visual state — Pitfall 6 guard).

    Two-column layout: `<div className="grid grid-cols-2 gap-4 mt-4">` with left column `<div className="flex flex-col gap-2">` and right column `<div className="flex flex-col gap-2">`.

    Left item buttons (one per problem.pairs, in original order):
    - aria-pressed={!!pairings[item]}
    - aria-label="{item} — {selectedLeft===item ? 'selected' : pairings[item] ? 'paired with '+pairings[item] : 'unpaired'}"
    - onClick: handleLeftClick(item) — if checked, no-op. If selectedLeft===item, deselect (setSelectedLeft(null)). Else setSelectedLeft(item). If item already paired, its previous right partner becomes unpaired (remove from pairings) AND set selectedLeft=item so student can re-pair (D-03 interaction rule 3).
    - Visual class derived from state: correct/incorrect (post-Check from matchResults), paired (pre-Check, in pairings), selected (selectedLeft===item), default (else).

    Right item buttons (shuffledRight array):
    - onClick: handleRightClick(item) — if checked or !selectedLeft, no-op. Form pair: setSelectedLeft(null), setPairings({...pairings, [selectedLeft]: item}).
    - If item is already paired to another left item, that prior pair is broken (the left item goes back to unpaired — reassign allowed per D-03 interaction rule 2 reading).
    - Visual class: correct/incorrect (post-Check), paired (appears in Object.values(pairings)), default (else).

    Check button: disabled={checked || Object.keys(pairings).length < problem.pairs.length} (all left items must be paired before Check). Label: "Check matches". onClick: handleCheck — call gradeMatching(pairings, problem.pairs), setMatchResults, setChecked(true), call onComplete(id) IMMEDIATELY (D-05/D-06).

    Post-Check incorrect pair reveals (shown below the grid): for each left item in problem.pairs order where matchResults?.[left]===false: `<p className="font-label text-[13px] text-error mt-1">"{left}" pairs with "{correctRight}"</p>` where correctRight = problem.pairs.find(p=>p.left===left)?.right.

    Result summary: `<p role="status" aria-live="polite" className="mt-3 font-label text-[13px] text-on-surface-variant">` showing "{N} of {total} correct".

    No retry (D-09). No "Try again" button. All items disabled (cursor-default, pointer-events-none) after Check.

    Update `src/components/practice/PracticeCardRouter.tsx`: Replace the two null return cases from Plan 02:
    - 'conjugation-table': return `<ConjugationTableCard problem={problemData} id={subComponentId} isCompleted={isCompleted} onComplete={onComplete} />`
    - 'matching': return `<MatchingCard problem={problemData} id={subComponentId} isCompleted={isCompleted} onComplete={onComplete} />`
    Import ConjugationTableCard and MatchingCard. Remove the TODO comments left by Plan 02. The switch statement is now exhaustive over all 5 ProblemData types.
  </action>
  <verify>
    <automated>npx tsc --noEmit --skipLibCheck 2>&1 | grep -i "Matching\|Conjugation\|PracticeCard" | head -20</automated>
  </verify>
  <done>MatchingCard compiles. gradeMatching imported from @/lib/practice/grading. Right column shuffled via useState (stable, not re-shuffled on re-render). Click-to-pair state machine covers: select left, pair right, re-select paired left (breaks prior pair). Check fires gradeMatching + onComplete + sets checked. PracticeCardRouter switch is exhaustive — no null cases, no TypeScript "not all code paths return a value" errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client interaction → grading | ConjugationTableCard and MatchingCard grade entirely client-side. Only sub-component ID sent to server. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | MatchingCard Check without all pairs filled | mitigate | Check button disabled until Object.keys(pairings).length === problem.pairs.length. Button is also aria-disabled when disabled. |
| T-05-07 | Tampering | ConjugationTableCard Check with all-empty cells | mitigate | Check button disabled when no cell has content (Object.values(cellValues).some(v=>v.trim()!=='') must be true). Per D-09, any submission auto-completes — the guard prevents a zero-effort auto-complete. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx tsc --noEmit --skipLibCheck` — zero TypeScript errors across all new files
- `npx jest` — full test suite stays green (no regressions)
- `grep -n "gradeConjugationTable\|gradeMatching" src/components/practice/*.tsx` confirms both grading functions are imported (not re-implemented)
- Grep: `grep -n "shuffle\|Fisher" src/components/practice/MatchingCard.tsx` confirms shuffle is present
- Grep: `grep -n "case 'conjugation-table'\|case 'matching'" src/components/practice/PracticeCardRouter.tsx` confirms no null returns remain
</verification>

<success_criteria>
- ConjugationTableCard: 6-cell grid, per-cell correct/incorrect state, auto-complete on any Check, no retry
- ConjugationTableCard: isCompleted=true initializes to completed visual state (all cells disabled, values pre-filled)
- MatchingCard: right column shuffled once on mount (not in same order as left column)
- MatchingCard: click-to-pair works including re-pairing an already-paired left item
- MatchingCard: Check grades all pairs, shows incorrect reveals, auto-completes
- PracticeCardRouter routes all 5 types to real components (no nulls, no exhaustiveness errors)
</success_criteria>

<output>
Create `.planning/phases/05-practice-problem-engine/05-03-SUMMARY.md` when done
</output>
