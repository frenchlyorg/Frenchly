---
phase: 06-ai-writing-checker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260624_phase6_writing.sql
  - .env.local
  - src/__tests__/practice/schema.test.ts
  - src/__tests__/practice/WrittenCard.test.tsx
  - src/__tests__/api/check-writing.test.ts
autonomous: true
requirements: [AI-01, AI-02, AI-03, AI-04]

must_haves:
  truths:
    - "Migration SQL file exists with writing_submissions table, unique index, rate-limit index, grants, and RLS policies"
    - "ANTHROPIC_API_KEY= slot is present in .env.local with a comment pointing to console.anthropic.com"
    - "Wave 0 test scaffolds exist for WrittenCard, check-writing API, and schema 'written' case — all failing red (no implementation yet)"
  artifacts:
    - path: "supabase/migrations/20260624_phase6_writing.sql"
      provides: "writing_submissions table DDL ready for push"
      contains: "create table public.writing_submissions"
    - path: ".env.local"
      provides: "ANTHROPIC_API_KEY env slot"
      contains: "ANTHROPIC_API_KEY="
    - path: "src/__tests__/practice/WrittenCard.test.tsx"
      provides: "Wave 0 failing tests for AI-01 (render), AI-04 (fallback)"
    - path: "src/__tests__/api/check-writing.test.ts"
      provides: "Wave 0 failing tests for AI-03 (rate limit)"
  key_links:
    - from: "supabase/migrations/20260624_phase6_writing.sql"
      to: "Plan 02 schema push"
      via: "supabase db push reads this file"
      pattern: "writing_submissions"
---

<objective>
Foundation layer for the AI Writing Checker. Creates the DB migration SQL, seeds the ANTHROPIC_API_KEY env slot, and scaffolds Wave 0 test files that will go RED (no implementation exists yet).

Purpose: Unblocks all downstream plans — schema push needs the SQL, the API route needs the env var, and tests must be scaffolded before implementation begins.
Output: Migration SQL file, updated .env.local, three test scaffolds in failing state.
</objective>

<execution_context>
@C:/Users/Ericc/frenchly/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Ericc/frenchly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ai-writing-checker/06-CONTEXT.md
@.planning/phases/06-ai-writing-checker/06-RESEARCH.md

<interfaces>
<!-- Key patterns from existing migrations and test files the executor replicates. -->

From supabase/migrations/20260622_phase3_lessons.sql (user-scoped RLS pattern):
- Table structure: uuid PK (gen_random_uuid()), FK references on delete cascade, timestamptz created_at default now()
- Grant pattern: `grant select, insert on public.TABLE to authenticated; grant all on public.TABLE to service_role;`
- RLS enable: `alter table public.TABLE enable row level security;`
- User-scoped select: `using ((select auth.uid()) = user_id);`
- User-scoped insert: `with check ((select auth.uid()) = user_id);`

From src/__tests__/practice/schema.test.ts (test file pattern):
- import { parseProblemContent } from '@/lib/practice/schema'
- describe block per feature, it() per case
- Failing scaffold = test with no implementation imported yet (will fail with "not a function" or import error)

From src/lib/practice/types.ts (current ProblemData union — executor must know what 'written' joins):
- ProblemData = MCProblem | FillInProblem | ConjugationTableProblem | ConjugationSingleProblem | MatchingProblem
- WrittenProblem will add: { type: 'written', prompt: string }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write writing_submissions migration SQL</name>
  <files>supabase/migrations/20260624_phase6_writing.sql</files>
  <read_first>
    - supabase/migrations/20260622_phase3_lessons.sql (RLS policy structure and grant pattern to replicate exactly)
    - supabase/migrations/20260623_phase5_practice.sql (check constraint pattern — confirm 'written' need not be added here; writing kind uses problem_type = NULL)
    - .planning/phases/06-ai-writing-checker/06-RESEARCH.md §Pattern 7 (full migration template)
  </read_first>
  <action>
Create supabase/migrations/20260624_phase6_writing.sql with the following exact structure:

Table: public.writing_submissions
Columns:
  - id uuid not null primary key default gen_random_uuid()
  - user_id uuid not null references auth.users on delete cascade
  - sub_component_id uuid not null references public.sub_components on delete cascade
  - feedback_text text (nullable — null means API error graceful fallback, row still written per D-06 and Pitfall 5)
  - created_at timestamptz not null default now()

Indexes:
  - UNIQUE on (user_id, sub_component_id): `create unique index idx_ws_user_sub on public.writing_submissions (user_id, sub_component_id);` — enforces D-12 one-shot per sub-component
  - Rate-limit index: `create index idx_ws_user_created on public.writing_submissions (user_id, created_at);` — used by the `gte('created_at', todayUTC)` COUNT query in the API route

Grants (mirror phase3 user-scoped table pattern exactly):
  - `grant select, insert on public.writing_submissions to authenticated;`
  - `grant all on public.writing_submissions to service_role;`

RLS:
  - `alter table public.writing_submissions enable row level security;`
  - Policy "Students can read own writing submissions": for select to authenticated using ((select auth.uid()) = user_id)
  - Policy "Students can insert own writing submissions": for insert to authenticated with check ((select auth.uid()) = user_id)

Note: No UPDATE policy — rows are immutable once written (D-12). No delete policy — cascade handles cleanup when user is deleted.

Do NOT modify the phase5 check constraint on sub_components.problem_type — writing sub-components use problem_type = NULL (confirmed in RESEARCH.md open question 3).
  </action>
  <verify>
    <automated>grep -c "create table public.writing_submissions" supabase/migrations/20260624_phase6_writing.sql && grep -c "idx_ws_user_sub" supabase/migrations/20260624_phase6_writing.sql && grep -c "idx_ws_user_created" supabase/migrations/20260624_phase6_writing.sql && grep -c "enable row level security" supabase/migrations/20260624_phase6_writing.sql</automated>
  </verify>
  <done>File exists. All four grep commands return 1. Migration contains table DDL, both indexes, RLS enable, and two RLS policies.</done>
</task>

<task type="auto">
  <name>Task 2: Add ANTHROPIC_API_KEY slot to .env.local</name>
  <files>.env.local</files>
  <read_first>
    - .env.local (read current contents — do not overwrite existing vars)
  </read_first>
  <action>
Append to .env.local (after existing vars, do not disturb any existing key):

```
# Anthropic — AI Writing Checker (Phase 6)
# Get your key at: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
```

The value stays blank — this is a slot for the developer to fill. The executor must NOT populate an actual key. The comment line tells the developer exactly where to get the value.

Do not add this to any client-side env file. This key must stay server-only (CLAUDE.md Security Rules). It must never appear in any file prefixed NEXT_PUBLIC_.
  </action>
  <verify>
    <automated>grep -c "ANTHROPIC_API_KEY=" .env.local</automated>
  </verify>
  <done>grep returns 1 or more. .env.local contains the ANTHROPIC_API_KEY= slot. Existing vars are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold Wave 0 failing test files</name>
  <files>
    src/__tests__/practice/schema.test.ts,
    src/__tests__/practice/WrittenCard.test.tsx,
    src/__tests__/api/check-writing.test.ts
  </files>
  <read_first>
    - src/__tests__/practice/schema.test.ts (existing file — append 'written' test cases to it; do not rewrite the whole file)
    - src/__tests__/practice/WrittenCard.test.tsx (does not exist yet — create new)
    - src/__tests__/api/check-writing.test.ts (does not exist yet — create new)
    - src/components/practice/FillInPracticeCard.tsx (testing-library render pattern to replicate)
    - .planning/phases/06-ai-writing-checker/06-RESEARCH.md §Validation Architecture (test map: AI-01, AI-03, AI-04)
  </read_first>
  <behavior>
    schema.test.ts additions:
    - 'written' valid JSON → parseProblemContent returns { type: 'written', prompt: string } (AI-01)
    - 'written' missing prompt field → parseProblemContent returns null

    WrittenCard.test.tsx (new file, RED state — WrittenCard does not exist yet):
    - Renders the prompt text from WrittenProblem.prompt
    - "Check my writing" button is present and initially enabled when text is entered
    - Textarea is disabled and feedback is shown after submission (mocked fetch returns { feedback: 'Bien!' })
    - On fetch returning 429, shows "You've used all your writing checks for today — come back tomorrow!" (D-07)
    - On fetch network error, shows "We couldn't check that right now — keep going!" (D-06)
    - When isCompleted=true and initialFeedback provided, renders feedback on mount without fetch (D-08, D-09)

    check-writing.test.ts (new file, RED state — route does not exist yet):
    - Rate limit: when DB count returns 10, handler returns 429 with error: 'rate_limited' (AI-03)
    - Unauthenticated request returns 401 (SEC-01)
    - Invalid body (missing subComponentId) returns 400
  </behavior>
  <action>
Append to src/__tests__/practice/schema.test.ts (after the last existing test block): two new it() cases for 'written' type — one valid and one missing-prompt. Do not alter existing tests.

Create src/__tests__/practice/WrittenCard.test.tsx as a new test file. Import WrittenCard from '@/components/practice/WrittenCard' (will fail to resolve until Plan 03 — that is expected RED state). Use @testing-library/react render and userEvent. Mock global.fetch using jest.fn(). Mock '@/app/lessons/actions' module to stub markSubComponentComplete as jest.fn(). Cover the six behavior cases above.

Create src/__tests__/api/check-writing.test.ts as a new test file. This tests route logic in isolation — mock '@/lib/supabase/server' createClient factory and '@anthropic-ai/sdk' Anthropic class. Cover the three behavior cases above. Note: Full HTTP integration tests are not required; unit-level mocking of the route handler is sufficient. Import the POST handler from '@/app/api/check-writing/route' (will fail until Plan 02 — expected RED state).

In all three files: use describe/it structure matching schema.test.ts style. Add a comment at the top: "Wave 0 scaffold — tests are RED until implementation plans complete."
  </action>
  <verify>
    <automated>npx jest src/__tests__/practice/schema.test.ts src/__tests__/practice/WrittenCard.test.tsx src/__tests__/api/check-writing.test.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>schema.test.ts 'written' cases run (they will PASS once types.ts is updated in Plan 02; they should currently FAIL with "not in union" or schema error). WrittenCard.test.tsx and check-writing.test.ts exist and are collected by Jest, failing with import/module errors (not syntax errors). No test file has a syntax error that prevents collection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer filesystem → Supabase remote | Migration SQL pushed via `supabase db push` — DDL executes with service_role on remote |
| .env.local → Next.js server runtime | ANTHROPIC_API_KEY must never leak to client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | ANTHROPIC_API_KEY in .env.local | mitigate | Key slot added server-only; no NEXT_PUBLIC_ prefix; CLAUDE.md Security Rules enforced in task action |
| T-06-02 | Tampering | writing_submissions RLS missing | mitigate | RLS enabled + select/insert policies scoped to auth.uid() in migration SQL |
| T-06-03 | Elevation of Privilege | UPDATE/DELETE not restricted on writing_submissions | accept | No UPDATE policy written → rows immutable; DELETE handled via cascade on user delete |
| T-06-SC | Tampering | npm install @anthropic-ai/sdk | mitigate | Package legitimacy audit in RESEARCH.md confirms official Anthropic maintainers; checkpoint:human-verify in Plan 02 before install |
</threat_model>

<verification>
Run after all three tasks:
- `grep -c "ANTHROPIC_API_KEY=" .env.local` → 1
- `ls supabase/migrations/20260624_phase6_writing.sql` → file exists
- `npx jest src/__tests__/practice/ --no-coverage 2>&1 | grep -E "PASS|FAIL|Tests:"` → schema.test.ts PASS (existing tests green); WrittenCard.test.tsx and check-writing.test.ts FAIL with import errors (expected RED state, not syntax errors)
</verification>

<success_criteria>
- Migration SQL file is complete and valid — can be pushed to Supabase without modification
- .env.local has ANTHROPIC_API_KEY= slot with comment; no actual key value present; no NEXT_PUBLIC_ variant created
- Three test scaffolds exist and are collected by Jest; they fail on import resolution (WrittenCard, route), not on syntax
- schema.test.ts existing 145-test suite is unchanged and still green
</success_criteria>

<output>
Create .planning/phases/06-ai-writing-checker/06-01-SUMMARY.md when done
</output>
