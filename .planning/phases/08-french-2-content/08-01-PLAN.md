---
id: 08-01
wave: 1
phase: 8
title: French 2 Content — All 10 Lessons SQL Migration
objective: Author and apply SQL migration seeding all 10 French 2 lessons with real grammar content (4 sub-components each), then UAT verify
depends_on: []
files_modified:
  - supabase/migrations/20260628_phase8_french2_content.sql
autonomous: true
requirements:
  - CONTENT-02
  - CONTENT-03
must_haves:
  goal: French 2 has 10 lessons seeded with real grammar content, completable end-to-end
  truths:
    - lessons table has 10 rows for level_id = 97600976-de70-45af-ab50-4aedd2852f3a
    - sub_components table has 40 rows tied to those lessons
    - all 4 problem types (mc, fill-in, conjugation-table/conjugation-single, matching) appear across the 10 lessons
    - every lesson has a writing sub-component at position 4 with a hints field
    - migration is idempotent — re-running produces no errors and no duplicate rows
  artifacts:
    - path: supabase/migrations/20260628_phase8_french2_content.sql
      provides: Complete French 2 content seed — 10 lessons × 4 sub-components
      contains: DO $body$ block
---

<objective>
Author the complete SQL migration for all 10 French 2 lessons, run it manually in the Supabase SQL Editor, and verify counts.

Purpose: French 2 is currently an empty level. This migration makes it completable end-to-end with real grammar content gated behind the French 1 diagnostic.
Output: supabase/migrations/20260628_phase8_french2_content.sql applied to the remote DB; 10 lessons + 40 sub-components visible in the app.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@c:\Users\Ericc\frenchly\.planning\PROJECT.md
@c:\Users\Ericc\frenchly\.planning\ROADMAP.md
@c:\Users\Ericc\frenchly\.planning\STATE.md

<!-- Primary template — replicate dollar-quoting, DO $body$ block, ON CONFLICT pattern, and IF lesson_id IS NOT NULL guard exactly -->
@c:\Users\Ericc\frenchly\supabase\migrations\20260625_phase7_french1_content.sql

<!-- Type contracts — all content JSON must satisfy these discriminated union shapes -->
@c:\Users\Ericc\frenchly\src\lib\practice\types.ts
@c:\Users\Ericc\frenchly\src\lib\practice\schema.ts

<!-- Phase context — all locked decisions (D-01 through D-04) -->
@c:\Users\Ericc\frenchly\.planning\phases\08-french-2-content\08-CONTEXT.md

<interfaces>
<!-- Content JSON shapes required by parseProblemContent() at render time. Every content field must be valid JSON matching one of these exactly. -->

MCProblem         — { "type": "mc", "prompt": "...", "options": ["...", "...", ...], "correctAnswer": "..." }
                    options: 2–4 strings; correctAnswer must be one of the options strings

FillInProblem     — { "type": "fill-in", "prompt": "...", "correctAnswer": "..." }

ConjugationTable  — { "type": "conjugation-table", "prompt": "...", "verb": "...", "answers": { "je": "...", "tu": "...", "il": "...", "nous": "...", "vous": "...", "ils": "..." } }
                    all six pronoun keys required

ConjugationSingle — { "type": "conjugation-single", "prompt": "...", "correctAnswer": "..." }

MatchingProblem   — { "type": "matching", "prompt": "...", "pairs": [{ "left": "...", "right": "..." }, ...] }
                    pairs: 2–6 objects

WrittenProblem    — { "type": "written", "prompt": "...", "hints": "line1\nline2\nline3" }
                    hints is a single string with \n separating phrases; rendered whitespace-pre-line
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the SQL migration file</name>
  <files>supabase/migrations/20260628_phase8_french2_content.sql</files>

  <read_first>
  - supabase/migrations/20260625_phase7_french1_content.sql — copy the DO $body$ DECLARE…BEGIN…END; $body$; wrapper verbatim; use same dollar-quoting ($body$, $json$, $txt$); use same ON CONFLICT (level_id, slug) DO NOTHING RETURNING id INTO v_lessonN_id; use same IF v_lessonN_id IS NOT NULL THEN guard for sub-component INSERTs
  - src/lib/practice/types.ts — discriminated union shapes; type field is the discriminant
  - src/lib/practice/schema.ts — Zod validation; options 2–4 for mc; pairs 2–6 for matching; all conjugation-table answers keys required
  - .planning/phases/08-french-2-content/08-CONTEXT.md — D-01 (slugs + sequence), D-02 (problem type per lesson), D-03 (migration file name, idempotency strategy), D-04 (SQL structure)
  </read_first>

  <action>
  Write supabase/migrations/20260628_phase8_french2_content.sql containing the full French 2 content seed. Follow the Phase 7 migration structure exactly — same dollar-quoting strategy, same variable declaration block, same guard pattern, same sub-component INSERT shape.

  **File header comment** (mirror Phase 7 style): describe what the file does, idempotency guarantee, dollar-quoting strategy.

  **DECLARE block:** v_french2_id uuid; v_lesson1_id through v_lesson10_id uuid; (11 variables total)

  **Guard (per D-04):**
  SELECT id INTO v_french2_id FROM public.levels WHERE slug = 'french-2';
  IF v_french2_id IS NULL THEN RAISE EXCEPTION 'French 2 level not found — run Phase 3 migration first.'; END IF;

  **Per-lesson pattern (repeat × 10):**
  1. INSERT INTO public.lessons (level_id, slug, title, estimated_minutes, position) VALUES (v_french2_id, '...', '...', 12, N) ON CONFLICT (level_id, slug) DO NOTHING RETURNING id INTO v_lessonN_id;
  2. IF v_lessonN_id IS NOT NULL THEN … END IF;
  3. Inside the IF: 4 INSERT INTO public.sub_components statements — pos 1 explainer ($txt$), pos 2 practice, pos 3 practice, pos 4 writing ($json$)

  **Sub-component INSERT columns:**
  - Explainer (kind='explainer', no problem_type): (lesson_id, title, kind, content, position)
  - Practice (kind='practice', problem_type required): (lesson_id, title, kind, problem_type, content, position)
  - Writing (kind='writing', no problem_type): (lesson_id, title, kind, content, position)

  **10 lessons — slugs, positions, and problem types (per D-01 and D-02):**

  Lesson 1 pos=1 slug='er-verbs' title='Regular -ER verbs'
    pos 2: conjugation-table — conjugate parler (parle, parles, parle, parlons, parlez, parlent)
    pos 3: fill-in — blank sentence using parler/manger/étudier
    pos 4: writing — 3 hint phrases about daily ER-verb actions

  Lesson 2 pos=2 slug='ir-verbs' title='Regular -IR verbs'
    pos 2: conjugation-table — conjugate finir (finis, finis, finit, finissons, finissez, finissent)
    pos 3: fill-in — blank sentence using finir/choisir/réussir
    pos 4: writing — 3 hint phrases about choosing/finishing activities

  Lesson 3 pos=3 slug='re-verbs' title='Regular -RE verbs'
    pos 2: conjugation-table — conjugate vendre (vends, vends, vend, vendons, vendez, vendent)
    pos 3: fill-in — blank sentence using vendre/attendre/répondre
    pos 4: writing — 3 hint phrases about selling/waiting/responding

  Lesson 4 pos=4 slug='negation' title='Negation: ne...pas'
    pos 2: mc — identify correct negation placement (4 options)
    pos 3: fill-in — supply ne...pas around a verb in a sentence
    pos 4: writing — 3 hint phrases using ne...pas, ne...jamais, ne...plus

  Lesson 5 pos=5 slug='questions' title='Question formation'
    pos 2: mc — identify the question type used in a sample sentence (est-ce que / inversion / intonation)
    pos 3: fill-in — form a yes/no question using est-ce que with a given statement
    pos 4: writing — 3 hint phrases demonstrating the three question strategies

  Lesson 6 pos=6 slug='futur-proche' title='Futur proche: aller + infinitive'
    pos 2: conjugation-single — fill one aller form in a futur proche sentence
    pos 3: fill-in — complete a futur proche sentence (aller + infinitive)
    pos 4: writing — 3 hint phrases about weekend plans using futur proche

  Lesson 7 pos=7 slug='passe-compose-avoir' title='Passé composé with avoir'
    pos 2: conjugation-table — fill passé composé forms for manger (ai mangé, as mangé, a mangé, avons mangé, avez mangé, ont mangé) — use "je/tu/il/nous/vous/ils" keys, answers are full "avoir + participle" strings
    pos 3: fill-in — supply the past participle of a regular -ER/-IR verb in a sentence
    pos 4: writing — 3 hint phrases describing what you did yesterday

  Lesson 8 pos=8 slug='passe-compose-etre' title='Passé composé with être'
    pos 2: matching — match VANDERTRAMP infinitives to their past participles (4–5 pairs; use common ones: aller→allé, venir→venu, partir→parti, arriver→arrivé)
    pos 3: conjugation-single — supply the correct être form + participle for one subject
    pos 4: writing — 3 hint phrases about a trip or movement using être verbs

  Lesson 9 pos=9 slug='object-pronouns' title='Direct object pronouns'
    pos 2: mc — choose the correct pronoun (le/la/les/l') to replace a noun object (4 options)
    pos 3: fill-in — rewrite a sentence replacing the underlined noun with the correct pronoun
    pos 4: writing — 3 hint phrases using le/la/les/l' before a verb

  Lesson 10 pos=10 slug='adjective-placement' title='Adjective placement: BAGS rule'
    pos 2: fill-in — place a given adjective correctly (before or after noun) in a sentence
    pos 3: matching — match BAGS adjectives to their category (Beauty/Age/Goodness/Size; 4 pairs)
    pos 4: writing — 3 hint phrases using pre-nominal BAGS adjectives and post-nominal color/shape adjectives

  **Content quality rules:**
  - Explainer markdown uses $txt$ quoting; use real French terms and a reference table per lesson
  - No bare single-quoted strings for any content containing apostrophes, accents, or special chars — use $json$ or $txt$ exclusively
  - All JSON keys camelCase; no trailing commas; valid JSON parseable by JSON.parse()
  - correctAnswer in mc must exactly match one of the options strings (case-sensitive)
  - conjugation-table answers object must include all six keys: je, tu, il, nous, vous, ils
  - matching pairs array: 2–6 objects; left and right are non-empty strings
  - writing hints: exactly 3 phrases joined by \n (literal backslash-n in the JSON string)
  - All French text uses proper accents (é, è, ê, à, â, ô, û, ç, î, ï, œ) — do not romanize
  - Sentence case for all titles and prompts (per CLAUDE.md)
  </action>

  <verify>
  After writing the file, confirm:
  - File exists at supabase/migrations/20260628_phase8_french2_content.sql
  - File contains exactly one DO $body$ ... $body$; block
  - File contains the French 2 guard (slug = 'french-2')
  - 10 lesson INSERT statements are present (grep for ON CONFLICT (level_id, slug) DO NOTHING)
  - 40 sub-component INSERT statements are present (grep for INSERT INTO public.sub_components)
  - No bare single-quoted strings contain apostrophes or accent characters (visual scan of the file)
  - All $json$ blocks contain valid JSON (mentally parse each; check that "type" field is present in every content block and matches the problem_type column value)
  </verify>

  <acceptance_criteria>
  - File exists at supabase/migrations/20260628_phase8_french2_content.sql
  - Single DO $body$ block wraps the entire migration
  - Guard raises exception if french-2 level is missing
  - Exactly 10 lesson INSERT blocks each with ON CONFLICT (level_id, slug) DO NOTHING RETURNING id INTO v_lessonN_id
  - Exactly 40 sub-component INSERT statements (4 per lesson, inside IF v_lessonN_id IS NOT NULL THEN guards)
  - pos 1 of every lesson is kind='explainer' with $txt$ markdown content
  - pos 4 of every lesson is kind='writing' with a written-type JSON block containing a hints field with 3 newline-separated phrases
  - All four auto-graded problem types appear: mc (lessons 4, 5, 9), fill-in (lessons 1–9), conjugation-table (lessons 1, 2, 3, 7), conjugation-single (lessons 6, 8), matching (lessons 8, 10)
  - No content JSON block is missing the "type" field
  - conjugation-table blocks include all six pronoun keys (je, tu, il, nous, vous, ils)
  - mc blocks have 2–4 options and correctAnswer matches one option exactly
  - matching blocks have 2–6 pairs
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Run migration in Supabase SQL Editor</name>

  <read_first>
  - supabase/migrations/20260628_phase8_french2_content.sql — the file authored in Task 1
  </read_first>

  <action>
  Open the Supabase dashboard for this project, navigate to SQL Editor, paste the entire contents of supabase/migrations/20260628_phase8_french2_content.sql, and click Run. The SQL Editor will report "Success. No rows returned." if the migration succeeds.

  If the editor reports an error: read the error message, identify which INSERT or block failed, fix the SQL in the local file, and re-run. Because the migration is idempotent (ON CONFLICT DO NOTHING + RETURNING guard), re-running the corrected file is safe.
  </action>

  <how-to-verify>
  After clicking Run:
  1. The Results panel shows "Success. No rows returned." with no red error banner.
  2. No EXCEPTION or ERROR text appears in the output.
  </how-to-verify>

  <resume-signal>Type "migrated" after the SQL Editor reports success, or paste the error message if it fails.</resume-signal>

  <acceptance_criteria>
  - Supabase SQL Editor reports "Success. No rows returned." with no errors
  - No RAISE EXCEPTION was triggered (which would surface as a DB error)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: UAT verification — count lessons and sub-components</name>

  <read_first>
  - No files needed — run the queries below in Supabase SQL Editor (or use the MCP Supabase tool if available)
  </read_first>

  <action>
  Run the following verification queries in the Supabase SQL Editor (or via MCP Supabase execute_sql):

  Query A — lesson count:
    SELECT COUNT(*) AS lesson_count
    FROM public.lessons
    WHERE level_id = '97600976-de70-45af-ab50-4aedd2852f3a';
  Expected: 10

  Query B — sub-component count:
    SELECT COUNT(*) AS sub_component_count
    FROM public.sub_components sc
    JOIN public.lessons l ON sc.lesson_id = l.id
    WHERE l.level_id = '97600976-de70-45af-ab50-4aedd2852f3a';
  Expected: 40

  Query C — problem type distribution:
    SELECT sc.problem_type, COUNT(*) AS cnt
    FROM public.sub_components sc
    JOIN public.lessons l ON sc.lesson_id = l.id
    WHERE l.level_id = '97600976-de70-45af-ab50-4aedd2852f3a'
      AND sc.kind = 'practice'
    GROUP BY sc.problem_type
    ORDER BY sc.problem_type;
  Expected: mc, fill-in, conjugation-table, conjugation-single, matching all appear with cnt >= 1

  Query D — writing prompts have hints:
    SELECT COUNT(*) AS writing_with_hints
    FROM public.sub_components sc
    JOIN public.lessons l ON sc.lesson_id = l.id
    WHERE l.level_id = '97600976-de70-45af-ab50-4aedd2852f3a'
      AND sc.kind = 'writing'
      AND sc.content::text LIKE '%hints%';
  Expected: 10

  Optionally navigate to /levels/french-2 in the running dev app to confirm all 10 lesson cards appear and are clickable.
  </action>

  <verify>
    <automated>All four SQL queries return the expected values listed above.</automated>
  </verify>

  <acceptance_criteria>
  - lesson_count = 10
  - sub_component_count = 40
  - Query C shows mc, fill-in, conjugation-table, conjugation-single, and matching each with at least 1 row
  - writing_with_hints = 10 (every writing sub-component has a hints field in its content JSON)
  - French 2 level page (/levels/french-2) shows all 10 lesson cards with no blank or broken entries
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor → Supabase DB | Migration runs with project owner credentials in a trusted admin context; no user-submitted data crosses this boundary |
| DB → app renderer | sub_components.content is parsed by parseProblemContent() via Zod; malformed JSON silently returns null and shows "not available yet" — no XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | SQL migration file | accept | Migration runs via trusted Supabase SQL Editor with owner auth; no untrusted input path |
| T-08-02 | Information Disclosure | content JSON in DB | accept | All French grammar content is public educational material; no PII or secrets stored |
| T-08-03 | Denial of Service | malformed content JSON | mitigate | parseProblemContent() wraps JSON.parse + Zod.parse in try/catch; returns null; UI shows graceful fallback — content validated before merge |
| T-08-SC | Tampering | npm/pip installs | accept | This plan contains zero package manager install steps; no supply-chain risk |
</threat_model>

<verification>
Phase 8 is complete when:
- supabase/migrations/20260628_phase8_french2_content.sql exists in the repo
- Supabase DB has 10 lessons for level_id = 97600976-de70-45af-ab50-4aedd2852f3a
- Supabase DB has 40 sub-components tied to those lessons
- All five auto-graded problem types are represented (mc, fill-in, conjugation-table, conjugation-single, matching)
- Every writing sub-component carries a hints field
- /levels/french-2 shows all 10 lesson cards with real content
- French 2 is completable end-to-end (no dead-end lessons, no null content)
</verification>

<success_criteria>
- 10 rows in public.lessons with level_id = 97600976-de70-45af-ab50-4aedd2852f3a (slugs er-verbs through adjective-placement)
- 40 rows in public.sub_components joined to those lessons
- All four auto-graded problem type families present across the lesson set
- Migration file is idempotent — re-running produces "Success. No rows returned." without duplicating data
- French 2 level is navigable and completable by a student who has passed the French 1 diagnostic
</success_criteria>

<output>
Create `.planning/phases/08-french-2-content/08-01-SUMMARY.md` when done.
</output>
