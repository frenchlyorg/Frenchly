---
phase: 04-diagnostic-system
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/lib/lessons/locking.ts
  - __tests__/lessons/level.test.ts
  - src/components/diagnostic/DiagnosticGate.tsx
  - src/app/dashboard/page.tsx
  - src/app/levels/[levelSlug]/page.tsx
autonomous: true
requirements: [DIAG-01, DIAG-03]
must_haves:
  truths:
    - "A level is locked when its level_number exceeds the student's unlocked_through_level_number watermark"
    - "deriveIsLevelLocked with a null watermark falls back to the Phase 3 UUID behavior gracefully"
    - "The watermark model has no hard-coded 2-level ceiling — it extends to future levels"
    - "A first-time student with no completed placement is shown the DiagnosticGate before dashboard/level content"
    - "After completing placement, the dashboard and level pages render normal content (gate gone)"
    - "A student placed at French 1 sees French 2 locked; a student at French 2 sees French 1 still accessible"
  artifacts:
    - path: "src/lib/lessons/locking.ts"
      provides: "Watermark-generalized deriveIsLevelLocked (signature stable, adds unlockedThroughLevelNumber)"
      contains: "unlockedThroughLevelNumber"
    - path: "src/components/diagnostic/DiagnosticGate.tsx"
      provides: "Placement gate interstitial (guillemet heading, single mandatory CTA)"
      min_lines: 20
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard with placement gate guard + watermark fetch"
      contains: "DiagnosticGate"
  key_links:
    - from: "src/app/levels/[levelSlug]/page.tsx"
      to: "src/lib/lessons/locking.ts"
      via: "deriveIsLevelLocked with unlockedThroughLevelNumber from profile"
      pattern: "unlockedThroughLevelNumber"
    - from: "src/app/dashboard/page.tsx"
      to: "diagnostic_attempts"
      via: "placement completion query → DiagnosticGate when none"
      pattern: "diagnostic_type.*placement"
---

<objective>
Lock model + placement gate slice (DIAG-03, and the gate half of DIAG-01) — generalize `deriveIsLevelLocked` from the Phase 3 UUID-equality check to the numeric `unlocked_through_level_number` watermark, then enforce the forced-placement gate on the dashboard and level pages for first-time students. After this slice, locked levels render correctly from the watermark and a new student cannot reach lesson content without completing placement first.

Purpose: D-S02 requires a watermark model that extends to future levels (no 2-level ceiling). D-P01 requires placement to be forced before any lesson access. This slice is the consumer side of the watermark that Plan 03's action writes — together they close DIAG-01 and DIAG-03. Runs in parallel with Plan 03 (no shared files).
Output: watermark-generalized `locking.ts` + extended tests, the `DiagnosticGate` component, and gate guards added to dashboard + level pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostic-system/04-CONTEXT.md
@.planning/phases/04-diagnostic-system/04-RESEARCH.md
@.planning/phases/04-diagnostic-system/04-PATTERNS.md
@.planning/phases/04-diagnostic-system/04-UI-SPEC.md
@DESIGN.md
@CLAUDE.md

@src/lib/lessons/locking.ts
@src/app/dashboard/page.tsx
@src/app/levels/[levelSlug]/page.tsx

<interfaces>
<!-- Current locking signature + caller + test, plus gate UI tokens. -->

Current src/lib/lessons/locking.ts:
```typescript
export function deriveIsLevelLocked(args: {
  levelId: string
  levelNumber: number               // reserved in Phase 3, used in Phase 4
  currentLevelId: string | null | undefined
}): boolean
```
Phase 4 extension (keep signature additive — add optional unlockedThroughLevelNumber):
- if unlockedThroughLevelNumber != null → return levelNumber > unlockedThroughLevelNumber
- else fallback: if currentLevelId == null return false; else return levelId !== currentLevelId

Current __tests__/lessons/level.test.ts: three cases on deriveIsLevelLocked (UUID match/mismatch/null). Add watermark cases without removing the fallback cases.

Current dashboard guard (src/app/dashboard/page.tsx): getUser() → redirect('/login?next=/dashboard'); then `.from('profiles').select('username').eq('id', user.id).single()`.

Current level page (src/app/levels/[levelSlug]/page.tsx) lines 80-93: `.select('current_level_id')` then deriveIsLevelLocked({ levelId, levelNumber, currentLevelId }).

DiagnosticGate tokens (04-UI-SPEC §DiagnosticGate, LevelCard analog):
- centered card bg-surface-container-low max-w-[480px] on bg-surface page
- heading font-heading text-[28px] font-semibold with « » guillemets in text-primary aria-hidden
- copy: heading "Before you begin"; body "Answer 10 short questions so we can find the right starting point for you. There are no wrong answers — this helps us help you."; CTA "Start placement diagnostic" linking to /diagnostic/placement
- NO dismiss/skip affordance (mandatory gate); resume variant copy when hasInProgress: "You have an unfinished diagnostic. Pick up where you left off." + CTA "Continue diagnostic"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize deriveIsLevelLocked to the watermark + extend tests (RED→GREEN)</name>
  <files>src/lib/lessons/locking.ts, __tests__/lessons/level.test.ts</files>
  <read_first>
    - src/lib/lessons/locking.ts (current implementation + the Phase 4 note in its doc comment)
    - __tests__/lessons/level.test.ts (existing three cases — extend, do not delete)
    - .planning/phases/04-diagnostic-system/04-RESEARCH.md (§Pattern 1 watermark generalization, §A3 null semantics)
    - .planning/phases/04-diagnostic-system/04-PATTERNS.md (§src/lib/lessons/locking.ts MODIFY)
    - .planning/phases/04-diagnostic-system/04-CONTEXT.md (D-S02, D-P04)
  </read_first>
  <behavior>
    - deriveIsLevelLocked: watermark=1, levelNumber=2 → true (French 2 locked when placed at French 1)
    - deriveIsLevelLocked: watermark=2, levelNumber=1 → false (French 1 stays accessible when placed at French 2, D-P04)
    - deriveIsLevelLocked: watermark=2, levelNumber=2 → false (current level unlocked)
    - deriveIsLevelLocked: watermark=2, levelNumber=3 → true (no hard-coded ceiling — future level locked)
    - deriveIsLevelLocked: watermark null + currentLevelId match → false (Phase 3 fallback preserved)
    - deriveIsLevelLocked: watermark null + currentLevelId mismatch → true (Phase 3 fallback preserved)
    - deriveIsLevelLocked: watermark null + currentLevelId null → false (new student fallback preserved)
  </behavior>
  <action>
    First extend `__tests__/lessons/level.test.ts`: add a new describe block "deriveIsLevelLocked — watermark (DIAG-03)" with the seven cases above, calling deriveIsLevelLocked with the new optional `unlockedThroughLevelNumber` arg. Keep all three existing Phase 3 cases. Then modify `src/lib/lessons/locking.ts`: add an optional `unlockedThroughLevelNumber?: number | null` to the args object; remove the eslint-disable on levelNumber (it is now used); update the doc comment to describe the watermark rule. Implementation: if `args.unlockedThroughLevelNumber != null` return `args.levelNumber > args.unlockedThroughLevelNumber`; otherwise keep the existing fallback (`currentLevelId == null` → false; else `levelId !== currentLevelId`). No hard-coded level-number branches (D-S02). Keep the function pure (no imports, no DB).
  </action>
  <verify>
    <automated>npx jest --testPathPattern="lessons/level"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `npx jest --testPathPattern="lessons/level"` passes all watermark cases AND the three preserved Phase 3 fallback cases
    - SOURCE: deriveIsLevelLocked signature adds optional `unlockedThroughLevelNumber?: number | null`; remains a pure function (no imports)
    - SOURCE: no hard-coded numeric ceiling (no `=== 2` / `levelNumber === N` branches); rule is `levelNumber > unlockedThroughLevelNumber`
    - VALIDATION: matches 04-VALIDATION.md seams "numeric watermark: L2 locked when watermark=1" and "null watermark falls back gracefully"
  </acceptance_criteria>
  <done>deriveIsLevelLocked uses the numeric watermark with graceful null fallback; tests cover watermark + fallback; no level ceiling hard-coded.</done>
</task>

<task type="auto">
  <name>Task 2: DiagnosticGate component + placement gate guards on dashboard and level pages</name>
  <files>src/components/diagnostic/DiagnosticGate.tsx, src/app/dashboard/page.tsx, src/app/levels/[levelSlug]/page.tsx</files>
  <read_first>
    - src/app/dashboard/page.tsx (current getUser guard + profile fetch)
    - src/app/levels/[levelSlug]/page.tsx (current profile fetch + deriveIsLevelLocked call)
    - src/components/lessons/LevelCard.tsx (card + guillemet heading tokens)
    - src/lib/lessons/locking.ts (the watermark-generalized function from Task 1)
    - .planning/phases/04-diagnostic-system/04-UI-SPEC.md (§DiagnosticGate, §Interaction Contracts — placement gate flow, §Copywriting)
    - .planning/phases/04-diagnostic-system/04-PATTERNS.md (§DiagnosticGate, §dashboard MODIFY, §level page MODIFY, §Pitfall 4)
  </read_first>
  <action>
    Create `src/components/diagnostic/DiagnosticGate.tsx` (presentational, no 'use client'): props `{ hasInProgress: boolean }`; centered card bg-surface-container-low max-w-[480px] on a bg-surface page (content max-width / lesson width); guillemet-framed heading font-heading text-[28px] font-semibold ("Before you begin" with « » in text-primary aria-hidden); body paragraph font-body text-[16px] text-on-surface-variant; single primary CTA (bg-primary text-white rounded-[8px] px-6 py-3 font-label font-semibold) — when hasInProgress use resume copy ("You have an unfinished diagnostic. Pick up where you left off." / "Continue diagnostic") else fresh copy ("Answer 10 short questions..." / "Start placement diagnostic"); CTA links to /diagnostic/placement; NO skip/dismiss link (D-P01/D-P02). All copy sentence case from UI-SPEC. Then modify `src/app/dashboard/page.tsx`: after the getUser guard, query diagnostic_attempts for a completed placement (`select('id, status').eq('user_id', user.id).eq('diagnostic_type','placement').eq('status','completed').maybeSingle()`); if none, query for an in_progress placement, then `return <DiagnosticGate hasInProgress={!!inProgress} />` (gate before normal content — Pitfall 4: this is a Server Component guard, NOT middleware). Extend the profile select to include `unlocked_through_level_number` for future dashboard use. Then modify `src/app/levels/[levelSlug]/page.tsx`: add the same completed-placement check after the getUser guard → render DiagnosticGate when no completed placement; extend the profile select to `current_level_id, unlocked_through_level_number`; pass `unlockedThroughLevelNumber: profile?.unlocked_through_level_number ?? null` into the existing deriveIsLevelLocked call. Do not rewrite the existing lock UI (LevelCard/LockBadge) — it is driven by the derived isLocked.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "dashboard/page|levels/\[levelSlug\]/page|DiagnosticGate" | head; echo "---"; grep -n "DiagnosticGate" src/app/dashboard/page.tsx src/app/levels/[levelSlug]/page.tsx && grep -n "unlockedThroughLevelNumber" src/app/levels/[levelSlug]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: DiagnosticGate.tsx exists, presentational (no 'use client'), guillemet heading, single mandatory CTA, no skip/dismiss link
    - SOURCE: dashboard and level pages query diagnostic_attempts for a completed placement and render DiagnosticGate when none exists (Server Component guard, not middleware — Pitfall 4)
    - SOURCE: level page passes `unlockedThroughLevelNumber` from profile into deriveIsLevelLocked; profile selects include `unlocked_through_level_number`
    - SOURCE: existing LevelCard/LockBadge lock UI is unchanged (driven by derived isLocked)
    - BEHAVIOR: `npx tsc --noEmit` reports no type errors for the gate component or the two modified pages
    - VALIDATION (manual, per 04-VALIDATION.md): fresh account visiting /dashboard and /levels/french-1 is shown the gate / routed to placement; after placement, normal content renders
  </acceptance_criteria>
  <done>DiagnosticGate exists; dashboard + level pages gate first-time users via a Server Component check and drive lock UI from the watermark; compiles cleanly with no lock-UI rewrite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Component (dashboard/level) | First-time user attempts to reach lesson content; gate must intercept based on a DB read of placement state. |
| watermark value → lock derivation | The displayed lock state must derive from the server-read watermark, never a client-supplied value. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-EoP-04 | Elevation of Privilege | bypassing placement to reach lessons | mitigate | Server Component gate on dashboard + level pages reads diagnostic_attempts (RLS-scoped) and renders DiagnosticGate when no completed placement. Gate is server-side (Pitfall 4 — not middleware, which cannot do the RLS read). |
| T-04-Tamp-05 | Tampering | client manipulates displayed lock state | mitigate | deriveIsLevelLocked runs in the Server Component from the server-read watermark; lock state is never trusted from the client. |
| T-04-Info-03 | Information Disclosure | locked-level content rendered to unauthorized student | mitigate | isLocked derived before render; LevelCard/LockBadge present locked UI; lesson content access remains gated by watermark + placement gate. |
| T-04-SC | Tampering | npm installs | accept | No new packages (04-RESEARCH.md Package Legitimacy Audit: none). |
</threat_model>

<verification>
- `npx jest --testPathPattern="lessons/level"` — watermark + fallback cases green
- `npx tsc --noEmit` — no type errors in gate + modified pages
- `npx jest` — full suite green (no regressions)
- grep confirms DiagnosticGate wired into both pages and watermark passed to deriveIsLevelLocked
</verification>

<success_criteria>
- deriveIsLevelLocked uses the numeric watermark with graceful null fallback and no level ceiling (DIAG-03, D-S02)
- First-time students are gated to placement before dashboard/level content (DIAG-01 gate half, D-P01)
- French 1 placement → French 2 locked; French 2 placement → French 1 still accessible (D-P04)
- Existing lock UI is reused unchanged, driven by the watermark
</success_criteria>

<output>
Create `.planning/phases/04-diagnostic-system/04-04-SUMMARY.md` when done
</output>
