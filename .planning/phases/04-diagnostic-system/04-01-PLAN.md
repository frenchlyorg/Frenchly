---
phase: 04-diagnostic-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/diagnostics/types.ts
  - src/lib/diagnostics/scoring.ts
  - src/lib/diagnostics/gating.ts
  - __tests__/diagnostic/scoring.test.ts
  - __tests__/diagnostic/gating.test.ts
autonomous: true
requirements: [DIAG-01, DIAG-02]
must_haves:
  truths:
    - "Fill-in answers grade leniently: café == cafe == Cafe all count correct"
    - "A correct fill-in with accent difference surfaces a soft accent note, still scored correct"
    - "Blank fill-in submissions always grade incorrect (no leniency)"
    - "Placement maps ≥80% to French 2, below to French 1"
    - "End-of-level pass/fail threshold is exactly 0.8"
    - "Cooldown computes completedAt + 3h and formats remaining as '2h 14m' / '45m'"
    - "drawQuestions returns exactly N items with no duplicates"
  artifacts:
    - path: "src/lib/diagnostics/types.ts"
      provides: "Diagnostic domain types (QuestionType, AttemptStatus, DiagnosticType, DiagnosticQuestion, DiagnosticAttempt, GradeResult)"
      contains: "export interface DiagnosticQuestion"
    - path: "src/lib/diagnostics/scoring.ts"
      provides: "Pure scoring functions: normalizeFillin, gradeAnswer, computeScore, derivePlacement, derivePassFail, drawQuestions"
      exports: ["normalizeFillin", "gradeAnswer", "computeScore", "derivePlacement", "derivePassFail", "drawQuestions"]
    - path: "src/lib/diagnostics/gating.ts"
      provides: "Pure gating functions: computeCooldownUntil, isCooldownActive, formatCooldownRemaining, deriveAllLessonsComplete"
      exports: ["computeCooldownUntil", "isCooldownActive", "formatCooldownRemaining", "deriveAllLessonsComplete"]
    - path: "__tests__/diagnostic/scoring.test.ts"
      provides: "Unit tests for all scoring pure functions"
      contains: "describe('gradeAnswer"
    - path: "__tests__/diagnostic/gating.test.ts"
      provides: "Unit tests for all gating pure functions"
      contains: "describe('computeCooldownUntil"
  key_links:
    - from: "src/lib/diagnostics/scoring.ts"
      to: "src/lib/diagnostics/types.ts"
      via: "import type { DiagnosticQuestion, GradeResult }"
      pattern: "import type.*from './types'"
---

<objective>
Foundation slice — establish the diagnostic domain types and the pure, testable grading/gating functions that every later slice depends on. This is the interface-first + Wave 0 layer: types and pure functions are written test-first so the placement, watermark, and end-of-level slices build against locked contracts.

Purpose: All scoring, placement, pass/fail, and cooldown logic must be pure (no DB, no imports beyond local types) so it can be unit-tested without Supabase and reused identically by the placement Server Action (Plan 03) and the end-of-level Server Action (Plan 05). These are the primary Nyquist test seams for the phase.
Output: `src/lib/diagnostics/{types,scoring,gating}.ts` + Wave 0 Jest tests covering every seam in 04-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-diagnostic-system/04-CONTEXT.md
@.planning/phases/04-diagnostic-system/04-RESEARCH.md
@.planning/phases/04-diagnostic-system/04-PATTERNS.md
@.planning/phases/04-diagnostic-system/04-VALIDATION.md

<interfaces>
<!-- Pure-function analog the executor must mirror. Already in codebase. -->

From src/lib/lessons/locking.ts (the pure-function + file-comment pattern to follow):
```typescript
/**
 * LESSON-04: Level lock derivation helper.
 * Pure function — no imports, no side effects, no DB access.
 * Safe to import in tests without a database.
 */
export function deriveIsLevelLocked(args: {
  levelId: string
  levelNumber: number
  currentLevelId: string | null | undefined
}): boolean { ... }
```

From __tests__/lessons/level.test.ts (the pure-fn test pattern — no mocks, no Supabase):
```typescript
import { deriveIsLevelLocked } from '@/lib/lessons/locking'
describe('isLevelLocked (LESSON-04)', () => {
  test('returns true when current_level_id does not match level id', () => {
    const result = deriveIsLevelLocked({ levelId: '...', levelNumber: 2, currentLevelId: '...' })
    expect(result).toBe(true)
  })
})
```

Target signatures (define these exactly — Plans 03 and 05 import them):
- normalizeFillin(raw: string): string
- gradeAnswer(question: DiagnosticQuestion, submitted: string): GradeResult
- computeScore(results: GradeResult[]): number
- derivePlacement(score: number): 1 | 2
- derivePassFail(score: number): 'pass' | 'fail'
- drawQuestions<T>(pool: T[], count: number): T[]
- computeCooldownUntil(completedAt: Date, cooldownHours: number): Date
- isCooldownActive(cooldownUntil: Date | null, now?: Date): boolean
- formatCooldownRemaining(cooldownUntil: Date, now?: Date): string
- deriveAllLessonsComplete(subComponentIds: string[], completedSubComponentIds: Set<string>): boolean
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Diagnostic domain types + Wave 0 test scaffolds (RED)</name>
  <files>src/lib/diagnostics/types.ts, __tests__/diagnostic/scoring.test.ts, __tests__/diagnostic/gating.test.ts</files>
  <read_first>
    - src/lib/lessons/locking.ts (pure-fn + file-comment pattern)
    - __tests__/lessons/level.test.ts (pure-fn test pattern, no mocks)
    - .planning/phases/04-diagnostic-system/04-PATTERNS.md (§types.ts, §scoring.test.ts, §gating.test.ts)
    - .planning/phases/04-diagnostic-system/04-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map)
  </read_first>
  <behavior>
    - scoring.test.ts asserts: normalizeFillin strips accents (café→cafe), lowercases, trims, returns '' for blank
    - scoring.test.ts asserts: gradeAnswer fill_in — café==cafe==Cafe all correct (D-D03); blank always incorrect (Pitfall 6); accentNote set when normalized match but raw differs; exact match → no accentNote
    - scoring.test.ts asserts: gradeAnswer mc — correct when submitted===correct_answer, incorrect otherwise
    - scoring.test.ts asserts: computeScore returns correct/total ratio; empty array → 0
    - scoring.test.ts asserts: derivePlacement(0.8)→2, derivePlacement(0.79)→1; derivePassFail(0.8)→'pass', (0.79)→'fail'
    - scoring.test.ts asserts: drawQuestions returns exactly N items, no duplicates, all from pool; count > pool returns whole pool
    - gating.test.ts asserts: computeCooldownUntil adds 3h (completedAt + 3*60*60*1000)
    - gating.test.ts asserts: isCooldownActive true when now < cooldownUntil, false when null or past
    - gating.test.ts asserts: formatCooldownRemaining "2h 14m" and "45m" (no leading 0h); '' when expired
    - gating.test.ts asserts: deriveAllLessonsComplete true only when every id present + non-empty list; false for empty list
  </behavior>
  <action>
    Create `src/lib/diagnostics/types.ts` exporting: `QuestionType = 'mc' | 'fill_in'`, `AttemptStatus = 'in_progress' | 'completed' | 'failed'`, `DiagnosticType = 'placement' | 'end_of_level'`, and interfaces `DiagnosticQuestion` (id, level_id, type: QuestionType, question_text, options: string[] | null, correct_answer: string, lesson_tag: string | null, position: number), `DiagnosticAttempt` (id, user_id, level_id, diagnostic_type: DiagnosticType, status: AttemptStatus, drawn_question_ids: string[], score: number | null, correct_count: number | null, total_count: number | null, started_at: string, completed_at: string | null, cooldown_until: string | null, elapsed_seconds: number | null), and `GradeResult` ({ correct: boolean; accentNote?: string }). Types only — no runtime imports. NOTE in a comment that `correct_answer` is on the type but is NEVER selected in client-facing queries (Pitfall 1). Then create both test files importing from `@/lib/diagnostics/scoring` and `@/lib/diagnostics/gating` with the assertions above. These imports will fail to resolve until Task 2 — that is the intended RED state. Use the `@/` path alias and `__tests__/diagnostic/` directory (mirror `__tests__/lessons/`). For gradeAnswer fill_in tests, construct a `DiagnosticQuestion` with `type: 'fill_in'`, `correct_answer: 'café'`, `options: null`; for mc tests use `type: 'mc'`, `correct_answer: 'le'`, `options: ['le','la','les']`.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="diagnostic" 2>&1 | grep -E "Cannot find module|FAIL" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `src/lib/diagnostics/types.ts` exists and exports `DiagnosticQuestion`, `DiagnosticAttempt`, `GradeResult`, `QuestionType`, `AttemptStatus`, `DiagnosticType`
    - SOURCE: `__tests__/diagnostic/scoring.test.ts` and `__tests__/diagnostic/gating.test.ts` exist with `describe`/`test` blocks covering every behavior listed above
    - BEHAVIOR (RED): `npx jest --testPathPattern="diagnostic"` fails with module-not-found for `@/lib/diagnostics/scoring` and `@/lib/diagnostics/gating` (functions not yet implemented)
    - CLI: types.ts contains a comment noting correct_answer must never appear in client-facing select projections
  </acceptance_criteria>
  <done>Domain types exist; both diagnostic test files exist and currently fail because scoring.ts/gating.ts do not exist yet (RED state confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scoring.ts + gating.ts pure functions (GREEN)</name>
  <files>src/lib/diagnostics/scoring.ts, src/lib/diagnostics/gating.ts</files>
  <read_first>
    - src/lib/diagnostics/types.ts (the types created in Task 1)
    - __tests__/diagnostic/scoring.test.ts, __tests__/diagnostic/gating.test.ts (the RED tests to satisfy)
    - .planning/phases/04-diagnostic-system/04-RESEARCH.md (§Pattern 3 Pure Scoring Functions, §Code Examples — normalization/shuffle/cooldown)
    - .planning/phases/04-diagnostic-system/04-PATTERNS.md (§scoring.ts, §gating.ts — exact implementations)
  </read_first>
  <action>
    Create `src/lib/diagnostics/scoring.ts` (file-comment header per locking.ts: "Pure function — no imports beyond local types, no side effects, no DB access"). Implement: `normalizeFillin(raw)` — if `raw.trim() === ''` return `''`, else `raw.trim().toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '')` (strip combining diacritical marks, U+0300–U+036F). `gradeAnswer(question, submitted)` — blank guard returns `{ correct: false }` first (D-D03 leniency does NOT apply to blanks, Pitfall 6); for `type === 'mc'` return `{ correct: submitted === question.correct_answer }`; for fill_in compare `normalizeFillin(submitted) === normalizeFillin(question.correct_answer)` and set `accentNote = question.correct_answer` only when correct AND `submitted.trim().toLowerCase() !== question.correct_answer.toLowerCase()`. `computeScore(results)` — `results.length === 0 ? 0 : correctCount/results.length`. `derivePlacement(score)` — `score >= 0.8 ? 2 : 1` (D-P03). `derivePassFail(score)` — `score >= 0.8 ? 'pass' : 'fail'` (D-E02). `drawQuestions<T>(pool, count)` — Fisher-Yates shuffle of a copy then `slice(0, Math.min(count, pool.length))` (D-D04, D-E04). Import `DiagnosticQuestion`, `GradeResult` as types from `./types`. Create `src/lib/diagnostics/gating.ts` with `computeCooldownUntil(completedAt, cooldownHours)` → `new Date(completedAt.getTime() + cooldownHours*60*60*1000)`; `isCooldownActive(cooldownUntil, now = new Date())` → false if null else `now < cooldownUntil` (D-U03); `formatCooldownRemaining(cooldownUntil, now = new Date())` → '' if ms<=0, else ceil to minutes, `Hh Mm` when hours>0 else `Mm` (no seconds per UI-SPEC); `deriveAllLessonsComplete(subComponentIds, completedSet)` → `subComponentIds.length > 0 && subComponentIds.every(id => completedSet.has(id))` (D-E01). All functions pure — no DB, no Next imports.
  </action>
  <verify>
    <automated>npx jest --testPathPattern="diagnostic"</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR (GREEN): `npx jest --testPathPattern="diagnostic/scoring"` passes all scoring assertions
    - BEHAVIOR (GREEN): `npx jest --testPathPattern="diagnostic/gating"` passes all gating assertions
    - SOURCE: scoring.ts and gating.ts have zero imports from `@/lib/supabase`, `next/*`, or any DB module (grep returns no matches)
    - CLI: `grep -nE "from '@/lib/supabase|from 'next" src/lib/diagnostics/scoring.ts src/lib/diagnostics/gating.ts | grep -v '^#'` returns nothing
    - BEHAVIOR: matches 04-VALIDATION.md seams — derivePlacement, normalizeFillin, gradeAnswer (MC + fill-in + blank + accentNote), computeScore, derivePassFail, drawQuestions, computeCooldownUntil, isCooldownActive, formatCooldownRemaining, deriveAllLessonsComplete all green
  </acceptance_criteria>
  <done>All diagnostic scoring + gating tests pass; functions are pure (no DB/Next imports). Wave 0 scoring/gating seams from 04-VALIDATION.md are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | This plan contains only pure functions and unit tests. No untrusted input crosses a boundary at runtime here; the security boundary is enforced in Plans 03/04/05 (Server Actions + RLS). The pure functions are the *mechanism* those boundaries rely on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | gradeAnswer / computeScore (relied on by Plan 03/05 server-side scoring) | mitigate | These functions are pure and run server-side only in later plans; they never trust a client-reported score. This plan guarantees correctness via unit tests so the server-side recomputation in Plan 03/05 is trustworthy. |
| T-04-02 | Information Disclosure | DiagnosticQuestion.correct_answer field | mitigate | Type carries correct_answer but a code comment mandates it is never in client-facing select projections; enforced in Plans 03/05 query tasks. |
| T-04-SC | Tampering | npm installs | mitigate | No new packages installed (04-RESEARCH.md Package Legitimacy Audit: none). No install task in this plan. |
</threat_model>

<verification>
- `npx jest --testPathPattern="diagnostic"` — all scoring + gating tests green
- `npx jest` — full suite (77 existing + new diagnostic tests) stays green (no regressions)
- scoring.ts / gating.ts contain no Supabase or Next imports (pure)
</verification>

<success_criteria>
- Diagnostic domain types exist and are imported by scoring.ts
- All pure-function seams from 04-VALIDATION.md (scoring + gating) have passing tests
- Fill-in leniency (D-D03), blank guard (Pitfall 6), accent note, placement/pass-fail thresholds (D-P03/D-E02), cooldown math, and all-lessons-complete derivation all verified
- No regression in the existing 77-test suite
</success_criteria>

<output>
Create `.planning/phases/04-diagnostic-system/04-01-SUMMARY.md` when done
</output>
